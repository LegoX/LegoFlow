[{"task_name": "actionshrimp__vim-xpath-17", "repo": "actionshrimp/vim-xpath", "language": "Python", "difficulty": "medium", "area": "backend", "topic": "python3", "bug_class": "language-migration", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "The vim-xpath plugin currently fails to function in modern Vim environments where only Python 3 is available, as it hard-codes an exit if `+python` is missing without checking for `+python3`. The plugin needs to be updated to support Python 3, ensuring compatibility when the `+python3` feature is enabled. This involves migrating the Python scripts within the plugin from Python 2 syntax (e.g., print statements, string handling) to Python 3 compliant code, and updating the Vim script to detect and use the `+python3` interpreter if available. Users currently encounter an immediate exit or error when loading the plugin in a Python 3-only environment.", "bytes": 654, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src/python\n\nexport CI=true\nsource /opt/venv/bin/activate\n\n# Copy HEAD test files from /tests (overwrites BASE state)\ncp \"/tests/python/vim_xpath/tests/namespace_prefix_guesser_tests.py\" \"vim_xpath/tests/namespace_prefix_guesser_tests.py\"\ncp \"/tests/python/vim_xpath/tests/xpath_tests.py\" \"vim_xpath/tests/xpath_tests.py\"\n\n# Tests import from helpers.file_helpers (not relative) and from vim_xpath,\n# so both python/ and python/vim_xpath/tests/ need to be on sys.path.\ncd /app/src/python/vim_xpath/tests\nPYTHONPATH=/app/src/python/vim_xpath/tests:/app/src/python pytest -xvs namespace_prefix_guesser_tests.py xpath_tests.py\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 794, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/python/vim_xpath/xpath.py b/python/vim_xpath/xpath.py\n--- a/python/vim_xpath/xpath.py\n+++ b/python/vim_xpath/xpath.py\n@@ -21,7 +21,7 @@ def _evaluate(xml, xpath, namespaces=dict(), compiled_xpath=None):\n     Reports line numbers correctly on xml with over 65534 lines\"\"\"\n\n     try:\n-        tree = etree.fromstring(xml)\n+        tree = etree.fromstring(xml.encode(\"utf-8\") if isinstance(xml, str) else xml)\n     except Exception as e:\n         raise from_lxml_parse_exception(e)\n\n@@ -92,7 +92,7 @@ def _output_line_number(match):\n     if isinstance(match, etree._Element):\n         sourceline = match.sourceline\n\n-    if isinstance(match, etree._ElementStringResult):\n+    if isinstance(match, etree._ElementUnicodeResult):\n         parent = match.getparent()\n         if parent is not None:\n             sourceline = parent.sourceline\n@@ -107,7 +107,7 @@ def _output_match(match, namespaces):\n         prefixed_name = _prefixed_name_from_absolute_name(match.tag, namespaces)\n         out = \"<{0}>\".format(prefixed_name)\n\n-    elif isinstance(match, etree._ElementStringResult):\n+    elif isinstance(match, etree._ElementUnicodeResult):\n         if match.is_attribute:\n             prefixed_name = _prefixed_name_from_absolute_name(match.attrname, namespaces)\n             out = \"@{0}\".format(prefixed_name)\n@@ -137,7 +137,10 @@ def _output_value(match):\n     else:\n         value_text = match\n\n-    return value_text.encode('utf-8')\n+    # Ensure we return a string, not bytes (lxml returns bytes when input is bytes)\n+    if isinstance(value_text, bytes):\n+        value_text = value_text.decode(\"utf-8\")\n+    return value_text\n\n def _prefixed_name_from_absolute_name(name, namespaces):\n     \"\"\"Convert an lxml {namespaceuri}Name to prefix:Name,\n@@ -147,7 +150,7 @@ def _prefixed_name_from_absolute_name(name, namespaces):\n         uri = regex_match.group(1)\n         uri_with_braces = regex_match.group(0)\n\n-        prefix = [k for k, v in namespaces.items() if v == uri][0]\n+        prefix = [k for k, v in list(namespaces.items()) if v == uri][0]\n         prefixed = name.replace(uri_with_braces, \"{0}:\".format(prefix))\n\n         return prefixed\ndiff --git a/python/vim_xpath/namespace_prefix_guesser.py b/python/vim_xpath/namespace_prefix_guesser.py\n--- a/python/vim_xpath/namespace_prefix_guesser.py\n+++ b/python/vim_xpath/namespace_prefix_guesser.py\n@@ -21,12 +21,12 @@ def _guess_prefixes(xml):\n\n     prefixes = {}\n\n-    tree = etree.fromstring(xml)\n+    tree = etree.fromstring(xml.encode(\"utf-8\") if isinstance(xml, str) else xml)\n\n     for el in tree.iter():\n         node_prefixes = el.nsmap\n         if node_prefixes is not None:\n-            for prefix in node_prefixes.keys():\n+            for prefix in list(node_prefixes.keys()):\n                 url = node_prefixes[prefix]\n\n                 if prefix is None:\n", "bytes": 2843, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/autoload/xpath.vim b/autoload/xpath.vim\nindex 5393420..df79d7e 100644\n--- a/autoload/xpath.vim\n+++ b/autoload/xpath.vim\n@@ -4,7 +4,7 @@ if exists(\"g:skip_xpath\")\n endif\n \n \"Check python is installed\n-if !has(\"python3\")\n+if !has(\"python\")\n     if !exists(\"g:quiet_xpath\")\n         echo 'vim-xpath requires vim to be compiled with python support, and '\n                     \\ . 'python to be installed. To stop this message from '\n@@ -18,7 +18,7 @@ endif\n \"Check python lxml library is installed\n let s:no_lxml = 0\n \n-py3 << EOF\n+py << EOF\n import vim\n try:\n     import lxml\n@@ -45,7 +45,7 @@ let s:xpath_search_history = []\n \n \" 24.02.16 sfx2k\n \" Fix #12: join path-elements with python\n-py3 << EOF\n+py << EOF\n import vim\n import os\n \n@@ -55,14 +55,14 @@ vim.command(\"let s:pyfile='\" + main_file + \"'\")\n EOF\n \n \"Pass script location into script itself as an argument\n-execute \"py3file \" . s:pyfile\n+execute \"pyfile \" . s:pyfile\n \n function! xpath#XPathGuessPrefixes()\n     let l:active_window = winnr()\n     let l:active_buffer = winbufnr(l:active_window)\n \n     try\n-        execute \"py3 vim_adaptor.guess_prefixes(\" . l:active_buffer . \")\"\n+        execute \"py vim_adaptor.guess_prefixes(\" . l:active_buffer . \")\"\n         call XPathSetBufferPrefixes(l:ns_prefixes)\n     catch\n     endtry\n@@ -200,7 +200,7 @@ endf\n function! XPathEvaluate(xpath, active_buffer, active_window)\n     let l:ns_prefixes = getbufvar(a:active_buffer, \"ns_prefixes\")\n     let l:xpath = escape(a:xpath, \"'\\\\\")\n-    execute \"py3 vim_adaptor.evaluate_xpath(\" .\n+    execute \"py vim_adaptor.evaluate_xpath(\" .\n         \\ a:active_buffer . \", \" .\n         \\ a:active_window . \", \" .\n         \\ \"'\" . l:xpath . \"', \" .\ndiff --git a/python/vim_adaptor.py b/python/vim_adaptor.py\nindex eb0602b..096002a 100644\n--- a/python/vim_adaptor.py\n+++ b/python/vim_adaptor.py\n@@ -3,7 +3,7 @@ try:\n     import vim\n except ImportError:\n     vim = None\n-\n+    \n from vim_xpath import xpath as x\n from vim_xpath import namespace_prefix_guesser as g\n from vim_xpath.exceptions import XPathError\n@@ -28,14 +28,13 @@ def evaluate_xpath(bufnr, winnr, xpath, ns_prefixes={}):\n     loc_list = VimLocListAdaptor(bufnr, winnr)\n     loc_list.clear_current_list()\n     loc_list.add_text_entry(\"Results for: \" + xpath)\n-\n-    xml = get_buffer_string(bufnr).encode()\n+    \n+    xml = get_buffer_string(bufnr)\n \n     try:\n         results = x.evaluate(xml, xpath, ns_prefixes)\n         if len(results) > 0:\n             for result in results:\n-                result[\"value\"] = result[\"value\"].decode(\"utf-8\")\n                 loc_list.add_result_entry(result)\n         else:\n             loc_list.add_error_entry('No results returned')\n@@ -49,7 +48,7 @@ def evaluate_xpath(bufnr, winnr, xpath, ns_prefixes={}):\n \n def guess_prefixes(bufnr):\n     try:\n-        xml = get_buffer_string(bufnr).encode()\n+        xml = get_buffer_string(bufnr)\n         prefixes = g.guess_prefixes(xml)\n \n         outstr = \"let l:ns_prefixes = {\"\ndiff --git a/python/vim_xpath/namespace_prefix_guesser.py b/python/vim_xpath/namespace_prefix_guesser.py\nindex a5fb0c1..84db541 100644\n--- a/python/vim_xpath/namespace_prefix_guesser.py\n+++ b/python/vim_xpath/namespace_prefix_guesser.py\n@@ -26,7 +26,7 @@ def _guess_prefixes(xml):\n     for el in tree.iter():\n         node_prefixes = el.nsmap\n         if node_prefixes is not None:\n-            for prefix in list(node_prefixes.keys()):\n+            for prefix in node_prefixes.keys():\n                 url = node_prefixes[prefix]\n \n                 if prefix is None:\ndiff --git a/python/vim_xpath/tests/namespace_prefix_guesser_tests.py b/python/vim_xpath/tests/namespace_prefix_guesser_tests.py\nindex b675bf3..642f938 100644\n--- a/python/vim_xpath/tests/namespace_prefix_guesser_tests.py\n+++ b/python/vim_xpath/tests/namespace_prefix_guesser_tests.py\n@@ -1,6 +1,6 @@\n import unittest\n \n-from .helpers.file_helpers import read_sample_xml\n+from helpers.file_helpers import read_sample_xml\n \n from vim_xpath import namespace_prefix_guesser\n \ndiff --git a/python/vim_xpath/tests/xpath_tests.py b/python/vim_xpath/tests/xpath_tests.py\nindex 42ad89d..1140041 100644\n--- a/python/vim_xpath/tests/xpath_tests.py\n+++ b/python/vim_xpath/tests/xpath_tests.py\n@@ -1,6 +1,6 @@\n import unittest\n \n-from .helpers.file_helpers import read_sample_xml\n+from helpers.file_helpers import read_sample_xml\n \n from vim_xpath import xpath\n from vim_xpath.exceptions import XPathError, BufferXmlError\ndiff --git a/python/vim_xpath/xpath.py b/python/vim_xpath/xpath.py\nindex e179080..1e53782 100644\n--- a/python/vim_xpath/xpath.py\n+++ b/python/vim_xpath/xpath.py\n@@ -147,7 +147,7 @@ def _prefixed_name_from_absolute_name(name, namespaces):\n         uri = regex_match.group(1)\n         uri_with_braces = regex_match.group(0)\n \n-        prefix = [k for k, v in list(namespaces.items()) if v == uri][0]\n+        prefix = [k for k, v in namespaces.items() if v == uri][0]\n         prefixed = name.replace(uri_with_braces, \"{0}:\".format(prefix))\n \n         return prefixed\n", "bytes": 5027, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \\\n    git curl ca-certificates patch python3 python3-venv && \\\n    rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone --depth 1 https://github.com/actionshrimp/vim-xpath.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 880e5a0af9dde157b572dff38e33ca78a56ac72e || git fetch --depth 1 origin \"+refs/pull/17/head:refs/remotes/origin/pr/17\") && \\\n    git checkout --detach FETCH_HEAD\n\nWORKDIR /app/src\n\nRUN uv venv /opt/venv && \\\n    uv pip install --python /opt/venv/bin/python lxml pytest\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 905, "truncated": false}]}, {"task_name": "adbautoplayer__adbautoplayer-216", "repo": "adbautoplayer/adbautoplayer", "language": "Python", "difficulty": "medium", "area": "library", "topic": "string-matching", "bug_class": "fuzzy-search", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "Implement a fuzzy substring matching function to handle popup message detection more robustly, addressing cases where exact string matching fails (e.g., 'Legend Trial' or 'Duras Trial' warnings).\n\nImplement `StringHelper.fuzzy_substring_match(text: str, pattern: str, threshold: ConfidenceValue = ConfidenceValue('80%')) -> bool`.\n\nThis function should determine if `pattern` appears within `text` with a similarity score at or above the given `threshold`. Key requirements:\n1. The matching should be case-insensitive.\n2. It should support fuzzy matching (allowing for slight typos or variations like 'hallo' matching 'hello') based on a configurable confidence threshold.\n3. If the `pattern` is empty, it should return `True`.\n4. If `text` is empty and `pattern` is not, it should return `False`.\n5. If `pattern` is longer than `text`, it should return `False`.\n6. The `threshold` is provided as a `ConfidenceValue` object (e.g., '80%', '90%') and defaults to 80%.\n\nThis function will be used to detect popup messages that may have slight variations in text, preventing `WarningException` issues when trial messages change slightly.", "bytes": 1133, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src\n\nexport CI=true\n\n# Copy test files from /tests into the source tree\nmkdir -p \"python/tests/utils/string_helper\"\ncp \"/tests/python/tests/utils/string_helper/test_fuzzy_substring_match.py\" \"python/tests/utils/string_helper/test_fuzzy_substring_match.py\"\n\n# Run the specific test for the fuzzy_substring_match method\n/opt/venv/bin/python -m pytest -xvs \"python/tests/utils/string_helper/test_fuzzy_substring_match.py\" 2>&1 | tee /tmp/pytest_output.txt\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 624, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/python/adb_auto_player/games/afk_journey/base.py b/python/adb_auto_player/games/afk_journey/base.py\nindex 18cc77f5..43781bc0 100644\n--- a/python/adb_auto_player/games/afk_journey/base.py\n+++ b/python/adb_auto_player/games/afk_journey/base.py\n@@ -19,7 +19,6 @@ from adb_auto_player.models import ConfidenceValue\n from adb_auto_player.models.decorators import GameGUIMetadata\n from adb_auto_player.models.geometry import Point\n from adb_auto_player.models.image_manipulation import CropRegions\n-from adb_auto_player.models.template_matching import MatchMode\n from adb_auto_player.util import SummaryGenerator\n \n from .afkjourneynavigation import AFKJourneyNavigation\n@@ -305,15 +304,18 @@ class AFKJourneyBase(AFKJourneyNavigation, AFKJourneyPopupHandler, Game):\n         Returns:\n             str | None: Name of excluded hero\n         \"\"\"\n-        result = self.find_any_template(\n-            templates=list(excluded_heroes.keys()),\n-            crop_regions=CropRegions(left=0.1, right=0.2, top=0.3, bottom=0.4),\n-        )\n-        if result is None:\n+        sleep(0.5)\n+        try:\n+            result = self.wait_for_any_template(\n+                templates=list(excluded_heroes.keys()),\n+                crop_regions=CropRegions(left=0.1, right=0.2, top=0.3, bottom=0.4),\n+                timeout=1.0,\n+                delay=0.5,\n+            )\n+            return excluded_heroes.get(result.template)\n+        except GameTimeoutError:\n             return None\n \n-        return excluded_heroes.get(result.template)\n-\n     def _start_battle(self) -> bool:\n         \"\"\"Begin battle.\n \n@@ -353,25 +355,14 @@ class AFKJourneyBase(AFKJourneyNavigation, AFKJourneyPopupHandler, Game):\n             else:\n                 self._click_confirm_on_popup()\n \n-        while self.find_any_template(\n-            [\n-                \"battle/no_hero_is_placed_on_the_talent_buff_tile.png\",\n-                \"duras_trials/blessed_heroes_specific_tiles.png\",\n-            ],\n-        ):\n-            checkbox = self.game_find_template_match(\n-                \"popup/checkbox_unchecked.png\",\n-                match_mode=MatchMode.TOP_LEFT,\n-                crop_regions=CropRegions(right=0.8, top=0.2, bottom=0.6),\n-                threshold=ConfidenceValue(\"80%\"),\n-            )\n-            if checkbox is None:\n-                logging.error('Could not find \"Don\\'t remind for x days\" checkbox')\n-            else:\n-                self.tap(checkbox)\n+        # Just handle however many popups show up\n+        # Needs a counter to prevent infinite loop on freeze though\n+        max_count = 10\n+        count = 0\n+        while self._click_confirm_on_popup() and count < max_count:\n             self._click_confirm_on_popup()\n-\n-        self._click_confirm_on_popup()\n+            count += 1\n+            sleep(0.5)\n         return True\n \n     def _click_confirm_on_popup(self) -> bool:\n@@ -380,7 +371,10 @@ class AFKJourneyBase(AFKJourneyNavigation, AFKJourneyPopupHandler, Game):\n         Returns:\n             bool: True if confirmed, False if not.\n         \"\"\"\n-        self.handle_confirmation_popups()\n+        if self.handle_confirmation_popups():\n+            return True\n+\n+        # Legacy code keeping it as a fallback\n         result = self.find_any_template(\n             templates=[\"navigation/confirm.png\", \"confirm_text.png\"],\n             crop_regions=CropRegions(top=0.4),\ndiff --git a/python/adb_auto_player/games/afk_journey/popup_handler.py b/python/adb_auto_player/games/afk_journey/popup_handler.py\nindex 0c1aff59..a0cb44ae 100644\n--- a/python/adb_auto_player/games/afk_journey/popup_handler.py\n+++ b/python/adb_auto_player/games/afk_journey/popup_handler.py\n@@ -2,16 +2,17 @@ import logging\n import time\n from abc import ABC\n from dataclasses import dataclass\n-from difflib import SequenceMatcher\n \n import numpy as np\n from adb_auto_player import Game\n+from adb_auto_player.exceptions import AutoPlayerWarningError\n from adb_auto_player.image_manipulation import Color, ColorFormat\n from adb_auto_player.models import ConfidenceValue\n from adb_auto_player.models.geometry import Point\n from adb_auto_player.models.image_manipulation import CropRegions\n from adb_auto_player.models.template_matching import MatchMode, TemplateMatchResult\n from adb_auto_player.ocr import PSM, TesseractBackend, TesseractConfig\n+from adb_auto_player.util import StringHelper\n \n \n @dataclass(frozen=True)\n@@ -23,56 +24,51 @@ class PopupMessage:\n     hold_to_confirm: bool = False\n     hold_duration_seconds: float = 5.0\n     ignore: bool = False\n+    exception_to_raise: Exception | None = None\n \n \n-# You do not actually need to add the whole text of the Popup Message\n-# A snippet works.\n-popup_messages: list[PopupMessage] = [\n-    # Exit Game\n+season_talent_messages = [\n     PopupMessage(\n-        text=\"Are you sure you want to exit the game\",\n-        # Does not have \"remind me\" checkbox\n-    ),\n-    # Season Talent Stages\n-    PopupMessage(\n-        text=(\n-            \"You haven't activated any Season Faction Talent.\"\n-            # Do you want to start the battle anyway?\n-        ),\n+        # You haven't activated any Season Faction Talent.\n+        # Do you want to start the battle anyway?\n+        text=\"You haven't activated any Season Faction Talent\",\n         click_dont_remind_me=True,\n     ),\n     PopupMessage(\n-        text=(\n-            \"No hero is placed on the Talent Buff Tile. \"\n-            \"Do you want to start the battle anyways?\"\n-        ),\n+        # No hero is placed on the Talent Buff Tile.\n+        # Do you want to start the battle anyways?\n+        text=(\"No hero is placed on the Talent Buff Tile\"),\n         click_dont_remind_me=True,\n     ),\n-    # General Battle\n+]\n+\n+general_battle_messages = [\n     PopupMessage(\n         text=\"Skip this battle?\",\n-        # Does not have \"remind me\" checkbox\n+        click_dont_remind_me=False,\n     ),\n     PopupMessage(\n-        text=\"Your formation is incomplete.\",\n         # There are 2 different messages for this\n         # \"Your formation is incomplete. Begin battle anyway?\"\n         # \"Your formation is incomplete. Turn on Auto Battle mode anyway? ...\"\n+        text=\"Your formation is incomplete\",\n         click_dont_remind_me=True,\n     ),\n-    # Arena\n+]\n+\n+arena_messages = [\n     PopupMessage(\n-        text=\"Spend to purchase Warrior's Guarantee\",\n         # Daily attempts: x/5\n+        text=\"Spend to purchase Warrior's Guarantee\",\n         ignore=True,\n     ),\n-    # Arcane Labyrinth\n+]\n+\n+arcane_labyrinth_messages = [\n     PopupMessage(\n-        text=\"Do you still want to start your exploration?\",\n-        # partial text because full text did not get detected, but does not matter.\n-        # claimed the Clear Rewards of current difficulty.\n         # You won't receive any rewards for attempting this difficulty outside of\n         # the event period. Do you still want to start your exploration?\n+        text=\"Do you still want to start your exploration?\",\n         click_dont_remind_me=False,  # I think it does not have one\n     ),\n     PopupMessage(\n@@ -80,47 +76,78 @@ popup_messages: list[PopupMessage] = [\n         confirm_button_template=\"arcane_labyrinth/hold_to_exit.png\",\n         hold_to_confirm=True,\n         hold_duration_seconds=5.0,\n-        # Does not have \"remind me\" checkbox\n+        click_dont_remind_me=False,\n     ),\n-    # Duras Trials\n+]\n+\n+duras_trials_messages = [\n     PopupMessage(\n-        text=\"Keep challenging this stage?\",\n         # You have made multiple attempts.\n         # Keep challenging this stage?\n         # Challenge Attempts: x\n-        click_dont_remind_me=False,  # I think it does not have one\n+        text=\"Keep challenging this stage?\",\n+        click_dont_remind_me=False,\n     ),\n     PopupMessage(\n-        # possibly appears in other places too\n+        # Spend x to challenge this stage again?\n         text=\"to challenge this stage again?\",\n-        # Spend 2000 to challenge this stage again?\n         ignore=True,  # handled elsewhere\n+        # possibly appears in other places\n     ),\n     PopupMessage(\n-        # Should probably throw an exception or something\n-        text=\"Please wait for the reset\",\n         # Multiple attempts made. Please wait for the reset.\n-        ignore=True,\n+        text=\"Please wait for the reset\",\n+        exception_to_raise=AutoPlayerWarningError(\n+            \"All attempts used, have to wait for reset.\"\n+        ),\n+    ),\n+    PopupMessage(\n+        text=\"Blessed Heroes are not deployed\",\n+        click_dont_remind_me=True,\n     ),\n-    # Emporium, other places?\n+]\n+\n+misc_messages = [\n     PopupMessage(\n+        # Emporium\n         text=\"Confirm to use Diamonds?\",\n         ignore=True,\n     ),\n-    # Fishing\n     PopupMessage(\n-        text=\"You are currently fishing.\",\n+        text=\"Are you sure you want to exit the game\",\n+        click_dont_remind_me=False,\n+    ),\n+]\n+\n+fishing_messages = [\n+    PopupMessage(\n+        # You are currently fishing.\n         # If you quit this fishing attempt will fail. Quit anyway?\n-        # Does not have \"remind me\" checkbox\n+        text=\"You are currently fishing\",\n+        click_dont_remind_me=False,\n     ),\n-    # Legend Trial\n+]\n+\n+legend_trial_messages = [\n     PopupMessage(\n-        text=\"Legend Trial has been refreshed.\",\n+        text=\"Legend Trial has been refreshed\",\n         ignore=True,\n-        # Would make sense to throw an exception for this\n+        exception_to_raise=AutoPlayerWarningError(\"Legend Trial has been refreshed.\"),\n     ),\n ]\n \n+# Combine all messages into one list\n+popup_messages: list[PopupMessage] = (\n+    season_talent_messages\n+    + general_battle_messages\n+    + arena_messages\n+    + arcane_labyrinth_messages\n+    + duras_trials_messages\n+    + misc_messages\n+    + fishing_messages\n+    + legend_trial_messages\n+)\n+\n \n @dataclass(frozen=True)\n class PopupPreprocessResult:\n@@ -130,38 +157,6 @@ class PopupPreprocessResult:\n     dont_remind_me_checkbox: TemplateMatchResult | None = None\n \n \n-def _fuzzy_substring_match(\n-    text: str,\n-    pattern: str,\n-    similarity_threshold: ConfidenceValue = ConfidenceValue(\"80%\"),\n-) -> bool:\n-    \"\"\"Check if pattern exists as a fuzzy substring in text.\n-\n-    Args:\n-        text: The text to search in (OCR result)\n-        pattern: The pattern to search for (popup message text)\n-        similarity_threshold: Minimum similarity ratio (0.0 to 1.0)\n-\n-    Returns:\n-        bool: True if fuzzy match found, False otherwise\n-    \"\"\"\n-    text_lower = text.lower()\n-    pattern_lower = pattern.lower()\n-\n-    # If pattern is longer than text, no match possible\n-    if len(pattern_lower) > len(text_lower):\n-        return False\n-\n-    # Check all possible substrings of text with the same length as pattern\n-    for i in range(len(text_lower) - len(pattern_lower) + 1):\n-        substring = text_lower[i : i + len(pattern_lower)]\n-        similarity = SequenceMatcher(None, substring, pattern_lower).ratio()\n-        if similarity >= similarity_threshold:\n-            return True\n-\n-    return False\n-\n-\n def _find_matching_popup(\n     ocr_text: str, similarity_threshold: ConfidenceValue = ConfidenceValue(\"80%\")\n ) -> PopupMessage | None:\n@@ -175,20 +170,28 @@ def _find_matching_popup(\n         PopupMessage or None if no match found\n     \"\"\"\n     for popup in popup_messages:\n-        if _fuzzy_substring_match(ocr_text, popup.text, similarity_threshold):\n+        if StringHelper.fuzzy_substring_match(\n+            ocr_text,\n+            popup.text,\n+            similarity_threshold,\n+        ):\n             return popup\n     return None\n \n \n class AFKJourneyPopupHandler(Game, ABC):\n-    def handle_confirmation_popups(self) -> None:\n+    def handle_confirmation_popups(self) -> bool:\n         \"\"\"Handles multiple popups.\"\"\"\n         max_popups = 5\n         count = 0\n+\n+        result = False\n         while count < max_popups and self._handle_confirmation_popup():\n             count += 1\n+            result = True\n+        return result\n \n-    def _handle_confirmation_popup(self) -> bool:\n+    def _handle_confirmation_popup(self) -> PopupMessage | None:\n         \"\"\"Confirm popups.\n \n         Returns:\n@@ -199,7 +202,7 @@ class AFKJourneyPopupHandler(Game, ABC):\n         image = self.get_screenshot()\n         preprocess_result = self._preprocess_for_popup(image)\n         if not preprocess_result:\n-            return False\n+            return None\n \n         ocr_results = ocr.detect_text_blocks(\n             image=preprocess_result.cropped_image, min_confidence=ConfidenceValue(\"80%\")\n@@ -222,10 +225,13 @@ class AFKJourneyPopupHandler(Game, ABC):\n                 \"Unknown popup detected, \"\n                 f\"please post on Discord so it can be added: {ocr_results}\"\n             )\n-            return False\n+            return None\n+\n+        if matching_popup.exception_to_raise:\n+            raise matching_popup.exception_to_raise\n \n         if matching_popup.ignore:\n-            return False\n+            return None\n \n         if matching_popup.click_dont_remind_me:\n             if preprocess_result.dont_remind_me_checkbox:\n@@ -245,7 +251,7 @@ class AFKJourneyPopupHandler(Game, ABC):\n         result: PopupPreprocessResult,\n         popup: PopupMessage,\n         image: np.ndarray,\n-    ) -> bool:\n+    ) -> PopupMessage | None:\n         if result.button.template == popup.confirm_button_template:\n             button: TemplateMatchResult | None = result.button\n         else:\n@@ -255,14 +261,14 @@ class AFKJourneyPopupHandler(Game, ABC):\n             )\n \n         if not button:\n-            return False\n+            return None\n \n         if popup.hold_to_confirm:\n             self.hold(coordinates=button, duration=popup.hold_duration_seconds)\n         else:\n             self.tap(coordinates=button)\n         time.sleep(3)\n-        return True\n+        return popup\n \n     def _preprocess_for_popup(self, image: np.ndarray) -> PopupPreprocessResult | None:\n         height, width = image.shape[:2]\ndiff --git a/python/adb_auto_player/games/afk_journey/templates/battle/no_hero_is_placed_on_the_talent_buff_tile.png b/python/adb_auto_player/games/afk_journey/templates/battle/no_hero_is_placed_on_the_talent_buff_tile.png\ndeleted file mode 100644\nindex 85e7523b..00000000\nBinary files a/python/adb_auto_player/games/afk_journey/templates/battle/no_hero_is_placed_on_the_talent_buff_tile.png and /dev/null differ\ndiff --git a/python/adb_auto_player/games/afk_journey/templates/duras_trials/blessed_heroes_specific_tiles.png b/python/adb_auto_player/games/afk_journey/templates/duras_trials/blessed_heroes_specific_tiles.png\ndeleted file mode 100644\nindex 2e33fb3c..00000000\nBinary files a/python/adb_auto_player/games/afk_journey/templates/duras_trials/blessed_heroes_specific_tiles.png and /dev/null differ\ndiff --git a/python/adb_auto_player/util/string_helper.py b/python/adb_auto_player/util/string_helper.py\nindex ffe212ad..be7cdbd8 100644\n--- a/python/adb_auto_player/util/string_helper.py\n+++ b/python/adb_auto_player/util/string_helper.py\n@@ -1,6 +1,9 @@\n \"\"\"String utility functions and helpers.\"\"\"\n \n import os\n+from difflib import SequenceMatcher\n+\n+from adb_auto_player.models import ConfidenceValue\n \n \n class StringHelper:\n@@ -42,3 +45,35 @@ class StringHelper:\n             )\n \n         return module_path[2]\n+\n+    @staticmethod\n+    def fuzzy_substring_match(\n+        text: str,\n+        pattern: str,\n+        similarity_threshold: ConfidenceValue = ConfidenceValue(\"80%\"),\n+    ) -> bool:\n+        \"\"\"Check if pattern exists as a fuzzy substring in text.\n+\n+        Args:\n+            text: The text to search in (OCR result)\n+            pattern: The pattern to search for (popup message text)\n+            similarity_threshold: Minimum similarity ratio\n+\n+        Returns:\n+            bool: True if fuzzy match found, False otherwise\n+        \"\"\"\n+        text_lower = text.lower()\n+        pattern_lower = pattern.lower()\n+\n+        # If pattern is longer than text, no match possible\n+        if len(pattern_lower) > len(text_lower):\n+            return False\n+\n+        # Check all possible substrings of text with the same length as pattern\n+        for i in range(len(text_lower) - len(pattern_lower) + 1):\n+            substring = text_lower[i : i + len(pattern_lower)]\n+            similarity = SequenceMatcher(None, substring, pattern_lower).ratio()\n+            if similarity >= similarity_threshold:\n+                return True\n+\n+        return False\n", "bytes": 16506, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/python/adb_auto_player/games/afk_journey/base.py b/python/adb_auto_player/games/afk_journey/base.py\nindex 43781bc0..18cc77f5 100644\n--- a/python/adb_auto_player/games/afk_journey/base.py\n+++ b/python/adb_auto_player/games/afk_journey/base.py\n@@ -19,6 +19,7 @@ from adb_auto_player.models import ConfidenceValue\n from adb_auto_player.models.decorators import GameGUIMetadata\n from adb_auto_player.models.geometry import Point\n from adb_auto_player.models.image_manipulation import CropRegions\n+from adb_auto_player.models.template_matching import MatchMode\n from adb_auto_player.util import SummaryGenerator\n \n from .afkjourneynavigation import AFKJourneyNavigation\n@@ -304,18 +305,15 @@ class AFKJourneyBase(AFKJourneyNavigation, AFKJourneyPopupHandler, Game):\n         Returns:\n             str | None: Name of excluded hero\n         \"\"\"\n-        sleep(0.5)\n-        try:\n-            result = self.wait_for_any_template(\n-                templates=list(excluded_heroes.keys()),\n-                crop_regions=CropRegions(left=0.1, right=0.2, top=0.3, bottom=0.4),\n-                timeout=1.0,\n-                delay=0.5,\n-            )\n-            return excluded_heroes.get(result.template)\n-        except GameTimeoutError:\n+        result = self.find_any_template(\n+            templates=list(excluded_heroes.keys()),\n+            crop_regions=CropRegions(left=0.1, right=0.2, top=0.3, bottom=0.4),\n+        )\n+        if result is None:\n             return None\n \n+        return excluded_heroes.get(result.template)\n+\n     def _start_battle(self) -> bool:\n         \"\"\"Begin battle.\n \n@@ -355,14 +353,25 @@ class AFKJourneyBase(AFKJourneyNavigation, AFKJourneyPopupHandler, Game):\n             else:\n                 self._click_confirm_on_popup()\n \n-        # Just handle however many popups show up\n-        # Needs a counter to prevent infinite loop on freeze though\n-        max_count = 10\n-        count = 0\n-        while self._click_confirm_on_popup() and count < max_count:\n+        while self.find_any_template(\n+            [\n+                \"battle/no_hero_is_placed_on_the_talent_buff_tile.png\",\n+                \"duras_trials/blessed_heroes_specific_tiles.png\",\n+            ],\n+        ):\n+            checkbox = self.game_find_template_match(\n+                \"popup/checkbox_unchecked.png\",\n+                match_mode=MatchMode.TOP_LEFT,\n+                crop_regions=CropRegions(right=0.8, top=0.2, bottom=0.6),\n+                threshold=ConfidenceValue(\"80%\"),\n+            )\n+            if checkbox is None:\n+                logging.error('Could not find \"Don\\'t remind for x days\" checkbox')\n+            else:\n+                self.tap(checkbox)\n             self._click_confirm_on_popup()\n-            count += 1\n-            sleep(0.5)\n+\n+        self._click_confirm_on_popup()\n         return True\n \n     def _click_confirm_on_popup(self) -> bool:\n@@ -371,10 +380,7 @@ class AFKJourneyBase(AFKJourneyNavigation, AFKJourneyPopupHandler, Game):\n         Returns:\n             bool: True if confirmed, False if not.\n         \"\"\"\n-        if self.handle_confirmation_popups():\n-            return True\n-\n-        # Legacy code keeping it as a fallback\n+        self.handle_confirmation_popups()\n         result = self.find_any_template(\n             templates=[\"navigation/confirm.png\", \"confirm_text.png\"],\n             crop_regions=CropRegions(top=0.4),\ndiff --git a/python/adb_auto_player/games/afk_journey/popup_handler.py b/python/adb_auto_player/games/afk_journey/popup_handler.py\nindex a0cb44ae..0c1aff59 100644\n--- a/python/adb_auto_player/games/afk_journey/popup_handler.py\n+++ b/python/adb_auto_player/games/afk_journey/popup_handler.py\n@@ -2,17 +2,16 @@ import logging\n import time\n from abc import ABC\n from dataclasses import dataclass\n+from difflib import SequenceMatcher\n \n import numpy as np\n from adb_auto_player import Game\n-from adb_auto_player.exceptions import AutoPlayerWarningError\n from adb_auto_player.image_manipulation import Color, ColorFormat\n from adb_auto_player.models import ConfidenceValue\n from adb_auto_player.models.geometry import Point\n from adb_auto_player.models.image_manipulation import CropRegions\n from adb_auto_player.models.template_matching import MatchMode, TemplateMatchResult\n from adb_auto_player.ocr import PSM, TesseractBackend, TesseractConfig\n-from adb_auto_player.util import StringHelper\n \n \n @dataclass(frozen=True)\n@@ -24,51 +23,56 @@ class PopupMessage:\n     hold_to_confirm: bool = False\n     hold_duration_seconds: float = 5.0\n     ignore: bool = False\n-    exception_to_raise: Exception | None = None\n \n \n-season_talent_messages = [\n+# You do not actually need to add the whole text of the Popup Message\n+# A snippet works.\n+popup_messages: list[PopupMessage] = [\n+    # Exit Game\n     PopupMessage(\n-        # You haven't activated any Season Faction Talent.\n-        # Do you want to start the battle anyway?\n-        text=\"You haven't activated any Season Faction Talent\",\n+        text=\"Are you sure you want to exit the game\",\n+        # Does not have \"remind me\" checkbox\n+    ),\n+    # Season Talent Stages\n+    PopupMessage(\n+        text=(\n+            \"You haven't activated any Season Faction Talent.\"\n+            # Do you want to start the battle anyway?\n+        ),\n         click_dont_remind_me=True,\n     ),\n     PopupMessage(\n-        # No hero is placed on the Talent Buff Tile.\n-        # Do you want to start the battle anyways?\n-        text=(\"No hero is placed on the Talent Buff Tile\"),\n+        text=(\n+            \"No hero is placed on the Talent Buff Tile. \"\n+            \"Do you want to start the battle anyways?\"\n+        ),\n         click_dont_remind_me=True,\n     ),\n-]\n-\n-general_battle_messages = [\n+    # General Battle\n     PopupMessage(\n         text=\"Skip this battle?\",\n-        click_dont_remind_me=False,\n+        # Does not have \"remind me\" checkbox\n     ),\n     PopupMessage(\n+        text=\"Your formation is incomplete.\",\n         # There are 2 different messages for this\n         # \"Your formation is incomplete. Begin battle anyway?\"\n         # \"Your formation is incomplete. Turn on Auto Battle mode anyway? ...\"\n-        text=\"Your formation is incomplete\",\n         click_dont_remind_me=True,\n     ),\n-]\n-\n-arena_messages = [\n+    # Arena\n     PopupMessage(\n-        # Daily attempts: x/5\n         text=\"Spend to purchase Warrior's Guarantee\",\n+        # Daily attempts: x/5\n         ignore=True,\n     ),\n-]\n-\n-arcane_labyrinth_messages = [\n+    # Arcane Labyrinth\n     PopupMessage(\n+        text=\"Do you still want to start your exploration?\",\n+        # partial text because full text did not get detected, but does not matter.\n+        # claimed the Clear Rewards of current difficulty.\n         # You won't receive any rewards for attempting this difficulty outside of\n         # the event period. Do you still want to start your exploration?\n-        text=\"Do you still want to start your exploration?\",\n         click_dont_remind_me=False,  # I think it does not have one\n     ),\n     PopupMessage(\n@@ -76,78 +80,47 @@ arcane_labyrinth_messages = [\n         confirm_button_template=\"arcane_labyrinth/hold_to_exit.png\",\n         hold_to_confirm=True,\n         hold_duration_seconds=5.0,\n-        click_dont_remind_me=False,\n+        # Does not have \"remind me\" checkbox\n     ),\n-]\n-\n-duras_trials_messages = [\n+    # Duras Trials\n     PopupMessage(\n+        text=\"Keep challenging this stage?\",\n         # You have made multiple attempts.\n         # Keep challenging this stage?\n         # Challenge Attempts: x\n-        text=\"Keep challenging this stage?\",\n-        click_dont_remind_me=False,\n+        click_dont_remind_me=False,  # I think it does not have one\n     ),\n     PopupMessage(\n-        # Spend x to challenge this stage again?\n+        # possibly appears in other places too\n         text=\"to challenge this stage again?\",\n+        # Spend 2000 to challenge this stage again?\n         ignore=True,  # handled elsewhere\n-        # possibly appears in other places\n     ),\n     PopupMessage(\n-        # Multiple attempts made. Please wait for the reset.\n+        # Should probably throw an exception or something\n         text=\"Please wait for the reset\",\n-        exception_to_raise=AutoPlayerWarningError(\n-            \"All attempts used, have to wait for reset.\"\n-        ),\n-    ),\n-    PopupMessage(\n-        text=\"Blessed Heroes are not deployed\",\n-        click_dont_remind_me=True,\n+        # Multiple attempts made. Please wait for the reset.\n+        ignore=True,\n     ),\n-]\n-\n-misc_messages = [\n+    # Emporium, other places?\n     PopupMessage(\n-        # Emporium\n         text=\"Confirm to use Diamonds?\",\n         ignore=True,\n     ),\n+    # Fishing\n     PopupMessage(\n-        text=\"Are you sure you want to exit the game\",\n-        click_dont_remind_me=False,\n-    ),\n-]\n-\n-fishing_messages = [\n-    PopupMessage(\n-        # You are currently fishing.\n+        text=\"You are currently fishing.\",\n         # If you quit this fishing attempt will fail. Quit anyway?\n-        text=\"You are currently fishing\",\n-        click_dont_remind_me=False,\n+        # Does not have \"remind me\" checkbox\n     ),\n-]\n-\n-legend_trial_messages = [\n+    # Legend Trial\n     PopupMessage(\n-        text=\"Legend Trial has been refreshed\",\n+        text=\"Legend Trial has been refreshed.\",\n         ignore=True,\n-        exception_to_raise=AutoPlayerWarningError(\"Legend Trial has been refreshed.\"),\n+        # Would make sense to throw an exception for this\n     ),\n ]\n \n-# Combine all messages into one list\n-popup_messages: list[PopupMessage] = (\n-    season_talent_messages\n-    + general_battle_messages\n-    + arena_messages\n-    + arcane_labyrinth_messages\n-    + duras_trials_messages\n-    + misc_messages\n-    + fishing_messages\n-    + legend_trial_messages\n-)\n-\n \n @dataclass(frozen=True)\n class PopupPreprocessResult:\n@@ -157,6 +130,38 @@ class PopupPreprocessResult:\n     dont_remind_me_checkbox: TemplateMatchResult | None = None\n \n \n+def _fuzzy_substring_match(\n+    text: str,\n+    pattern: str,\n+    similarity_threshold: ConfidenceValue = ConfidenceValue(\"80%\"),\n+) -> bool:\n+    \"\"\"Check if pattern exists as a fuzzy substring in text.\n+\n+    Args:\n+        text: The text to search in (OCR result)\n+        pattern: The pattern to search for (popup message text)\n+        similarity_threshold: Minimum similarity ratio (0.0 to 1.0)\n+\n+    Returns:\n+        bool: True if fuzzy match found, False otherwise\n+    \"\"\"\n+    text_lower = text.lower()\n+    pattern_lower = pattern.lower()\n+\n+    # If pattern is longer than text, no match possible\n+    if len(pattern_lower) > len(text_lower):\n+        return False\n+\n+    # Check all possible substrings of text with the same length as pattern\n+    for i in range(len(text_lower) - len(pattern_lower) + 1):\n+        substring = text_lower[i : i + len(pattern_lower)]\n+        similarity = SequenceMatcher(None, substring, pattern_lower).ratio()\n+        if similarity >= similarity_threshold:\n+            return True\n+\n+    return False\n+\n+\n def _find_matching_popup(\n     ocr_text: str, similarity_threshold: ConfidenceValue = ConfidenceValue(\"80%\")\n ) -> PopupMessage | None:\n@@ -170,28 +175,20 @@ def _find_matching_popup(\n         PopupMessage or None if no match found\n     \"\"\"\n     for popup in popup_messages:\n-        if StringHelper.fuzzy_substring_match(\n-            ocr_text,\n-            popup.text,\n-            similarity_threshold,\n-        ):\n+        if _fuzzy_substring_match(ocr_text, popup.text, similarity_threshold):\n             return popup\n     return None\n \n \n class AFKJourneyPopupHandler(Game, ABC):\n-    def handle_confirmation_popups(self) -> bool:\n+    def handle_confirmation_popups(self) -> None:\n         \"\"\"Handles multiple popups.\"\"\"\n         max_popups = 5\n         count = 0\n-\n-        result = False\n         while count < max_popups and self._handle_confirmation_popup():\n             count += 1\n-            result = True\n-        return result\n \n-    def _handle_confirmation_popup(self) -> PopupMessage | None:\n+    def _handle_confirmation_popup(self) -> bool:\n         \"\"\"Confirm popups.\n \n         Returns:\n@@ -202,7 +199,7 @@ class AFKJourneyPopupHandler(Game, ABC):\n         image = self.get_screenshot()\n         preprocess_result = self._preprocess_for_popup(image)\n         if not preprocess_result:\n-            return None\n+            return False\n \n         ocr_results = ocr.detect_text_blocks(\n             image=preprocess_result.cropped_image, min_confidence=ConfidenceValue(\"80%\")\n@@ -225,13 +222,10 @@ class AFKJourneyPopupHandler(Game, ABC):\n                 \"Unknown popup detected, \"\n                 f\"please post on Discord so it can be added: {ocr_results}\"\n             )\n-            return None\n-\n-        if matching_popup.exception_to_raise:\n-            raise matching_popup.exception_to_raise\n+            return False\n \n         if matching_popup.ignore:\n-            return None\n+            return False\n \n         if matching_popup.click_dont_remind_me:\n             if preprocess_result.dont_remind_me_checkbox:\n@@ -251,7 +245,7 @@ class AFKJourneyPopupHandler(Game, ABC):\n         result: PopupPreprocessResult,\n         popup: PopupMessage,\n         image: np.ndarray,\n-    ) -> PopupMessage | None:\n+    ) -> bool:\n         if result.button.template == popup.confirm_button_template:\n             button: TemplateMatchResult | None = result.button\n         else:\n@@ -261,14 +255,14 @@ class AFKJourneyPopupHandler(Game, ABC):\n             )\n \n         if not button:\n-            return None\n+            return False\n \n         if popup.hold_to_confirm:\n             self.hold(coordinates=button, duration=popup.hold_duration_seconds)\n         else:\n             self.tap(coordinates=button)\n         time.sleep(3)\n-        return popup\n+        return True\n \n     def _preprocess_for_popup(self, image: np.ndarray) -> PopupPreprocessResult | None:\n         height, width = image.shape[:2]\ndiff --git a/python/adb_auto_player/games/afk_journey/templates/battle/no_hero_is_placed_on_the_talent_buff_tile.png b/python/adb_auto_player/games/afk_journey/templates/battle/no_hero_is_placed_on_the_talent_buff_tile.png\nnew file mode 100644\nindex 00000000..85e7523b\nBinary files /dev/null and b/python/adb_auto_player/games/afk_journey/templates/battle/no_hero_is_placed_on_the_talent_buff_tile.png differ\ndiff --git a/python/adb_auto_player/games/afk_journey/templates/duras_trials/blessed_heroes_specific_tiles.png b/python/adb_auto_player/games/afk_journey/templates/duras_trials/blessed_heroes_specific_tiles.png\nnew file mode 100644\nindex 00000000..2e33fb3c\nBinary files /dev/null and b/python/adb_auto_player/games/afk_journey/templates/duras_trials/blessed_heroes_specific_tiles.png differ\ndiff --git a/python/adb_auto_player/util/string_helper.py b/python/adb_auto_player/util/string_helper.py\nindex be7cdbd8..ffe212ad 100644\n--- a/python/adb_auto_player/util/string_helper.py\n+++ b/python/adb_auto_player/util/string_helper.py\n@@ -1,9 +1,6 @@\n \"\"\"String utility functions and helpers.\"\"\"\n \n import os\n-from difflib import SequenceMatcher\n-\n-from adb_auto_player.models import ConfidenceValue\n \n \n class StringHelper:\n@@ -45,35 +42,3 @@ class StringHelper:\n             )\n \n         return module_path[2]\n-\n-    @staticmethod\n-    def fuzzy_substring_match(\n-        text: str,\n-        pattern: str,\n-        similarity_threshold: ConfidenceValue = ConfidenceValue(\"80%\"),\n-    ) -> bool:\n-        \"\"\"Check if pattern exists as a fuzzy substring in text.\n-\n-        Args:\n-            text: The text to search in (OCR result)\n-            pattern: The pattern to search for (popup message text)\n-            similarity_threshold: Minimum similarity ratio\n-\n-        Returns:\n-            bool: True if fuzzy match found, False otherwise\n-        \"\"\"\n-        text_lower = text.lower()\n-        pattern_lower = pattern.lower()\n-\n-        # If pattern is longer than text, no match possible\n-        if len(pattern_lower) > len(text_lower):\n-            return False\n-\n-        # Check all possible substrings of text with the same length as pattern\n-        for i in range(len(text_lower) - len(pattern_lower) + 1):\n-            substring = text_lower[i : i + len(pattern_lower)]\n-            similarity = SequenceMatcher(None, substring, pattern_lower).ratio()\n-            if similarity >= similarity_threshold:\n-                return True\n-\n-        return False\ndiff --git a/python/tests/utils/string_helper/test_fuzzy_substring_match.py b/python/tests/utils/string_helper/test_fuzzy_substring_match.py\ndeleted file mode 100644\nindex cf36b82e..00000000\n--- a/python/tests/utils/string_helper/test_fuzzy_substring_match.py\n+++ /dev/null\n@@ -1,53 +0,0 @@\n-import pytest\n-from adb_auto_player.models import ConfidenceValue\n-from adb_auto_player.util import StringHelper\n-\n-\n-class TestFuzzySubstringMatch:\n-    \"\"\"Test cases for StringHelper.fuzzy_substring_match method.\"\"\"\n-\n-    @pytest.mark.parametrize(\n-        \"text,pattern,threshold,expected\",\n-        [\n-            # Exact matches\n-            (\"hello world\", \"hello\", \"80%\", True),\n-            (\"hello world\", \"world\", \"80%\", True),\n-            (\"HELLO WORLD\", \"hello\", \"80%\", True),  # case insensitive\n-            (\"hello world\", \"HELLO\", \"80%\", True),  # case insensitive\n-            # Fuzzy matches\n-            (\"hello world\", \"hallo\", \"90%\", False),  # below threshold\n-            (\"hello world\", \"hallo\", \"70%\", True),  # above threshold\n-            (\"hello world\", \"hellx\", \"90%\", False),  # below threshold\n-            (\"hello world\", \"hellx\", \"75%\", True),  # above threshold\n-            # Partial matches\n-            (\"hello world\", \"hello worl\", \"80%\", True),\n-            (\"hello world\", \"ello wor\", \"80%\", True),\n-            # Edge cases\n-            (\"hello world\", \"\", \"80%\", True),  # empty pattern\n-            (\"\", \"hello\", \"80%\", False),  # empty text\n-            (\"short\", \"longer text\", \"80%\", False),  # pattern longer than text\n-            # Threshold tests\n-            (\"hello world\", \"h3llo\", \"60%\", True),\n-            (\"hello world\", \"h3llo\", \"70%\", True),\n-            (\"hello world\", \"he11o\", \"50%\", True),\n-            (\"hello world\", \"he11o\", \"80%\", False),\n-        ],\n-    )\n-    def test_fuzzy_substring_match(self, text, pattern, threshold, expected):\n-        \"\"\"Test fuzzy substring matching with various scenarios.\"\"\"\n-        threshold = ConfidenceValue(threshold)\n-        assert StringHelper.fuzzy_substring_match(text, pattern, threshold) == expected\n-\n-    def test_default_threshold(self):\n-        \"\"\"Test that the default threshold of 80% works as expected.\"\"\"\n-        # Should match at 80%\n-        assert StringHelper.fuzzy_substring_match(\"hello world\", \"hello\") is True\n-        # Should match at 80%\n-        assert StringHelper.fuzzy_substring_match(\"hello world\", \"hallo\") is True\n-        # Should not match at 90%\n-        assert (\n-            StringHelper.fuzzy_substring_match(\n-                \"hello world\", \"hallo\", ConfidenceValue(\"90%\")\n-            )\n-            is False\n-        )\n", "bytes": 19144, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get clean && \\\n    apt-get update -o Acquire::Retries=3 && \\\n    apt-get install -y --allow-change-held-packages \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-venv \\\n    python3-dev \\\n    libffi-dev \\\n    libssl-dev \\\n    ffmpeg \\\n    pkg-config \\\n    libavformat-dev \\\n    libavcodec-dev \\\n    libavdevice-dev \\\n    libavutil-dev \\\n    libavfilter-dev \\\n    libswscale-dev \\\n    libswresample-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone https://github.com/AdbAutoPlayer/AdbAutoPlayer.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 3802834b005e1ab98ee517970bdeea6ab1289d0b || git fetch --depth 1 origin \"+refs/pull/216/head:refs/remotes/origin/pr/216\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\nENV CI=true\n\n# Install Python project dependencies from the python/ subdirectory\nRUN uv venv /opt/venv --python python3 && \\\n    uv pip install --python /opt/venv/bin/python -e ./python[dev] && \\\n    uv pip install --python /opt/venv/bin/python pytest\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1426, "truncated": false}]}, {"task_name": "adbautoplayer__adbautoplayer-5", "repo": "adbautoplayer/adbautoplayer", "language": "Python", "difficulty": "hard", "area": "library", "topic": "logging", "bug_class": "missing-sensitive-data-redaction", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "The application's logging system currently exposes full file paths that contain the user's home directory (e.g., C:\\Users\\<username>\\AppData\\... on Windows or /home/<username>/... on Unix/Linux). This presents a privacy and security risk by leaking the logged-in username in plain text logs. A path sanitization mechanism must be implemented for both the Python and Go components to redact usernames from log outputs before they are written.\n\nIn Python, implement a sanitize_path function that accepts a log string and replaces the user's home directory path with <redacted>. It must handle Windows-style paths, Unix-style paths, multiple occurrences in a single line, already-redacted paths, and usernames with special characters.\n\nIn Go, implement a path sanitizer using a NewPathSanitizerWithConfig(isWindows bool, username string) constructor and a SanitizePath(input string) string method. It should dynamically replace the username in file paths with <redacted> based on the OS flag and provided username, preserving existing redacted markers and handling multiple occurrences.\n\nThis functionality should be integrated into the logging pipeline so that all log messages are automatically sanitized, ensuring user privacy is maintained across different operating systems without manual intervention per log line.", "bytes": 1317, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src\n\nexport CI=true\n\n# Copy HEAD test files from /tests into the working tree\nmkdir -p internal/ipc\ncp \"/tests/internal/ipc/path_sanitizer_test.go\" \"internal/ipc/path_sanitizer_test.go\"\nmkdir -p python/tests\ncp \"/tests/python/tests/test_logging_setup.py\" \"python/tests/test_logging_setup.py\"\n\n# Run Go test for path_sanitizer\ngo test -v ./internal/ipc/... -run TestSanitizePath 2>&1\ngo_status=$?\n\n# Run Python test for sanitize_path\ncd python && poetry run pytest tests/test_logging_setup.py -v 2>&1\npy_status=$?\n\n# Both must pass\nif [ $go_status -eq 0 ] && [ $py_status -eq 0 ]; then\n    echo 1 > /logs/verifier/reward.txt\nelse\n    echo 0 > /logs/verifier/reward.txt\nfi\nexit $(( go_status | py_status ))\n", "bytes": 726, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/internal/ipc/logger.go b/internal/ipc/logger.go\nindex 9f70645c..603523e8 100644\n--- a/internal/ipc/logger.go\n+++ b/internal/ipc/logger.go\n@@ -7,8 +7,9 @@ import (\n )\n \n type FrontendLogger struct {\n-\tctx      context.Context\n-\tlogLevel uint8\n+\tctx       context.Context\n+\tlogLevel  uint8\n+\tsanitizer *PathSanitizer\n }\n \n var logLevelPriority = map[LogLevel]uint8{\n@@ -26,7 +27,8 @@ func (l *FrontendLogger) Startup(ctx context.Context) {\n \n func NewFrontendLogger(logLevel uint8) *FrontendLogger {\n \treturn &FrontendLogger{\n-\t\tlogLevel: logLevel,\n+\t\tlogLevel:  logLevel,\n+\t\tsanitizer: NewPathSanitizer(),\n \t}\n }\n \n@@ -120,5 +122,6 @@ func (l *FrontendLogger) LogMessage(message LogMessage) {\n \tif l.logLevel > logLevelPriority[message.Level] {\n \t\treturn\n \t}\n+\tmessage.Message = l.sanitizer.SanitizePath(message.Message)\n \truntime.EventsEmit(l.ctx, \"log-message\", message)\n }\ndiff --git a/internal/ipc/path_sanitizer.go b/internal/ipc/path_sanitizer.go\nnew file mode 100644\nindex 00000000..d7814bcd\n--- /dev/null\n+++ b/internal/ipc/path_sanitizer.go\n@@ -0,0 +1,64 @@\n+package ipc\n+\n+import (\n+\t\"os/user\"\n+\t\"regexp\"\n+\t\"runtime\"\n+\t\"strings\"\n+)\n+\n+type PathSanitizer struct {\n+\tisWindows bool\n+\tusername  string\n+\tsanitize  bool\n+}\n+\n+func NewPathSanitizer() *PathSanitizer {\n+\tsanitizer := &PathSanitizer{\n+\t\tisWindows: runtime.GOOS == \"windows\",\n+\t\tsanitize:  false,\n+\t}\n+\n+\tcurrentUser, err := user.Current()\n+\tif err == nil {\n+\t\tusername := currentUser.Username\n+\t\tif i := strings.LastIndex(username, \"\\\\\"); i >= 0 {\n+\t\t\tusername = username[i+1:]\n+\t\t}\n+\n+\t\tsanitizer.username = username\n+\t\tsanitizer.sanitize = true\n+\t}\n+\n+\treturn sanitizer\n+}\n+\n+func NewPathSanitizerWithConfig(isWindows bool, username string) *PathSanitizer {\n+\treturn &PathSanitizer{\n+\t\tisWindows: isWindows,\n+\t\tusername:  username,\n+\t\tsanitize:  true,\n+\t}\n+}\n+\n+func (ps *PathSanitizer) SanitizePath(message string) string {\n+\tif !ps.sanitize || strings.Contains(message, \"<redacted>\") {\n+\t\treturn message\n+\t}\n+\n+\tvar pattern, replacement string\n+\tif ps.isWindows {\n+\t\tpattern = `C:\\\\Users\\\\` + regexp.QuoteMeta(ps.username)\n+\t\treplacement = `C:\\Users\\<redacted>`\n+\t} else {\n+\t\tpattern = `/home/` + regexp.QuoteMeta(ps.username)\n+\t\treplacement = `/home/<redacted>`\n+\t}\n+\n+\tre, err := regexp.Compile(pattern)\n+\tif err != nil {\n+\t\treturn message\n+\t}\n+\n+\treturn re.ReplaceAllString(message, replacement)\n+}\ndiff --git a/python/.idea/runConfigurations/EventMonopolyAssist.xml b/python/.idea/runConfigurations/EventMonopolyAssist.xml\nindex 0d0ff28f..c0979877 100644\n--- a/python/.idea/runConfigurations/EventMonopolyAssist.xml\n+++ b/python/.idea/runConfigurations/EventMonopolyAssist.xml\n@@ -13,7 +13,7 @@\n     <option name=\"ADD_CONTENT_ROOTS\" value=\"true\" />\n     <option name=\"ADD_SOURCE_ROOTS\" value=\"true\" />\n     <option name=\"SCRIPT_NAME\" value=\"$PROJECT_DIR$/adb_auto_player/main.py\" />\n-    <option name=\"PARAMETERS\" value=\"EventMonopolyAssist --output=raw --log-level=INFO\" />\n+    <option name=\"PARAMETERS\" value=\"EventMonopolyAssist --output=text --log-level=INFO\" />\n     <option name=\"SHOW_COMMAND_LINE\" value=\"false\" />\n     <option name=\"EMULATE_TERMINAL\" value=\"false\" />\n     <option name=\"MODULE_MODE\" value=\"false\" />\ndiff --git a/python/.pre-commit-config.yaml b/python/.pre-commit-config.yaml\ndeleted file mode 100644\nindex 98012258..00000000\n--- a/python/.pre-commit-config.yaml\n+++ /dev/null\n@@ -1,25 +0,0 @@\n-exclude: (^|/)\\.idea/\n-files: ^python/\n-repos:\n-  - repo: https://github.com/pre-commit/pre-commit-hooks\n-    rev: v5.0.0\n-    hooks:\n-      - id: trailing-whitespace\n-        exclude: \\.md$\n-      - id: end-of-file-fixer\n-  - repo: https://github.com/psf/black\n-    rev: 24.10.0\n-    hooks:\n-      - id: black\n-  - repo: https://github.com/pycqa/flake8\n-    rev: 7.1.1\n-    hooks:\n-    -   id: flake8\n-  - repo: local\n-    hooks:\n-      - id: mypy\n-        name: mypy\n-        entry: poetry run mypy --strict\n-        language: system\n-        types: [python]\n-        args: [--config, python/setup.cfg]\ndiff --git a/python/adb_auto_player/logging_setup.py b/python/adb_auto_player/logging_setup.py\nindex 0f7ec576..83ecd57f 100644\n--- a/python/adb_auto_player/logging_setup.py\n+++ b/python/adb_auto_player/logging_setup.py\n@@ -1,10 +1,39 @@\n import json\n import logging\n import sys\n+import os\n+import re\n \n from adb_auto_player.ipc.log_message import LogMessage, LogLevel\n \n \n+def sanitize_path(log_message):\n+    \"\"\"\n+    Sanitizes file paths in log messages by replacing the username with '<redacted>'.\n+    Works with both Windows and Unix-style paths.\n+\n+    Args:\n+        log_message (str): The log message containing file paths\n+\n+    Returns:\n+        str: The sanitized log message with usernames redacted\n+    \"\"\"\n+    home_dir = os.path.expanduser(\"~\")\n+\n+    if \"\\\\\" in home_dir:  # Windows path\n+        username = home_dir.split(\"\\\\\")[-1]\n+        pattern = re.escape(f\"C:\\\\Users\\\\{username}\")\n+        replacement = r\"C:\\\\Users\\\\<redacted>\"\n+    else:  # Unix path\n+        username = home_dir.split(\"/\")[-1]\n+        pattern = f\"/home/{username}\"\n+        replacement = \"/home/<redacted>\"\n+\n+    sanitized_message = re.sub(pattern, replacement, log_message)\n+\n+    return sanitized_message\n+\n+\n class JsonLogHandler(logging.Handler):\n     def emit(self, record):\n         level_mapping = {\n@@ -15,8 +44,10 @@ class JsonLogHandler(logging.Handler):\n             logging.CRITICAL: LogLevel.FATAL,\n         }\n \n+        sanitized_message = sanitize_path(record.getMessage())\n+\n         log_message = LogMessage.create_log_message(\n-            level_mapping.get(record.levelno, LogLevel.DEBUG), record.getMessage()\n+            level_mapping.get(record.levelno, LogLevel.DEBUG), sanitized_message\n         )\n         log_dict = log_message.to_dict()\n         log_json = json.dumps(log_dict)\n@@ -27,5 +58,33 @@ class JsonLogHandler(logging.Handler):\n def setup_json_log_handler(level: int | str):\n     logger = logging.getLogger()\n     logger.setLevel(level)\n-    json_handler = JsonLogHandler()\n-    logger.addHandler(json_handler)\n+    json_log_handler = JsonLogHandler()\n+    logger.addHandler(json_log_handler)\n+\n+\n+class TextLogHandler(logging.StreamHandler):\n+    COLORS = {\n+        \"DEBUG\": \"\\033[94m\",  # Blue\n+        \"INFO\": \"\\033[92m\",  # Green\n+        \"WARNING\": \"\\033[93m\",  # Yellow\n+        \"ERROR\": \"\\033[91m\",  # Red\n+        \"CRITICAL\": \"\\033[95m\",  # Magenta\n+        \"RESET\": \"\\033[0m\",  # Reset to default\n+    }\n+\n+    def emit(self, record):\n+        log_level = record.levelname\n+        sanitized_message = sanitize_path(record.getMessage())\n+        color = self.COLORS.get(log_level, self.COLORS[\"RESET\"])\n+        formatted_message = (\n+            f\"{color}[{log_level}] {sanitized_message}{self.COLORS['RESET']}\"\n+        )\n+        print(formatted_message)\n+        sys.stdout.flush()\n+\n+\n+def setup_text_log_handler(level: int | str):\n+    logger = logging.getLogger()\n+    logger.setLevel(level)\n+    text_log_handler = TextLogHandler()\n+    logger.addHandler(text_log_handler)\ndiff --git a/python/adb_auto_player/main.py b/python/adb_auto_player/main.py\nindex 371ddd5e..6ffac3e4 100644\n--- a/python/adb_auto_player/main.py\n+++ b/python/adb_auto_player/main.py\n@@ -12,7 +12,10 @@ def main() -> None:\n     parser = argparse.ArgumentParser(description=\"AFK Journey\")\n     parser.add_argument(\"command\", help=\"Command to run\")\n     parser.add_argument(\n-        \"--output\", choices=[\"json\", \"raw\"], default=\"json\", help=\"Output format\"\n+        \"--output\",\n+        choices=[\"json\", \"text\", \"raw\"],\n+        default=\"json\",\n+        help=\"Output format\",\n     )\n     parser.add_argument(\n         \"--log-level\",\n@@ -22,10 +25,13 @@ def main() -> None:\n     )\n \n     args = parser.parse_args()\n-    if args.output == \"json\":\n-        logging_setup.setup_json_log_handler(args.log_level)\n-    else:\n-        logging.getLogger().setLevel(args.log_level)\n+    match args.output:\n+        case \"json\":\n+            logging_setup.setup_json_log_handler(args.log_level)\n+        case \"text\":\n+            logging_setup.setup_text_log_handler(args.log_level)\n+        case _:\n+            logging.getLogger().setLevel(args.log_level)\n \n     for cmd in __get_commands():\n         if str.lower(cmd.name) == str.lower(args.command):\ndiff --git a/python/poetry.lock b/python/poetry.lock\nindex c4618ed8..651c942c 100644\n--- a/python/poetry.lock\n+++ b/python/poetry.lock\n@@ -248,6 +248,17 @@ files = [\n     {file = \"cigam-0.0.3-py3-none-any.whl\", hash = \"sha256:8fcf65d7361f0372c53780e861307abd1f11a94b6204fa653ba3f38277822783\"},\n ]\n \n+[[package]]\n+name = \"colorama\"\n+version = \"0.4.6\"\n+description = \"Cross-platform colored terminal text.\"\n+optional = false\n+python-versions = \"!=3.0.*,!=3.1.*,!=3.2.*,!=3.3.*,!=3.4.*,!=3.5.*,!=3.6.*,>=2.7\"\n+files = [\n+    {file = \"colorama-0.4.6-py2.py3-none-any.whl\", hash = \"sha256:4f1d9991f5acc0ca119f9d443620b77f9d6b33703e51011c16baf57afb285fc6\"},\n+    {file = \"colorama-0.4.6.tar.gz\", hash = \"sha256:08695f5cb7ed6e0531a20572697297273c47b8cae5a63ffc6d6ed5c201be6e44\"},\n+]\n+\n [[package]]\n name = \"decorator\"\n version = \"5.1.1\"\n@@ -287,6 +298,17 @@ files = [\n [package.extras]\n all = [\"flake8 (>=7.1.1)\", \"mypy (>=1.11.2)\", \"pytest (>=8.3.2)\", \"ruff (>=0.6.2)\"]\n \n+[[package]]\n+name = \"iniconfig\"\n+version = \"2.0.0\"\n+description = \"brain-dead simple config-ini parsing\"\n+optional = false\n+python-versions = \">=3.7\"\n+files = [\n+    {file = \"iniconfig-2.0.0-py3-none-any.whl\", hash = \"sha256:b6a85871a79d2e3b22d2d1b94ac2824226a63c6b741c88f7ae975f18b6778374\"},\n+    {file = \"iniconfig-2.0.0.tar.gz\", hash = \"sha256:2d91e135bf72d31a410b17c16da610a82cb55f6b0477d1a902134b24a455b8b3\"},\n+]\n+\n [[package]]\n name = \"nuitka\"\n version = \"2.6.2\"\n@@ -497,6 +519,21 @@ tests = [\"check-manifest\", \"coverage (>=7.4.2)\", \"defusedxml\", \"markdown2\", \"ole\n typing = [\"typing-extensions\"]\n xmp = [\"defusedxml\"]\n \n+[[package]]\n+name = \"pluggy\"\n+version = \"1.5.0\"\n+description = \"plugin and hook calling mechanisms for python\"\n+optional = false\n+python-versions = \">=3.8\"\n+files = [\n+    {file = \"pluggy-1.5.0-py3-none-any.whl\", hash = \"sha256:44e1ad92c8ca002de6377e165f3e0f1be63266ab4d554740532335b9d75ea669\"},\n+    {file = \"pluggy-1.5.0.tar.gz\", hash = \"sha256:2cffa88e94fdc978c4c574f15f9e59b7f4201d439195c3715ca9e2486f1d0cf1\"},\n+]\n+\n+[package.extras]\n+dev = [\"pre-commit\", \"tox\"]\n+testing = [\"pytest\", \"pytest-benchmark\"]\n+\n [[package]]\n name = \"py\"\n version = \"1.11.0\"\n@@ -662,6 +699,26 @@ files = [\n     {file = \"pyelftools-0.31.tar.gz\", hash = \"sha256:c774416b10310156879443b81187d182d8d9ee499660380e645918b50bc88f99\"},\n ]\n \n+[[package]]\n+name = \"pytest\"\n+version = \"8.3.4\"\n+description = \"pytest: simple powerful testing with Python\"\n+optional = false\n+python-versions = \">=3.8\"\n+files = [\n+    {file = \"pytest-8.3.4-py3-none-any.whl\", hash = \"sha256:50e16d954148559c9a74109af1eaf0c945ba2d8f30f0a3d3335edde19788b6f6\"},\n+    {file = \"pytest-8.3.4.tar.gz\", hash = \"sha256:965370d062bce11e73868e0335abac31b4d3de0e82f4007408d242b4f8610761\"},\n+]\n+\n+[package.dependencies]\n+colorama = {version = \"*\", markers = \"sys_platform == \\\"win32\\\"\"}\n+iniconfig = \"*\"\n+packaging = \"*\"\n+pluggy = \">=1.5,<2\"\n+\n+[package.extras]\n+dev = [\"argcomplete\", \"attrs (>=19.2)\", \"hypothesis (>=3.56)\", \"mock\", \"pygments (>=2.7.2)\", \"requests\", \"setuptools\", \"xmlschema\"]\n+\n [[package]]\n name = \"requests\"\n version = \"2.32.3\"\n@@ -852,4 +909,4 @@ cffi = [\"cffi (>=1.11)\"]\n [metadata]\n lock-version = \"2.0\"\n python-versions = \">=3.12,<3.14\"\n-content-hash = \"07d159bcefc145f5ea8ab58d0d014c0f92bc8f8efe738c4c89739d50233e8cb7\"\n+content-hash = \"10cebe1ec8ccb3b5d799d07f24579b563d79fdc381432a590f97ef0aa48085d0\"\ndiff --git a/python/pyproject.toml b/python/pyproject.toml\nindex 3f11ecbb..808eead5 100644\n--- a/python/pyproject.toml\n+++ b/python/pyproject.toml\n@@ -15,6 +15,7 @@ pydantic = \"^2.10.5\"\n \n [tool.poetry.group.dev.dependencies]\n nuitka = \"^2.5.9\"\n+pytest = \"^8.3.4\"\n \n [tool.black]\n line-length = 88\n", "bytes": 11938, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/.github/workflows/build.yml b/.github/workflows/build.yml\nindex bad19dc9..37f18eb5 100644\n--- a/.github/workflows/build.yml\n+++ b/.github/workflows/build.yml\n@@ -8,7 +8,6 @@ on:\n \n jobs:\n   build-and-release:\n-    name: ${{ matrix.os }})\n     runs-on: ${{ matrix.os }}\n     strategy:\n       matrix:\ndiff --git a/.github/workflows/go-tests.yml b/.github/workflows/go-tests.yml\ndeleted file mode 100644\nindex 1763bd91..00000000\n--- a/.github/workflows/go-tests.yml\n+++ /dev/null\n@@ -1,30 +0,0 @@\n-name: Go Tests\n-\n-on:\n-  push:\n-    branches:\n-      - \"*\"\n-  workflow_dispatch:\n-\n-jobs:\n-  go-tests:\n-    name: ${{ matrix.os }}\n-    runs-on: ${{ matrix.os }}\n-    strategy:\n-      matrix:\n-        os: [windows-latest, macos-latest]\n-\n-    steps:\n-      - uses: actions/checkout@v4\n-\n-      - uses: actions/setup-go@v5\n-        with:\n-          go-version: '1.23'\n-\n-      - name: Install Go dependencies\n-        run: |\n-          go mod tidy\n-\n-      - name: Run Go tests\n-        run: |\n-          go test -v ./...\ndiff --git a/.github/workflows/pre-commit.yml b/.github/workflows/pre-commit.yml\ndeleted file mode 100644\nindex d1d8fa93..00000000\n--- a/.github/workflows/pre-commit.yml\n+++ /dev/null\n@@ -1,26 +0,0 @@\n-name: Run\n-on:\n-  push:\n-    branches:\n-      - \"*\"\n-  workflow_dispatch:\n-\n-jobs:\n-  pre-commit:\n-    name: pre-commit hooks\n-    runs-on: ubuntu-latest\n-\n-    steps:\n-      - uses: actions/checkout@v4\n-\n-      - uses: actions/setup-python@v5\n-        with:\n-          python-version: '3.12'\n-\n-      - name: Install dependencies\n-        run: pip install -r requirements-dev.txt\n-\n-      - name: Run pre-commit\n-        run: |\n-          pip install pre-commit\n-          pre-commit run --all-files\ndiff --git a/.github/workflows/python-tests.yml b/.github/workflows/python-tests.yml\ndeleted file mode 100644\nindex 9ad7f913..00000000\n--- a/.github/workflows/python-tests.yml\n+++ /dev/null\n@@ -1,33 +0,0 @@\n-name: Python Tests\n-\n-on:\n-  push:\n-    branches:\n-      - \"*\"\n-  workflow_dispatch:\n-\n-jobs:\n-  python-tests:\n-    name: ${{ matrix.os }})\n-    runs-on: ${{ matrix.os }}\n-    strategy:\n-      matrix:\n-        os: [windows-latest, macos-latest]\n-\n-    steps:\n-      - uses: actions/checkout@v4\n-\n-      - uses: actions/setup-python@v5\n-        with:\n-          python-version: '3.12'\n-\n-      - name: install poetry\n-        run: python -m pip install poetry\n-\n-      - name: poetry install\n-        working-directory: python\n-        run: poetry install\n-\n-      - name: Run Python tests with Poetry\n-        working-directory: python\n-        run: poetry run pytest --maxfail=1 --disable-warnings -q\ndiff --git a/cmd/wails/frontend/build/.gitignore b/cmd/wails/frontend/build/.gitignore\ndeleted file mode 100644\nindex 72e8ffc0..00000000\n--- a/cmd/wails/frontend/build/.gitignore\n+++ /dev/null\n@@ -1 +0,0 @@\n-*\ndiff --git a/frontend/build/.gitignore b/frontend/build/.gitignore\ndeleted file mode 100644\nindex 72e8ffc0..00000000\n--- a/frontend/build/.gitignore\n+++ /dev/null\n@@ -1 +0,0 @@\n-*\ndiff --git a/internal/ipc/logger.go b/internal/ipc/logger.go\nindex 603523e8..9f70645c 100644\n--- a/internal/ipc/logger.go\n+++ b/internal/ipc/logger.go\n@@ -7,9 +7,8 @@ import (\n )\n \n type FrontendLogger struct {\n-\tctx       context.Context\n-\tlogLevel  uint8\n-\tsanitizer *PathSanitizer\n+\tctx      context.Context\n+\tlogLevel uint8\n }\n \n var logLevelPriority = map[LogLevel]uint8{\n@@ -27,8 +26,7 @@ func (l *FrontendLogger) Startup(ctx context.Context) {\n \n func NewFrontendLogger(logLevel uint8) *FrontendLogger {\n \treturn &FrontendLogger{\n-\t\tlogLevel:  logLevel,\n-\t\tsanitizer: NewPathSanitizer(),\n+\t\tlogLevel: logLevel,\n \t}\n }\n \n@@ -122,6 +120,5 @@ func (l *FrontendLogger) LogMessage(message LogMessage) {\n \tif l.logLevel > logLevelPriority[message.Level] {\n \t\treturn\n \t}\n-\tmessage.Message = l.sanitizer.SanitizePath(message.Message)\n \truntime.EventsEmit(l.ctx, \"log-message\", message)\n }\ndiff --git a/internal/ipc/path_sanitizer.go b/internal/ipc/path_sanitizer.go\ndeleted file mode 100644\nindex d7814bcd..00000000\n--- a/internal/ipc/path_sanitizer.go\n+++ /dev/null\n@@ -1,64 +0,0 @@\n-package ipc\n-\n-import (\n-\t\"os/user\"\n-\t\"regexp\"\n-\t\"runtime\"\n-\t\"strings\"\n-)\n-\n-type PathSanitizer struct {\n-\tisWindows bool\n-\tusername  string\n-\tsanitize  bool\n-}\n-\n-func NewPathSanitizer() *PathSanitizer {\n-\tsanitizer := &PathSanitizer{\n-\t\tisWindows: runtime.GOOS == \"windows\",\n-\t\tsanitize:  false,\n-\t}\n-\n-\tcurrentUser, err := user.Current()\n-\tif err == nil {\n-\t\tusername := currentUser.Username\n-\t\tif i := strings.LastIndex(username, \"\\\\\"); i >= 0 {\n-\t\t\tusername = username[i+1:]\n-\t\t}\n-\n-\t\tsanitizer.username = username\n-\t\tsanitizer.sanitize = true\n-\t}\n-\n-\treturn sanitizer\n-}\n-\n-func NewPathSanitizerWithConfig(isWindows bool, username string) *PathSanitizer {\n-\treturn &PathSanitizer{\n-\t\tisWindows: isWindows,\n-\t\tusername:  username,\n-\t\tsanitize:  true,\n-\t}\n-}\n-\n-func (ps *PathSanitizer) SanitizePath(message string) string {\n-\tif !ps.sanitize || strings.Contains(message, \"<redacted>\") {\n-\t\treturn message\n-\t}\n-\n-\tvar pattern, replacement string\n-\tif ps.isWindows {\n-\t\tpattern = `C:\\\\Users\\\\` + regexp.QuoteMeta(ps.username)\n-\t\treplacement = `C:\\Users\\<redacted>`\n-\t} else {\n-\t\tpattern = `/home/` + regexp.QuoteMeta(ps.username)\n-\t\treplacement = `/home/<redacted>`\n-\t}\n-\n-\tre, err := regexp.Compile(pattern)\n-\tif err != nil {\n-\t\treturn message\n-\t}\n-\n-\treturn re.ReplaceAllString(message, replacement)\n-}\ndiff --git a/internal/ipc/path_sanitizer_test.go b/internal/ipc/path_sanitizer_test.go\ndeleted file mode 100644\nindex 6cd7a8b3..00000000\n--- a/internal/ipc/path_sanitizer_test.go\n+++ /dev/null\n@@ -1,75 +0,0 @@\n-package ipc\n-\n-import (\n-\t\"testing\"\n-)\n-\n-func TestSanitizePath(t *testing.T) {\n-\ttests := []struct {\n-\t\tname      string\n-\t\tinput     string\n-\t\texpected  string\n-\t\tisWindows bool\n-\t\tusername  string\n-\t}{\n-\t\t{\n-\t\t\tname:      \"Windows path\",\n-\t\t\tinput:     `adb_path: C:\\Users\\testuser\\AppData\\Local\\file.txt`,\n-\t\t\texpected:  `adb_path: C:\\Users\\<redacted>\\AppData\\Local\\file.txt`,\n-\t\t\tisWindows: true,\n-\t\t\tusername:  \"testuser\",\n-\t\t},\n-\t\t{\n-\t\t\tname:      \"Unix path\",\n-\t\t\tinput:     \"/home/testuser/.config/file.txt\",\n-\t\t\texpected:  \"/home/<redacted>/.config/file.txt\",\n-\t\t\tisWindows: false,\n-\t\t\tusername:  \"testuser\",\n-\t\t},\n-\t\t{\n-\t\t\tname:      \"Multiple Windows paths\",\n-\t\t\tinput:     `adb_path: C:\\Users\\testuser\\AppData\\file.txt and C:\\Users\\testuser\\Desktop\\file2.txt`,\n-\t\t\texpected:  `adb_path: C:\\Users\\<redacted>\\AppData\\file.txt and C:\\Users\\<redacted>\\Desktop\\file2.txt`,\n-\t\t\tisWindows: true,\n-\t\t\tusername:  \"testuser\",\n-\t\t},\n-\t\t{\n-\t\t\tname:      \"Already redacted Windows\",\n-\t\t\tinput:     `adb_path: C:\\Users\\<redacted>\\AppData\\Local\\file.txt`,\n-\t\t\texpected:  `adb_path: C:\\Users\\<redacted>\\AppData\\Local\\file.txt`,\n-\t\t\tisWindows: true,\n-\t\t\tusername:  \"testuser\",\n-\t\t},\n-\t\t{\n-\t\t\tname:      \"Already redacted Unix\",\n-\t\t\tinput:     \"adb_path: /home/<redacted>/.config/file.txt\",\n-\t\t\texpected:  \"adb_path: /home/<redacted>/.config/file.txt\",\n-\t\t\tisWindows: false,\n-\t\t\tusername:  \"testuser\",\n-\t\t},\n-\t\t{\n-\t\t\tname:      \"Username with special characters Windows\",\n-\t\t\tinput:     `C:\\Users\\test.user\\AppData\\Local\\file.txt`,\n-\t\t\texpected:  `C:\\Users\\<redacted>\\AppData\\Local\\file.txt`,\n-\t\t\tisWindows: true,\n-\t\t\tusername:  \"test.user\",\n-\t\t},\n-\t\t{\n-\t\t\tname:      \"Username with special characters Unix\",\n-\t\t\tinput:     \"/home/test.user/.config/file.txt\",\n-\t\t\texpected:  \"/home/<redacted>/.config/file.txt\",\n-\t\t\tisWindows: false,\n-\t\t\tusername:  \"test.user\",\n-\t\t},\n-\t}\n-\n-\tfor _, tt := range tests {\n-\t\tt.Run(tt.name, func(t *testing.T) {\n-\t\t\tsanitizer := NewPathSanitizerWithConfig(tt.isWindows, tt.username)\n-\t\t\tresult := sanitizer.SanitizePath(tt.input)\n-\t\t\tif result != tt.expected {\n-\t\t\t\tt.Errorf(\"sanitizePath() = %v, want %v\", result, tt.expected)\n-\t\t\t}\n-\t\t})\n-\t}\n-}\ndiff --git a/python/.idea/runConfigurations/EventMonopolyAssist.xml b/python/.idea/runConfigurations/EventMonopolyAssist.xml\nindex c0979877..0d0ff28f 100644\n--- a/python/.idea/runConfigurations/EventMonopolyAssist.xml\n+++ b/python/.idea/runConfigurations/EventMonopolyAssist.xml\n@@ -13,7 +13,7 @@\n     <option name=\"ADD_CONTENT_ROOTS\" value=\"true\" />\n     <option name=\"ADD_SOURCE_ROOTS\" value=\"true\" />\n     <option name=\"SCRIPT_NAME\" value=\"$PROJECT_DIR$/adb_auto_player/main.py\" />\n-    <option name=\"PARAMETERS\" value=\"EventMonopolyAssist --output=text --log-level=INFO\" />\n+    <option name=\"PARAMETERS\" value=\"EventMonopolyAssist --output=raw --log-level=INFO\" />\n     <option name=\"SHOW_COMMAND_LINE\" value=\"false\" />\n     <option name=\"EMULATE_TERMINAL\" value=\"false\" />\n     <option name=\"MODULE_MODE\" value=\"false\" />\ndiff --git a/python/.pre-commit-config.yaml b/python/.pre-commit-config.yaml\nnew file mode 100644\nindex 00000000..98012258\n--- /dev/null\n+++ b/python/.pre-commit-config.yaml\n@@ -0,0 +1,25 @@\n+exclude: (^|/)\\.idea/\n+files: ^python/\n+repos:\n+  - repo: https://github.com/pre-commit/pre-commit-hooks\n+    rev: v5.0.0\n+    hooks:\n+      - id: trailing-whitespace\n+        exclude: \\.md$\n+      - id: end-of-file-fixer\n+  - repo: https://github.com/psf/black\n+    rev: 24.10.0\n+    hooks:\n+      - id: black\n+  - repo: https://github.com/pycqa/flake8\n+    rev: 7.1.1\n+    hooks:\n+    -   id: flake8\n+  - repo: local\n+    hooks:\n+      - id: mypy\n+        name: mypy\n+        entry: poetry run mypy --strict\n+        language: system\n+        types: [python]\n+        args: [--config, python/setup.cfg]\ndiff --git a/python/adb_auto_player/logging_setup.py b/python/adb_auto_player/logging_setup.py\nindex 83ecd57f..0f7ec576 100644\n--- a/python/adb_auto_player/logging_setup.py\n+++ b/python/adb_auto_player/logging_setup.py\n@@ -1,39 +1,10 @@\n import json\n import logging\n import sys\n-import os\n-import re\n \n from adb_auto_player.ipc.log_message import LogMessage, LogLevel\n \n \n-def sanitize_path(log_message):\n-    \"\"\"\n-    Sanitizes file paths in log messages by replacing the username with '<redacted>'.\n-    Works with both Windows and Unix-style paths.\n-\n-    Args:\n-        log_message (str): The log message containing file paths\n-\n-    Returns:\n-        str: The sanitized log message with usernames redacted\n-    \"\"\"\n-    home_dir = os.path.expanduser(\"~\")\n-\n-    if \"\\\\\" in home_dir:  # Windows path\n-        username = home_dir.split(\"\\\\\")[-1]\n-        pattern = re.escape(f\"C:\\\\Users\\\\{username}\")\n-        replacement = r\"C:\\\\Users\\\\<redacted>\"\n-    else:  # Unix path\n-        username = home_dir.split(\"/\")[-1]\n-        pattern = f\"/home/{username}\"\n-        replacement = \"/home/<redacted>\"\n-\n-    sanitized_message = re.sub(pattern, replacement, log_message)\n-\n-    return sanitized_message\n-\n-\n class JsonLogHandler(logging.Handler):\n     def emit(self, record):\n         level_mapping = {\n@@ -44,10 +15,8 @@ class JsonLogHandler(logging.Handler):\n             logging.CRITICAL: LogLevel.FATAL,\n         }\n \n-        sanitized_message = sanitize_path(record.getMessage())\n-\n         log_message = LogMessage.create_log_message(\n-            level_mapping.get(record.levelno, LogLevel.DEBUG), sanitized_message\n+            level_mapping.get(record.levelno, LogLevel.DEBUG), record.getMessage()\n         )\n         log_dict = log_message.to_dict()\n         log_json = json.dumps(log_dict)\n@@ -58,33 +27,5 @@ class JsonLogHandler(logging.Handler):\n def setup_json_log_handler(level: int | str):\n     logger = logging.getLogger()\n     logger.setLevel(level)\n-    json_log_handler = JsonLogHandler()\n-    logger.addHandler(json_log_handler)\n-\n-\n-class TextLogHandler(logging.StreamHandler):\n-    COLORS = {\n-        \"DEBUG\": \"\\033[94m\",  # Blue\n-        \"INFO\": \"\\033[92m\",  # Green\n-        \"WARNING\": \"\\033[93m\",  # Yellow\n-        \"ERROR\": \"\\033[91m\",  # Red\n-        \"CRITICAL\": \"\\033[95m\",  # Magenta\n-        \"RESET\": \"\\033[0m\",  # Reset to default\n-    }\n-\n-    def emit(self, record):\n-        log_level = record.levelname\n-        sanitized_message = sanitize_path(record.getMessage())\n-        color = self.COLORS.get(log_level, self.COLORS[\"RESET\"])\n-        formatted_message = (\n-            f\"{color}[{log_level}] {sanitized_message}{self.COLORS['RESET']}\"\n-        )\n-        print(formatted_message)\n-        sys.stdout.flush()\n-\n-\n-def setup_text_log_handler(level: int | str):\n-    logger = logging.getLogger()\n-    logger.setLevel(level)\n-    text_log_handler = TextLogHandler()\n-    logger.addHandler(text_log_handler)\n+    json_handler = JsonLogHandler()\n+    logger.addHandler(json_handler)\ndiff --git a/python/adb_auto_player/main.py b/python/adb_auto_player/main.py\nindex 6ffac3e4..371ddd5e 100644\n--- a/python/adb_auto_player/main.py\n+++ b/python/adb_auto_player/main.py\n@@ -12,10 +12,7 @@ def main() -> None:\n     parser = argparse.ArgumentParser(description=\"AFK Journey\")\n     parser.add_argument(\"command\", help=\"Command to run\")\n     parser.add_argument(\n-        \"--output\",\n-        choices=[\"json\", \"text\", \"raw\"],\n-        default=\"json\",\n-        help=\"Output format\",\n+        \"--output\", choices=[\"json\", \"raw\"], default=\"json\", help=\"Output format\"\n     )\n     parser.add_argument(\n         \"--log-level\",\n@@ -25,13 +22,10 @@ def main() -> None:\n     )\n \n     args = parser.parse_args()\n-    match args.output:\n-        case \"json\":\n-            logging_setup.setup_json_log_handler(args.log_level)\n-        case \"text\":\n-            logging_setup.setup_text_log_handler(args.log_level)\n-        case _:\n-            logging.getLogger().setLevel(args.log_level)\n+    if args.output == \"json\":\n+        logging_setup.setup_json_log_handler(args.log_level)\n+    else:\n+        logging.getLogger().setLevel(args.log_level)\n \n     for cmd in __get_commands():\n         if str.lower(cmd.name) == str.lower(args.command):\ndiff --git a/python/poetry.lock b/python/poetry.lock\nindex 651c942c..c4618ed8 100644\n--- a/python/poetry.lock\n+++ b/python/poetry.lock\n@@ -248,17 +248,6 @@ files = [\n     {file = \"cigam-0.0.3-py3-none-any.whl\", hash = \"sha256:8fcf65d7361f0372c53780e861307abd1f11a94b6204fa653ba3f38277822783\"},\n ]\n \n-[[package]]\n-name = \"colorama\"\n-version = \"0.4.6\"\n-description = \"Cross-platform colored terminal text.\"\n-optional = false\n-python-versions = \"!=3.0.*,!=3.1.*,!=3.2.*,!=3.3.*,!=3.4.*,!=3.5.*,!=3.6.*,>=2.7\"\n-files = [\n-    {file = \"colorama-0.4.6-py2.py3-none-any.whl\", hash = \"sha256:4f1d9991f5acc0ca119f9d443620b77f9d6b33703e51011c16baf57afb285fc6\"},\n-    {file = \"colorama-0.4.6.tar.gz\", hash = \"sha256:08695f5cb7ed6e0531a20572697297273c47b8cae5a63ffc6d6ed5c201be6e44\"},\n-]\n-\n [[package]]\n name = \"decorator\"\n version = \"5.1.1\"\n@@ -298,17 +287,6 @@ files = [\n [package.extras]\n all = [\"flake8 (>=7.1.1)\", \"mypy (>=1.11.2)\", \"pytest (>=8.3.2)\", \"ruff (>=0.6.2)\"]\n \n-[[package]]\n-name = \"iniconfig\"\n-version = \"2.0.0\"\n-description = \"brain-dead simple config-ini parsing\"\n-optional = false\n-python-versions = \">=3.7\"\n-files = [\n-    {file = \"iniconfig-2.0.0-py3-none-any.whl\", hash = \"sha256:b6a85871a79d2e3b22d2d1b94ac2824226a63c6b741c88f7ae975f18b6778374\"},\n-    {file = \"iniconfig-2.0.0.tar.gz\", hash = \"sha256:2d91e135bf72d31a410b17c16da610a82cb55f6b0477d1a902134b24a455b8b3\"},\n-]\n-\n [[package]]\n name = \"nuitka\"\n version = \"2.6.2\"\n@@ -519,21 +497,6 @@ tests = [\"check-manifest\", \"coverage (>=7.4.2)\", \"defusedxml\", \"markdown2\", \"ole\n typing = [\"typing-extensions\"]\n xmp = [\"defusedxml\"]\n \n-[[package]]\n-name = \"pluggy\"\n-version = \"1.5.0\"\n-description = \"plugin and hook calling mechanisms for python\"\n-optional = false\n-python-versions = \">=3.8\"\n-files = [\n-    {file = \"pluggy-1.5.0-py3-none-any.whl\", hash = \"sha256:44e1ad92c8ca002de6377e165f3e0f1be63266ab4d554740532335b9d75ea669\"},\n-    {file = \"pluggy-1.5.0.tar.gz\", hash = \"sha256:2cffa88e94fdc978c4c574f15f9e59b7f4201d439195c3715ca9e2486f1d0cf1\"},\n-]\n-\n-[package.extras]\n-dev = [\"pre-commit\", \"tox\"]\n-testing = [\"pytest\", \"pytest-benchmark\"]\n-\n [[package]]\n name = \"py\"\n version = \"1.11.0\"\n@@ -699,26 +662,6 @@ files = [\n     {file = \"pyelftools-0.31.tar.gz\", hash = \"sha256:c774416b10310156879443b81187d182d8d9ee499660380e645918b50bc88f99\"},\n ]\n \n-[[package]]\n-name = \"pytest\"\n-version = \"8.3.4\"\n-description = \"pytest: simple powerful testing with Python\"\n-optional = false\n-python-versions = \">=3.8\"\n-files = [\n-    {file = \"pytest-8.3.4-py3-none-any.whl\", hash = \"sha256:50e16d954148559c9a74109af1eaf0c945ba2d8f30f0a3d3335edde19788b6f6\"},\n-    {file = \"pytest-8.3.4.tar.gz\", hash = \"sha256:965370d062bce11e73868e0335abac31b4d3de0e82f4007408d242b4f8610761\"},\n-]\n-\n-[package.dependencies]\n-colorama = {version = \"*\", markers = \"sys_platform == \\\"win32\\\"\"}\n-iniconfig = \"*\"\n-packaging = \"*\"\n-pluggy = \">=1.5,<2\"\n-\n-[package.extras]\n-dev = [\"argcomplete\", \"attrs (>=19.2)\", \"hypothesis (>=3.56)\", \"mock\", \"pygments (>=2.7.2)\", \"requests\", \"setuptools\", \"xmlschema\"]\n-\n [[package]]\n name = \"requests\"\n version = \"2.32.3\"\n@@ -909,4 +852,4 @@ cffi = [\"cffi (>=1.11)\"]\n [metadata]\n lock-version = \"2.0\"\n python-versions = \">=3.12,<3.14\"\n-content-hash = \"10cebe1ec8ccb3b5d799d07f24579b563d79fdc381432a590f97ef0aa48085d0\"\n+content-hash = \"07d159bcefc145f5ea8ab58d0d014c0f92bc8f8efe738c4c89739d50233e8cb7\"\ndiff --git a/python/pyproject.toml b/python/pyproject.toml\nindex 808eead5..3f11ecbb 100644\n--- a/python/pyproject.toml\n+++ b/python/pyproject.toml\n@@ -15,7 +15,6 @@ pydantic = \"^2.10.5\"\n \n [tool.poetry.group.dev.dependencies]\n nuitka = \"^2.5.9\"\n-pytest = \"^8.3.4\"\n \n [tool.black]\n line-length = 88\ndiff --git a/python/tests/test_logging_setup.py b/python/tests/test_logging_setup.py\ndeleted file mode 100644\nindex 655207e0..00000000\n--- a/python/tests/test_logging_setup.py\n+++ /dev/null\n@@ -1,30 +0,0 @@\n-import unittest\n-from unittest.mock import patch\n-import adb_auto_player.logging_setup\n-\n-\n-class TestSanitizePath(unittest.TestCase):\n-\n-    @patch(\"os.path.expanduser\", return_value=r\"C:\\Users\\mockuser\")  # Mock home dir\n-    def test_windows_path(self, mock_expanduser):\n-        log = r\"adb_path: C:\\Users\\mockuser\\AppData\\Local\\file.txt\"\n-        expected = r\"adb_path: C:\\Users\\<redacted>\\AppData\\Local\\file.txt\"\n-        self.assertEqual(expected, adb_auto_player.logging_setup.sanitize_path(log))\n-\n-    @patch(\"os.path.expanduser\", return_value=\"/home/mockuser\")  # Mock home dir\n-    def test_unix_path(self, mock_expanduser):\n-        log = \"/home/mockuser/.config/file.txt\"\n-        expected = \"/home/<redacted>/.config/file.txt\"\n-        self.assertEqual(expected, adb_auto_player.logging_setup.sanitize_path(log))\n-\n-    @patch(\"os.path.expanduser\", return_value=r\"C:\\Users\\mockuser\")\n-    def test_multiple_occurrences(self, mock_expanduser):\n-        log = r\"adb_path: C:\\Users\\mockuser\\AppData\\file.txt\"\n-        r\" and C:\\Users\\mockuser\\Desktop\\file2.txt\"\n-        expected = r\"adb_path: C:\\Users\\<redacted>\\AppData\\file.txt\"\n-        r\" and C:\\Users\\<redacted>\\Desktop\\file2.txt\"\n-        self.assertEqual(expected, adb_auto_player.logging_setup.sanitize_path(log))\n-\n-\n-if __name__ == \"__main__\":\n-    unittest.main()\n", "bytes": 18899, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    python3-dev \\\n    libffi-dev \\\n    libssl-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nRUN curl -fsSL https://go.dev/dl/go1.23.4.linux-amd64.tar.gz | tar -C /usr/local -xzf - && \\\n    ln -s /usr/local/go/bin/go /usr/local/bin/go\n\nWORKDIR /app\n\nRUN git clone https://github.com/AdbAutoPlayer/AdbAutoPlayer.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 777f7619a99e5531ea47758c10c73fb8e8caf237 || git fetch --depth 1 origin \"+refs/pull/5/head:refs/remotes/origin/pr/5\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\n# Install Python dependencies via poetry (use --break-system-packages for ubuntu 24.04)\nRUN pip install --break-system-packages poetry && \\\n    cd python && \\\n    poetry install\n\n# Install Go module dependencies\nRUN go mod download\n\nENV CI=true\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/usr/local/go/bin:/app/python/.venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1340, "truncated": false}]}, {"task_name": "adbautoplayer__adbautoplayer-8", "repo": "adbautoplayer/adbautoplayer", "language": "Python", "difficulty": "medium", "area": "library", "topic": "path-sanitization", "bug_class": "incomplete-path-sanitization", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "Fix the path sanitization logic in the `sanitize_path` function (and `SanitizePath` method in Go) to correctly handle all Windows drive letters, not just `C:`.\n\nCurrently, the sanitization pattern is hardcoded to only match paths starting with `C:\\Users\\`. When log messages or file paths reference other drives (e.g., `D:\\Users\\john\\...` or `E:\\Users\\jane\\...`), the username remains visible in the sanitized output instead of being replaced with `<redacted>`.\n\nExpected behavior:\nThe function should use a dynamic regex pattern that matches any single uppercase drive letter (`[A-Z]:`) followed by `\\Users\\`. Both the `C:` drive and all other drive letters should be sanitized identically.\n\nExamples of expected behavior:\n- Input: `adb_path: D:\\Users\\john\\AppData\\Local\\file.txt`\n  Expected Output: `adb_path: D:\\Users\\<redacted>\\AppData\\Local\\file.txt`\n- Input: `C:\\Users\\alice\\AppData\\file.txt and D:\\Users\\bob\\Desktop\\file2.txt`\n  Expected Output: `C:\\Users\\<redacted>\\AppData\\file.txt and D:\\Users\\<redacted>\\Desktop\\file2.txt`\n- Input: `C:\\Users\\<redacted>\\AppData\\Local\\file.txt`\n  Expected Output: `C:\\Users\\<redacted>\\AppData\\Local\\file.txt` (already sanitized paths should remain unchanged without errors or double-redaction)\n\nApply this fix to both the Python and Go implementations to ensure consistent cross-platform behavior for Windows logging and path cleaning.", "bytes": 1378, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\ncd /app/src\n\n# Copy test files from /tests into the repo\nmkdir -p \"internal/ipc\"\ncp \"/tests/internal/ipc/path_sanitizer_test.go\" \"internal/ipc/path_sanitizer_test.go\"\nmkdir -p \"python/tests\"\ncp \"/tests/python/tests/test_logging_setup.py\" \"python/tests/test_logging_setup.py\"\n\ntest_failed=0\n\n# Run Go tests for the internal/ipc package\necho \"=== Running Go tests ===\"\ngo test -v ./internal/ipc/... || test_failed=1\n\nif [ $test_failed -ne 0 ]; then\n    echo \"Go tests failed\"\n    echo 0 > /logs/verifier/reward.txt\n    exit 1\nfi\n\n# Run Python test for the sanitize_path function\necho \"=== Running Python tests ===\"\ncd python\npython3 -m pytest tests/test_logging_setup.py -xvs || test_failed=1\n\nif [ $test_failed -ne 0 ]; then\n    echo \"Python tests failed\"\n    echo 0 > /logs/verifier/reward.txt\n    exit 1\nfi\n\necho \"All tests passed\"\necho 1 > /logs/verifier/reward.txt\nexit 0\n", "bytes": 887, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/internal/ipc/path_sanitizer.go b/internal/ipc/path_sanitizer.go\nindex d7814bcd..76759823 100644\n--- a/internal/ipc/path_sanitizer.go\n+++ b/internal/ipc/path_sanitizer.go\n@@ -48,8 +48,8 @@ func (ps *PathSanitizer) SanitizePath(message string) string {\n \n \tvar pattern, replacement string\n \tif ps.isWindows {\n-\t\tpattern = `C:\\\\Users\\\\` + regexp.QuoteMeta(ps.username)\n-\t\treplacement = `C:\\Users\\<redacted>`\n+\t\tpattern = `:\\\\Users\\\\` + regexp.QuoteMeta(ps.username)\n+\t\treplacement = `:\\Users\\<redacted>`\n \t} else {\n \t\tpattern = `/home/` + regexp.QuoteMeta(ps.username)\n \t\treplacement = `/home/<redacted>`\ndiff --git a/python/adb_auto_player/logging_setup.py b/python/adb_auto_player/logging_setup.py\nindex 83ecd57f..20f30f3c 100644\n--- a/python/adb_auto_player/logging_setup.py\n+++ b/python/adb_auto_player/logging_setup.py\n@@ -22,8 +22,8 @@ def sanitize_path(log_message):\n \n     if \"\\\\\" in home_dir:  # Windows path\n         username = home_dir.split(\"\\\\\")[-1]\n-        pattern = re.escape(f\"C:\\\\Users\\\\{username}\")\n-        replacement = r\"C:\\\\Users\\\\<redacted>\"\n+        pattern = re.escape(f\":\\\\Users\\\\{username}\")\n+        replacement = r\":\\\\Users\\\\<redacted>\"\n     else:  # Unix path\n         username = home_dir.split(\"/\")[-1]\n         pattern = f\"/home/{username}\"\ndiff --git a/python/poetry.lock b/python/poetry.lock\nindex 651c942c..7e5d9097 100644\n--- a/python/poetry.lock\n+++ b/python/poetry.lock\n@@ -311,12 +311,12 @@ files = [\n \n [[package]]\n name = \"nuitka\"\n-version = \"2.6.2\"\n+version = \"2.6.3\"\n description = \"Python compiler with full language support and CPython compatibility\"\n optional = false\n python-versions = \"*\"\n files = [\n-    {file = \"Nuitka-2.6.2.tar.gz\", hash = \"sha256:4557b4fa1c2c0a67c8ac3fb96bb76c0566faf419269cc328304a7fbd4eadf7c5\"},\n+    {file = \"Nuitka-2.6.3.tar.gz\", hash = \"sha256:12638d21de0358ddccd3eaaf694c46d6ad71ac67d9ee7c7c0d948e34fbbb2002\"},\n ]\n \n [package.dependencies]\n", "bytes": 1926, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/internal/ipc/path_sanitizer.go b/internal/ipc/path_sanitizer.go\nindex 76759823..d7814bcd 100644\n--- a/internal/ipc/path_sanitizer.go\n+++ b/internal/ipc/path_sanitizer.go\n@@ -48,8 +48,8 @@ func (ps *PathSanitizer) SanitizePath(message string) string {\n \n \tvar pattern, replacement string\n \tif ps.isWindows {\n-\t\tpattern = `:\\\\Users\\\\` + regexp.QuoteMeta(ps.username)\n-\t\treplacement = `:\\Users\\<redacted>`\n+\t\tpattern = `C:\\\\Users\\\\` + regexp.QuoteMeta(ps.username)\n+\t\treplacement = `C:\\Users\\<redacted>`\n \t} else {\n \t\tpattern = `/home/` + regexp.QuoteMeta(ps.username)\n \t\treplacement = `/home/<redacted>`\ndiff --git a/internal/ipc/path_sanitizer_test.go b/internal/ipc/path_sanitizer_test.go\nindex b3b0e230..6cd7a8b3 100644\n--- a/internal/ipc/path_sanitizer_test.go\n+++ b/internal/ipc/path_sanitizer_test.go\n@@ -28,8 +28,8 @@ func TestSanitizePath(t *testing.T) {\n \t\t},\n \t\t{\n \t\t\tname:      \"Multiple Windows paths\",\n-\t\t\tinput:     `adb_path: C:\\Users\\testuser\\AppData\\file.txt and D:\\Users\\testuser\\Desktop\\file2.txt`,\n-\t\t\texpected:  `adb_path: C:\\Users\\<redacted>\\AppData\\file.txt and D:\\Users\\<redacted>\\Desktop\\file2.txt`,\n+\t\t\tinput:     `adb_path: C:\\Users\\testuser\\AppData\\file.txt and C:\\Users\\testuser\\Desktop\\file2.txt`,\n+\t\t\texpected:  `adb_path: C:\\Users\\<redacted>\\AppData\\file.txt and C:\\Users\\<redacted>\\Desktop\\file2.txt`,\n \t\t\tisWindows: true,\n \t\t\tusername:  \"testuser\",\n \t\t},\ndiff --git a/python/adb_auto_player/logging_setup.py b/python/adb_auto_player/logging_setup.py\nindex 20f30f3c..83ecd57f 100644\n--- a/python/adb_auto_player/logging_setup.py\n+++ b/python/adb_auto_player/logging_setup.py\n@@ -22,8 +22,8 @@ def sanitize_path(log_message):\n \n     if \"\\\\\" in home_dir:  # Windows path\n         username = home_dir.split(\"\\\\\")[-1]\n-        pattern = re.escape(f\":\\\\Users\\\\{username}\")\n-        replacement = r\":\\\\Users\\\\<redacted>\"\n+        pattern = re.escape(f\"C:\\\\Users\\\\{username}\")\n+        replacement = r\"C:\\\\Users\\\\<redacted>\"\n     else:  # Unix path\n         username = home_dir.split(\"/\")[-1]\n         pattern = f\"/home/{username}\"\ndiff --git a/python/poetry.lock b/python/poetry.lock\nindex 7e5d9097..651c942c 100644\n--- a/python/poetry.lock\n+++ b/python/poetry.lock\n@@ -311,12 +311,12 @@ files = [\n \n [[package]]\n name = \"nuitka\"\n-version = \"2.6.3\"\n+version = \"2.6.2\"\n description = \"Python compiler with full language support and CPython compatibility\"\n optional = false\n python-versions = \"*\"\n files = [\n-    {file = \"Nuitka-2.6.3.tar.gz\", hash = \"sha256:12638d21de0358ddccd3eaaf694c46d6ad71ac67d9ee7c7c0d948e34fbbb2002\"},\n+    {file = \"Nuitka-2.6.2.tar.gz\", hash = \"sha256:4557b4fa1c2c0a67c8ac3fb96bb76c0566faf419269cc328304a7fbd4eadf7c5\"},\n ]\n \n [package.dependencies]\ndiff --git a/python/tests/test_logging_setup.py b/python/tests/test_logging_setup.py\nindex c5cabf1e..655207e0 100644\n--- a/python/tests/test_logging_setup.py\n+++ b/python/tests/test_logging_setup.py\n@@ -20,9 +20,9 @@ class TestSanitizePath(unittest.TestCase):\n     @patch(\"os.path.expanduser\", return_value=r\"C:\\Users\\mockuser\")\n     def test_multiple_occurrences(self, mock_expanduser):\n         log = r\"adb_path: C:\\Users\\mockuser\\AppData\\file.txt\"\n-        r\" and D:\\Users\\mockuser\\Desktop\\file2.txt\"\n+        r\" and C:\\Users\\mockuser\\Desktop\\file2.txt\"\n         expected = r\"adb_path: C:\\Users\\<redacted>\\AppData\\file.txt\"\n-        r\" and D:\\Users\\<redacted>\\Desktop\\file2.txt\"\n+        r\" and C:\\Users\\<redacted>\\Desktop\\file2.txt\"\n         self.assertEqual(expected, adb_auto_player.logging_setup.sanitize_path(log))\n \n \n", "bytes": 3538, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    python3-dev \\\n    libffi-dev \\\n    libssl-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\n# Install Go 1.23 (matching go.mod toolchain)\nRUN curl -fsSL https://go.dev/dl/go1.23.4.linux-amd64.tar.gz | tar -C /usr/local -xzf - && \\\n    ln -s /usr/local/go/bin/go /usr/local/bin/go && \\\n    ln -s /usr/local/go/bin/gofmt /usr/local/bin/gofmt\nENV PATH=\"/usr/local/go/bin:${PATH}\"\n\nWORKDIR /app\n\nRUN git clone https://github.com/AdbAutoPlayer/AdbAutoPlayer.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin ce90afee701cce6b13132ff1230ea85864e9f624 || git fetch --depth 1 origin \"+refs/pull/8/head:refs/remotes/origin/pr/8\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\nENV CI=true\n\n# Set up Python venv and install test deps (Poetry for the python/ subdir, pytest globally)\nRUN python3 -m venv /opt/venv && \\\n    . /opt/venv/bin/activate && \\\n    pip install --no-cache-dir --upgrade pip && \\\n    pip install --no-cache-dir pytest poetry\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1481, "truncated": false}]}, {"task_name": "adbautoplayer__adbautoplayer-9", "repo": "adbautoplayer/adbautoplayer", "language": "Python", "difficulty": "hard", "area": "backend", "topic": "template-matching", "bug_class": "validation-error", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "Implement the `compare_roi_similarity` function in the `template_matching` module and integrate it into the `Game` class's `roi_has_changed` method.\n\n**1. Template Matching Logic:**\nImplement a function `compare_roi_similarity(image1, image2, roi)` that compares the similarity of a Region of Interest (ROI) defined by a bounding box `(sx, sy, ex, ey)` between two images.\n\n- **Input Validation:**\n  - Coordinates cannot be negative. Raise `ValueError` if any coordinate is < 0.\n  - Start coordinates (`sx`, `sy`) must be strictly less than end coordinates (`ex`, `ey`). Raise `ValueError` if `sx >= ex` or `sy >= ey`.\n  - End coordinates must not exceed the dimensions of the base image (`ex <= image_width`, `ey <= image_height`). Raise `ValueError` otherwise.\n\n- **Comparison Logic:**\n  - Extract the ROI from both images based on the provided coordinates.\n  - Compare the similarity of these two ROIs. The function should return a boolean: `True` if the ROI is effectively identical (high similarity) and `False` if they differ significantly.\n\n**2. Game Class Integration:**\nUpdate the `Game` class method `roi_has_changed(sx, sy, ex, ey)` to utilize this new comparison functionality.\n\n- **Preconditions:**\n  - If the ROI coordinates are invalid (0, 0, 0, 0), raise a `ValueError`.\n  - The method requires the game to be initialized (specifically, `self.scale_factor` must be set). If not initialized, raise `NotInitializedError`.\n\n- **Behavior:**\n  - Retrieve the current screenshot and compare it against `self.previous_screenshot`.\n  - Use the `compare_roi_similarity` function with the provided ROI and the scaling factor to determine if the content within that region has changed.\n  - Update `self.previous_screenshot` with the current screenshot after comparison.\n  - Return `True` if a change is detected, `False` otherwise.", "bytes": 1836, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src/python\n\nexport CI=true\n\n# Copy test files from /tests (overwrites BASE state with HEAD versions)\nmkdir -p \"tests/data\"\ncp \"/tests/python/tests/data/records_formation_1.png\" \"tests/data/records_formation_1.png\"\ncp \"/tests/python/tests/data/records_formation_2.png\" \"tests/data/records_formation_2.png\"\ncp \"/tests/python/tests/data/records_formation_5.png\" \"tests/data/records_formation_5.png\"\ncp \"/tests/python/tests/data/records_formation_6.png\" \"tests/data/records_formation_6.png\"\ncp \"/tests/python/tests/data/records_formation_7.png\" \"tests/data/records_formation_7.png\"\ncp \"/tests/python/tests/test_game.py\" \"tests/test_game.py\"\ncp \"/tests/python/tests/test_logging_setup.py\" \"tests/test_logging_setup.py\"\ncp \"/tests/python/tests/test_template_matching.py\" \"tests/test_template_matching.py\"\n\n# Run only the Python test files (not image data files)\npython -m pytest -xvs tests/test_game.py tests/test_logging_setup.py tests/test_template_matching.py\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 1128, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/python/.idea/codeStyles/Project.xml b/python/.idea/codeStyles/Project.xml\nnew file mode 100644\nindex 00000000..6ba17248\n--- /dev/null\n+++ b/python/.idea/codeStyles/Project.xml\n@@ -0,0 +1,6 @@\n+<component name=\"ProjectCodeStyleConfiguration\">\n+  <code_scheme name=\"Project\" version=\"173\">\n+    <option name=\"LINE_SEPARATOR\" value=\"&#10;\" />\n+    <option name=\"RIGHT_MARGIN\" value=\"88\" />\n+  </code_scheme>\n+</component>\n\\ No newline at end of file\ndiff --git a/python/.idea/codeStyles/codeStyleConfig.xml b/python/.idea/codeStyles/codeStyleConfig.xml\nnew file mode 100644\nindex 00000000..79ee123c\n--- /dev/null\n+++ b/python/.idea/codeStyles/codeStyleConfig.xml\n@@ -0,0 +1,5 @@\n+<component name=\"ProjectCodeStyleConfiguration\">\n+  <state>\n+    <option name=\"USE_PER_PROJECT_SETTINGS\" value=\"true\" />\n+  </state>\n+</component>\n\\ No newline at end of file\ndiff --git a/python/adb_auto_player/game.py b/python/adb_auto_player/game.py\nnew file mode 100644\nindex 00000000..acfecbea\n--- /dev/null\n+++ b/python/adb_auto_player/game.py\n@@ -0,0 +1,451 @@\n+import io\n+from abc import abstractmethod\n+from pathlib import Path\n+from time import sleep, time\n+from typing import Any, TypeVar, Callable, Literal\n+\n+from PIL import Image\n+from adbutils._device import AdbDevice\n+from pydantic import BaseModel\n+\n+import adb_auto_player.adb as adb\n+import logging\n+import adb_auto_player.template_matching as template_matching\n+from adb_auto_player.command import Command\n+from adb_auto_player.exceptions import (\n+    UnsupportedResolutionException,\n+    TimeoutException,\n+    AdbException,\n+    NotInitializedError,\n+)\n+from adb_auto_player.template_matching import MatchMode\n+\n+\n+class Game:\n+    def __init__(self) -> None:\n+        self.device: AdbDevice | None = None\n+        self.config: BaseModel | None = None\n+        self.store: dict[str, Any] = {}\n+        self.previous_screenshot: Image.Image | None = None\n+        self.resolution: tuple[int, int] | None = None\n+        self.scale_factor: float | None = None\n+\n+    @abstractmethod\n+    def get_template_dir_path(self) -> Path:\n+        pass\n+\n+    @abstractmethod\n+    def load_config(self):\n+        pass\n+\n+    @abstractmethod\n+    def get_menu_commands(self) -> list[Command]:\n+        pass\n+\n+    @abstractmethod\n+    def get_supported_resolutions(self) -> list[str]:\n+        pass\n+\n+    @abstractmethod\n+    def get_config(self) -> BaseModel:\n+        pass\n+\n+    def check_requirements(self) -> None:\n+        \"\"\"\n+        :raises UnsupportedResolutionException:\n+        \"\"\"\n+        resolution = adb.get_screen_resolution(self.get_device())\n+        supported_resolutions = self.get_supported_resolutions()\n+\n+        try:\n+            width, height = map(int, resolution.split(\"x\"))\n+        except ValueError:\n+            raise UnsupportedResolutionException(\n+                f\"Invalid resolution format: {resolution}\"\n+            )\n+\n+        is_supported = False\n+        for supported_resolution in supported_resolutions:\n+            if \"x\" in supported_resolution:\n+                if resolution == supported_resolution:\n+                    is_supported = True\n+                    break\n+            elif \":\" in supported_resolution:\n+                try:\n+                    aspect_width, aspect_height = map(\n+                        int, supported_resolution.split(\":\")\n+                    )\n+                    if width * aspect_height == height * aspect_width:\n+                        is_supported = True\n+                        break\n+                except ValueError:\n+                    raise UnsupportedResolutionException(\n+                        f\"Invalid aspect ratio format: {supported_resolution}\"\n+                    )\n+\n+        if not is_supported:\n+            raise UnsupportedResolutionException(\n+                \"This bot only supports these resolutions: \"\n+                f\"{', '.join(supported_resolutions)}\"\n+            )\n+\n+        self.resolution = width, height\n+        return None\n+\n+    def get_scale_factor(self) -> float:\n+        if self.scale_factor:\n+            return self.scale_factor\n+        reference_resolution = (1080, 1920)\n+        if self.get_resolution() == reference_resolution:\n+            self.scale_factor = 1.0\n+        else:\n+            self.scale_factor = self.get_resolution()[0] / reference_resolution[0]\n+        return self.scale_factor\n+\n+    def get_resolution(self) -> tuple[int, int]:\n+        if self.resolution is None:\n+            raise NotInitializedError()\n+        return self.resolution\n+\n+    def set_device(self) -> None:\n+        self.device = adb.get_device()\n+        self.check_requirements()\n+\n+    def get_device(self) -> AdbDevice:\n+        if self.device is None:\n+            raise NotInitializedError()\n+        return self.device\n+\n+    def click(\n+        self,\n+        x: int,\n+        y: int,\n+        scale: bool = False,\n+    ) -> None:\n+        if not scale:\n+            self.get_device().click(x, y)\n+            return None\n+        x, y = self.__scale_coordinates(x, y)\n+        self.get_device().click(x, y)\n+        return None\n+\n+    def get_screenshot(self) -> Image.Image:\n+        \"\"\"\n+        :raises AdbException: Screenshot cannot be recorded\n+        \"\"\"\n+        screenshot_data = self.get_device().shell(\"screencap -p\", encoding=None)\n+        if isinstance(screenshot_data, bytes):\n+            self.previous_screenshot = Image.open(io.BytesIO(screenshot_data))\n+            return self.previous_screenshot\n+        raise AdbException(\n+            f\"Screenshots cannot be recorded from device: {self.get_device().serial}\"\n+        )\n+\n+    def get_previous_screenshot(self) -> Image.Image:\n+        if self.previous_screenshot is not None:\n+            return self.previous_screenshot\n+        logging.warning(\"No previous screenshot\")\n+        return self.get_screenshot()\n+\n+    def __get_screenshot(self, previous_screenshot: bool):\n+        if previous_screenshot:\n+            return self.get_previous_screenshot()\n+        else:\n+            return self.get_screenshot()\n+\n+    def roi_has_changed(\n+        self,\n+        sx: int,\n+        sy: int,\n+        ex: int,\n+        ey: int,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+    ):\n+        prev = self.previous_screenshot\n+        if prev is None:\n+            raise ValueError(\n+                \"Region of interest cannot have changed if \"\n+                \"there is no previous screenshot.\"\n+            )\n+\n+        sx, sy, ex, ey = self.__scale_coordinates(sx, sy, ex, ey)\n+\n+        return not template_matching.compare_roi_similarity(\n+            prev,\n+            self.get_screenshot(),\n+            roi=(sx, sy, ex, ey),\n+            threshold=threshold,\n+            grayscale=grayscale,\n+        )\n+\n+    def wait_for_roi_change(\n+        self,\n+        sx: int,\n+        sy: int,\n+        ex: int,\n+        ey: int,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+        delay: float = 1,\n+        timeout: float = 30,\n+        timeout_message: str | None = None,\n+    ) -> bool:\n+        \"\"\"\n+        :raises TimeoutException:\n+        \"\"\"\n+\n+        sx, sy, ex, ey = self.__scale_coordinates(sx, sy, ex, ey)\n+\n+        def roi_changed() -> Literal[True] | None:\n+            result = self.roi_has_changed(\n+                sx=sx,\n+                sy=sy,\n+                ex=ex,\n+                ey=ey,\n+                threshold=threshold,\n+                grayscale=grayscale,\n+            )\n+\n+            if result is True:\n+                return True\n+            return None\n+\n+        if timeout_message is None:\n+            timeout_message = (\n+                f\"Region of Interest has not changed after {timeout} seconds\"\n+            )\n+\n+        return self.__execute_or_timeout(\n+            roi_changed, delay=delay, timeout=timeout, timeout_message=timeout_message\n+        )\n+\n+    def find_template_match(\n+        self,\n+        template: str,\n+        match_mode: template_matching.MatchMode = template_matching.MatchMode.BEST,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+        use_previous_screenshot: bool = False,\n+    ) -> tuple[int, int] | None:\n+        template_path = self.get_template_dir_path() / template\n+\n+        return template_matching.find_template_match(\n+            base_image=self.__get_screenshot(\n+                previous_screenshot=use_previous_screenshot\n+            ),\n+            template_image=template_matching.load_image(\n+                image_path=template_path,\n+                image_scale_factor=self.get_scale_factor(),\n+            ),\n+            match_mode=match_mode,\n+            threshold=threshold,\n+            grayscale=grayscale,\n+        )\n+\n+    def find_all_template_matches(\n+        self,\n+        template: str,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+        min_distance: int = 10,\n+        use_previous_screenshot: bool = False,\n+    ) -> list[tuple[int, int]]:\n+        template_path = self.get_template_dir_path() / template\n+\n+        return template_matching.find_all_template_matches(\n+            base_image=self.__get_screenshot(\n+                previous_screenshot=use_previous_screenshot\n+            ),\n+            template_image=template_matching.load_image(\n+                image_path=template_path,\n+                image_scale_factor=self.get_scale_factor(),\n+            ),\n+            threshold=threshold,\n+            grayscale=grayscale,\n+            min_distance=min_distance,\n+        )\n+\n+    def wait_for_template(\n+        self,\n+        template: str,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+        delay: float = 1,\n+        timeout: float = 30,\n+        timeout_message: str | None = None,\n+    ) -> tuple[int, int]:\n+        \"\"\"\n+        :raises TimeoutException:\n+        \"\"\"\n+\n+        def find_template() -> tuple[int, int] | None:\n+            result = self.find_template_match(\n+                template,\n+                threshold=threshold,\n+                grayscale=grayscale,\n+            )\n+            if result is not None:\n+                logging.debug(f\"{template} found\")\n+            return result\n+\n+        if timeout_message is None:\n+            timeout_message = (\n+                f\"Could not find Template: '{template}' after {timeout} seconds\"\n+            )\n+\n+        return self.__execute_or_timeout(\n+            find_template, delay=delay, timeout=timeout, timeout_message=timeout_message\n+        )\n+\n+    def wait_until_template_disappears(\n+        self,\n+        template: str,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+        delay: float = 1,\n+        timeout: float = 30,\n+        timeout_message: str | None = None,\n+    ) -> None:\n+        \"\"\"\n+        :raises TimeoutException:\n+        \"\"\"\n+\n+        def find_best_template() -> tuple[int, int] | None:\n+            result = self.find_template_match(\n+                template,\n+                threshold=threshold,\n+                grayscale=grayscale,\n+            )\n+            if result is None:\n+                logging.debug(f\"{template} no longer visible\")\n+\n+            return result\n+\n+        if timeout_message is None:\n+            timeout_message = (\n+                f\"Template: {template} is still visible after {timeout} seconds\"\n+            )\n+\n+        self.__execute_or_timeout(\n+            find_best_template,\n+            delay=delay,\n+            timeout=timeout,\n+            timeout_message=timeout_message,\n+        )\n+        return None\n+\n+    def wait_for_any_template(\n+        self,\n+        templates: list[str],\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+        delay: float = 1,\n+        timeout: float = 30,\n+        timeout_message: str | None = None,\n+    ) -> tuple[str, int, int]:\n+        \"\"\"\n+        :raises TimeoutException:\n+        \"\"\"\n+\n+        def find_template() -> tuple[str, int, int] | None:\n+            return self.find_any_template(\n+                templates,\n+                threshold=threshold,\n+                grayscale=grayscale,\n+            )\n+\n+        if timeout_message is None:\n+            timeout_message = (\n+                f\"None of the templates {templates} were found after {timeout} seconds\"\n+            )\n+\n+        return self.__execute_or_timeout(\n+            find_template, delay=delay, timeout=timeout, timeout_message=timeout_message\n+        )\n+\n+    def find_any_template(\n+        self,\n+        templates: list[str],\n+        match_mode: MatchMode = MatchMode.BEST,\n+        threshold: float = 0.9,\n+        grayscale: bool = False,\n+    ) -> tuple[str, int, int] | None:\n+        self.get_screenshot()\n+        for template in templates:\n+            result = self.find_template_match(\n+                template,\n+                match_mode=match_mode,\n+                threshold=threshold,\n+                grayscale=grayscale,\n+                use_previous_screenshot=True,\n+            )\n+            if result is not None:\n+                x, y = result\n+                return template, x, y\n+        return None\n+\n+    def press_back_button(self) -> None:\n+        self.get_device().keyevent(4)\n+\n+    def swipe_down(self, sy: int = 1350, ey: int = 500, duration: float = 1.0) -> None:\n+        if sy <= ey:\n+            raise ValueError(\n+                \"sy (start y) must be greater than ey (end y) to swipe down.\"\n+            )\n+\n+        logging.debug(f\"swiping down: {sy} to {ey}\")\n+        self.swipe(sx=540, sy=sy, ex=540, ey=ey, duration=duration)\n+\n+    def swipe_up(self, sy: int = 500, ey: int = 1350, duration: float = 1.0) -> None:\n+        if ey >= sy:\n+            raise ValueError(\"s (start y) must be smaller than ey (end y) to swipe up.\")\n+\n+        logging.debug(f\"swiping up: {sy} to {ey}\")\n+        self.swipe(sx=540, sy=sy, ex=540, ey=ey, duration=duration)\n+\n+    def swipe(self, sx: int, sy: int, ex: int, ey: int, duration: float = 1.0):\n+        sx, sy, ex, ey = self.__scale_coordinates(sx, sy, ex, ey)\n+        self.get_device().swipe(sx=sx, sy=sy, ex=ex, ey=ey, duration=duration)\n+        sleep(2)\n+\n+    T = TypeVar(\"T\")\n+\n+    @staticmethod\n+    def __execute_or_timeout(\n+        operation: Callable[[], T | None],\n+        timeout_message: str,\n+        delay: float = 1,\n+        timeout: float = 30,\n+        result_should_be_none: bool = False,\n+    ) -> T:\n+        \"\"\"\n+        :raises TimeoutException:\n+        \"\"\"\n+        time_spent_waiting: float = 0\n+        end_time = time() + timeout\n+        end_time_exceeded = False\n+\n+        while True:\n+            result = operation()\n+            if result_should_be_none and result is None:\n+                return None  # type: ignore\n+            elif result is not None:\n+                return result\n+\n+            sleep(delay)\n+            time_spent_waiting += delay\n+\n+            if time_spent_waiting >= timeout or end_time_exceeded:\n+                raise TimeoutException(f\"{timeout_message}\")\n+\n+            if end_time <= time():\n+                end_time_exceeded = True\n+\n+    def __scale_coordinates(self, *coordinates: int) -> tuple[int, ...]:\n+        \"\"\"Scale a variable number of coordinates by the given scale factor.\"\"\"\n+        scale_factor = self.get_scale_factor()\n+        if scale_factor != 1.0:\n+            coordinates = tuple(int(round(c * scale_factor)) for c in coordinates)\n+\n+        return coordinates\ndiff --git a/python/adb_auto_player/games/afk_journey/run.py b/python/adb_auto_player/games/afk_journey/run.py\nindex 9ad207a1..f1d3e184 100644\n--- a/python/adb_auto_player/games/afk_journey/run.py\n+++ b/python/adb_auto_player/games/afk_journey/run.py\n@@ -11,25 +11,31 @@ from adb_auto_player.exceptions import (\n     NotInitializedError,\n )\n from adb_auto_player.games.afk_journey.config import Config\n-from adb_auto_player.games.game import Game\n+from adb_auto_player.game import Game\n from adb_auto_player.config_loader import get_games_dir\n \n \n class AFKJourney(Game):\n+    template_dir_path: Path | None = None\n+    config_file_path: Path | None = None\n+\n+    # Timeout constants (in seconds)\n     BATTLE_TIMEOUT: int = 180\n+    MIN_TIMEOUT: int = 10\n+\n+    # Store keys\n     STORE_SEASON: str = \"SEASON\"\n     STORE_MODE: str = \"MODE\"\n+    STORE_MAX_ATTEMPTS_REACHED: str = \"MAX_ATTEMPTS_REACHED\"\n+    STORE_FORMATION_NUM: str = \"FORMATION_NUM\"\n+\n+    # Game modes\n     MODE_DURAS_TRIALS: str = \"DURAS_TRIALS\"\n     MODE_AFK_STAGES: str = \"AFK_STAGES\"\n     MODE_LEGEND_TRIALS: str = \"LEGEND_TRIALS\"\n-    STORE_MAX_ATTEMPTS_REACHED: str = \"MAX_ATTEMPTS_REACHED\"\n-    STORE_FORMATION_NUM: str = \"FORMATION_NUM\"\n-    CONFIG_GENERAL: str = \"general\"\n-    CONFIG_AFK_STAGES: str = \"afk_stages\"\n-    CONFIG_DURAS_TRIALS: str = \"duras_trials\"\n \n-    template_dir_path: Path | None = None\n-    config_file_path: Path | None = None\n+    # UI coordinates (x1, y1, x2, y2)\n+    RECORDS_FORMATION_SELECT_AREA: tuple[int, int, int, int] = (450, 280, 780, 400)\n \n     def start_up(self) -> None:\n         if self.device is None:\n@@ -160,13 +166,14 @@ class AFKJourney(Game):\n         while counter > 1:\n             formation_next = self.wait_for_template(\n                 \"battle/formation_next.png\",\n-                timeout=5,\n+                timeout=self.MIN_TIMEOUT,\n                 timeout_message=f\"Formation #{formation_num} not found\",\n             )\n             self.click(*formation_next)\n-            sleep(1)\n+            self.wait_for_roi_change(\n+                *self.RECORDS_FORMATION_SELECT_AREA, delay=0.5, timeout=self.MIN_TIMEOUT\n+            )\n             counter -= 1\n-        sleep(1)\n         excluded_hero = self.__formation_contains_excluded_hero()\n         if excluded_hero is not None:\n             logging.warning(\n@@ -181,7 +188,7 @@ class AFKJourney(Game):\n         self.click(*records)\n         copy = self.wait_for_template(\n             \"copy.png\",\n-            timeout=5,\n+            timeout=self.MIN_TIMEOUT,\n             timeout_message=\"No formations available for this battle\",\n         )\n \n@@ -609,7 +616,7 @@ class AFKJourney(Game):\n             template, x, y = self.wait_for_any_template(\n                 [\"assist/join_now.png\", \"assist/synergy.png\", \"assist/chat_button.png\"],\n                 delay=0.1,\n-                timeout=2,\n+                timeout=self.MIN_TIMEOUT,\n             )\n         except TimeoutException:\n             return False\n@@ -637,7 +644,7 @@ class AFKJourney(Game):\n         return False\n \n     def __handle_corrupt_creature(self) -> bool:\n-        ready = self.wait_for_template(\"assist/ready.png\", timeout=10)\n+        ready = self.wait_for_template(\"assist/ready.png\", timeout=self.MIN_TIMEOUT)\n \n         self.click(*ready)\n         # Sometimes people wait forever for a third to join...\n@@ -759,7 +766,7 @@ class AFKJourney(Game):\n             threshold=0.8,\n             grayscale=True,\n             delay=0.5,\n-            timeout=6,\n+            timeout=self.MIN_TIMEOUT,\n         )\n         _, x, y = challenge_btn\n         self.click(x, y)\n@@ -832,7 +839,7 @@ class AFKJourney(Game):\n         while True:\n             self.wait_for_template(\n                 \"event/monopoly_assist/log.png\",\n-                timeout=5,\n+                timeout=self.MIN_TIMEOUT,\n                 timeout_message=\"Monopoly assists screen not found\",\n             )\n \ndiff --git a/python/adb_auto_player/template_matching.py b/python/adb_auto_player/template_matching.py\nnew file mode 100644\nindex 00000000..d7adb538\n--- /dev/null\n+++ b/python/adb_auto_player/template_matching.py\n@@ -0,0 +1,227 @@\n+from enum import StrEnum, auto\n+from pathlib import Path\n+\n+import cv2\n+import numpy as np\n+from PIL import Image\n+\n+\n+class MatchMode(StrEnum):\n+    BEST = auto()\n+    TOP_LEFT = auto()\n+    TOP_RIGHT = auto()\n+    BOTTOM_LEFT = auto()\n+    BOTTOM_RIGHT = auto()\n+    LEFT_TOP = auto()\n+    LEFT_BOTTOM = auto()\n+    RIGHT_TOP = auto()\n+    RIGHT_BOTTOM = auto()\n+\n+\n+template_cache: dict[str, Image.Image] = {}\n+\n+\n+def load_image(image_path: Path, image_scale_factor: float = 1.0) -> Image.Image:\n+    \"\"\"\n+    Loads an image from disk or returns the cached version if available.\n+    Resizes the image if needed and stores it in the global template_cache.\n+\n+    :param image_path: Path to the template image.\n+    :param image_scale_factor: Scale factor for resizing the image.\n+    :return: PIL Image object.\n+\n+    :raises FileNotFoundError:\n+    :raises IOError:\n+    \"\"\"\n+\n+    cache_key = f\"{str(image_path)}_{image_scale_factor}\"\n+    if cache_key in template_cache:\n+        return template_cache[cache_key]\n+\n+    image = Image.open(image_path)\n+    image.load()\n+\n+    if image_scale_factor != 1.0:\n+        new_width = int(image.width * image_scale_factor)\n+        new_height = int(image.height * image_scale_factor)\n+        image = image.resize((new_width, new_height), Image.Resampling.LANCZOS)\n+\n+    template_cache[cache_key] = image\n+    return image\n+\n+\n+def compare_roi_similarity(\n+    base_image: Image.Image,\n+    template_image: Image.Image,\n+    roi: tuple[int, int, int, int],\n+    threshold: float = 0.9,\n+    grayscale: bool = False,\n+) -> bool:\n+    \"\"\"\n+    Compares the similarity of a region of interest (ROI) between two images.\n+\n+    Args:\n+        base_image: The reference image.\n+        template_image: The image to compare against.\n+        roi: A tuple (sx, sy, ex, ey) representing the coordinates of the ROI.\n+        threshold: Minimum similarity threshold (0-1). If the similarity is below this,\n+          returns False.\n+        grayscale: Whether to convert both images to grayscale before comparison.\n+\n+    Returns:\n+        bool: True if the ROI in the base_image matches based on the given threshold.\n+    \"\"\"\n+    __validate_threshold(threshold)\n+    sx, sy, ex, ey = roi\n+\n+    if (\n+        base_image.width != template_image.width\n+        or base_image.height != template_image.height\n+    ):\n+        raise ValueError(\n+            \"The dimensions of the base image and template image do not match.\"\n+        )\n+\n+    if not (0 <= sx < ex <= base_image.width and 0 <= sy < ey <= base_image.height):\n+        raise ValueError(\"Invalid ROI coordinates\")\n+\n+    base_cv = cv2.cvtColor(np.array(base_image), cv2.COLOR_RGB2BGR)\n+    template_cv = cv2.cvtColor(np.array(template_image), cv2.COLOR_RGB2BGR)\n+\n+    if grayscale:\n+        base_cv = cv2.cvtColor(base_cv, cv2.COLOR_BGR2GRAY)\n+        template_cv = cv2.cvtColor(template_cv, cv2.COLOR_BGR2GRAY)\n+\n+    roi_base = base_cv[sy:ey, sx:ex]\n+    roi_template = template_cv[sy:ey, sx:ex]\n+\n+    result = cv2.matchTemplate(roi_base, roi_template, method=cv2.TM_CCOEFF_NORMED)\n+    return np.max(result) >= threshold\n+\n+\n+def find_template_match(\n+    base_image: Image.Image,\n+    template_image: Image.Image,\n+    match_mode: MatchMode = MatchMode.BEST,\n+    threshold: float = 0.9,\n+    grayscale: bool = False,\n+) -> tuple[int, int] | None:\n+    \"\"\"\n+    Find a template image within a base image with different matching modes.\n+\n+    Args:\n+        base_image: The image to search in\n+        template_image: The template to search for\n+        match_mode: The mode determining which match to return if multiple are found\n+        threshold: Minimum similarity threshold (0-1)\n+        grayscale: Whether to convert images to grayscale before matching\n+\n+    Returns:\n+        tuple of (center_x, center_y) coordinates or None if no match found\n+    \"\"\"\n+    __validate_threshold(threshold)\n+    base_cv = cv2.cvtColor(np.array(base_image), cv2.COLOR_RGB2BGR)\n+    template_cv = cv2.cvtColor(np.array(template_image), cv2.COLOR_RGB2BGR)\n+\n+    if grayscale:\n+        base_cv = cv2.cvtColor(base_cv, cv2.COLOR_BGR2GRAY)\n+        template_cv = cv2.cvtColor(template_cv, cv2.COLOR_BGR2GRAY)\n+\n+    result = cv2.matchTemplate(base_cv, template_cv, cv2.TM_CCOEFF_NORMED)\n+    if match_mode == MatchMode.BEST:\n+        _, max_val, _, max\n\n… truncated at 24,000 bytes of 28,032", "bytes": 28032, "truncated": true}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/python/.idea/codeStyles/Project.xml b/python/.idea/codeStyles/Project.xml\ndeleted file mode 100644\nindex 6ba17248..00000000\n--- a/python/.idea/codeStyles/Project.xml\n+++ /dev/null\n@@ -1,6 +0,0 @@\n-<component name=\"ProjectCodeStyleConfiguration\">\n-  <code_scheme name=\"Project\" version=\"173\">\n-    <option name=\"LINE_SEPARATOR\" value=\"&#10;\" />\n-    <option name=\"RIGHT_MARGIN\" value=\"88\" />\n-  </code_scheme>\n-</component>\n\\ No newline at end of file\ndiff --git a/python/.idea/codeStyles/codeStyleConfig.xml b/python/.idea/codeStyles/codeStyleConfig.xml\ndeleted file mode 100644\nindex 79ee123c..00000000\n--- a/python/.idea/codeStyles/codeStyleConfig.xml\n+++ /dev/null\n@@ -1,5 +0,0 @@\n-<component name=\"ProjectCodeStyleConfiguration\">\n-  <state>\n-    <option name=\"USE_PER_PROJECT_SETTINGS\" value=\"true\" />\n-  </state>\n-</component>\n\\ No newline at end of file\ndiff --git a/python/adb_auto_player/games/afk_journey/run.py b/python/adb_auto_player/games/afk_journey/run.py\nindex f1d3e184..9ad207a1 100644\n--- a/python/adb_auto_player/games/afk_journey/run.py\n+++ b/python/adb_auto_player/games/afk_journey/run.py\n@@ -11,31 +11,25 @@ from adb_auto_player.exceptions import (\n     NotInitializedError,\n )\n from adb_auto_player.games.afk_journey.config import Config\n-from adb_auto_player.game import Game\n+from adb_auto_player.games.game import Game\n from adb_auto_player.config_loader import get_games_dir\n \n \n class AFKJourney(Game):\n-    template_dir_path: Path | None = None\n-    config_file_path: Path | None = None\n-\n-    # Timeout constants (in seconds)\n     BATTLE_TIMEOUT: int = 180\n-    MIN_TIMEOUT: int = 10\n-\n-    # Store keys\n     STORE_SEASON: str = \"SEASON\"\n     STORE_MODE: str = \"MODE\"\n-    STORE_MAX_ATTEMPTS_REACHED: str = \"MAX_ATTEMPTS_REACHED\"\n-    STORE_FORMATION_NUM: str = \"FORMATION_NUM\"\n-\n-    # Game modes\n     MODE_DURAS_TRIALS: str = \"DURAS_TRIALS\"\n     MODE_AFK_STAGES: str = \"AFK_STAGES\"\n     MODE_LEGEND_TRIALS: str = \"LEGEND_TRIALS\"\n+    STORE_MAX_ATTEMPTS_REACHED: str = \"MAX_ATTEMPTS_REACHED\"\n+    STORE_FORMATION_NUM: str = \"FORMATION_NUM\"\n+    CONFIG_GENERAL: str = \"general\"\n+    CONFIG_AFK_STAGES: str = \"afk_stages\"\n+    CONFIG_DURAS_TRIALS: str = \"duras_trials\"\n \n-    # UI coordinates (x1, y1, x2, y2)\n-    RECORDS_FORMATION_SELECT_AREA: tuple[int, int, int, int] = (450, 280, 780, 400)\n+    template_dir_path: Path | None = None\n+    config_file_path: Path | None = None\n \n     def start_up(self) -> None:\n         if self.device is None:\n@@ -166,14 +160,13 @@ class AFKJourney(Game):\n         while counter > 1:\n             formation_next = self.wait_for_template(\n                 \"battle/formation_next.png\",\n-                timeout=self.MIN_TIMEOUT,\n+                timeout=5,\n                 timeout_message=f\"Formation #{formation_num} not found\",\n             )\n             self.click(*formation_next)\n-            self.wait_for_roi_change(\n-                *self.RECORDS_FORMATION_SELECT_AREA, delay=0.5, timeout=self.MIN_TIMEOUT\n-            )\n+            sleep(1)\n             counter -= 1\n+        sleep(1)\n         excluded_hero = self.__formation_contains_excluded_hero()\n         if excluded_hero is not None:\n             logging.warning(\n@@ -188,7 +181,7 @@ class AFKJourney(Game):\n         self.click(*records)\n         copy = self.wait_for_template(\n             \"copy.png\",\n-            timeout=self.MIN_TIMEOUT,\n+            timeout=5,\n             timeout_message=\"No formations available for this battle\",\n         )\n \n@@ -616,7 +609,7 @@ class AFKJourney(Game):\n             template, x, y = self.wait_for_any_template(\n                 [\"assist/join_now.png\", \"assist/synergy.png\", \"assist/chat_button.png\"],\n                 delay=0.1,\n-                timeout=self.MIN_TIMEOUT,\n+                timeout=2,\n             )\n         except TimeoutException:\n             return False\n@@ -644,7 +637,7 @@ class AFKJourney(Game):\n         return False\n \n     def __handle_corrupt_creature(self) -> bool:\n-        ready = self.wait_for_template(\"assist/ready.png\", timeout=self.MIN_TIMEOUT)\n+        ready = self.wait_for_template(\"assist/ready.png\", timeout=10)\n \n         self.click(*ready)\n         # Sometimes people wait forever for a third to join...\n@@ -766,7 +759,7 @@ class AFKJourney(Game):\n             threshold=0.8,\n             grayscale=True,\n             delay=0.5,\n-            timeout=self.MIN_TIMEOUT,\n+            timeout=6,\n         )\n         _, x, y = challenge_btn\n         self.click(x, y)\n@@ -839,7 +832,7 @@ class AFKJourney(Game):\n         while True:\n             self.wait_for_template(\n                 \"event/monopoly_assist/log.png\",\n-                timeout=self.MIN_TIMEOUT,\n+                timeout=5,\n                 timeout_message=\"Monopoly assists screen not found\",\n             )\n \ndiff --git a/python/adb_auto_player/game.py b/python/adb_auto_player/games/game.py\nsimilarity index 80%\nrename from python/adb_auto_player/game.py\nrename to python/adb_auto_player/games/game.py\nindex acfecbea..1dcfcdd0 100644\n--- a/python/adb_auto_player/game.py\n+++ b/python/adb_auto_player/games/game.py\n@@ -2,7 +2,7 @@ import io\n from abc import abstractmethod\n from pathlib import Path\n from time import sleep, time\n-from typing import Any, TypeVar, Callable, Literal\n+from typing import Any, TypeVar, Callable\n \n from PIL import Image\n from adbutils._device import AdbDevice\n@@ -10,7 +10,7 @@ from pydantic import BaseModel\n \n import adb_auto_player.adb as adb\n import logging\n-import adb_auto_player.template_matching as template_matching\n+import adb_auto_player.screen_utils as screen_utils\n from adb_auto_player.command import Command\n from adb_auto_player.exceptions import (\n     UnsupportedResolutionException,\n@@ -18,7 +18,7 @@ from adb_auto_player.exceptions import (\n     AdbException,\n     NotInitializedError,\n )\n-from adb_auto_player.template_matching import MatchMode\n+from adb_auto_player.screen_utils import MatchMode\n \n \n class Game:\n@@ -125,7 +125,9 @@ class Game:\n         if not scale:\n             self.get_device().click(x, y)\n             return None\n-        x, y = self.__scale_coordinates(x, y)\n+        if self.get_scale_factor() != 1.0:\n+            x = int(round(x * self.get_scale_factor()))\n+            y = int(round(y * self.get_scale_factor()))\n         self.get_device().click(x, y)\n         return None\n \n@@ -153,88 +155,21 @@ class Game:\n         else:\n             return self.get_screenshot()\n \n-    def roi_has_changed(\n-        self,\n-        sx: int,\n-        sy: int,\n-        ex: int,\n-        ey: int,\n-        threshold: float = 0.9,\n-        grayscale: bool = False,\n-    ):\n-        prev = self.previous_screenshot\n-        if prev is None:\n-            raise ValueError(\n-                \"Region of interest cannot have changed if \"\n-                \"there is no previous screenshot.\"\n-            )\n-\n-        sx, sy, ex, ey = self.__scale_coordinates(sx, sy, ex, ey)\n-\n-        return not template_matching.compare_roi_similarity(\n-            prev,\n-            self.get_screenshot(),\n-            roi=(sx, sy, ex, ey),\n-            threshold=threshold,\n-            grayscale=grayscale,\n-        )\n-\n-    def wait_for_roi_change(\n-        self,\n-        sx: int,\n-        sy: int,\n-        ex: int,\n-        ey: int,\n-        threshold: float = 0.9,\n-        grayscale: bool = False,\n-        delay: float = 1,\n-        timeout: float = 30,\n-        timeout_message: str | None = None,\n-    ) -> bool:\n-        \"\"\"\n-        :raises TimeoutException:\n-        \"\"\"\n-\n-        sx, sy, ex, ey = self.__scale_coordinates(sx, sy, ex, ey)\n-\n-        def roi_changed() -> Literal[True] | None:\n-            result = self.roi_has_changed(\n-                sx=sx,\n-                sy=sy,\n-                ex=ex,\n-                ey=ey,\n-                threshold=threshold,\n-                grayscale=grayscale,\n-            )\n-\n-            if result is True:\n-                return True\n-            return None\n-\n-        if timeout_message is None:\n-            timeout_message = (\n-                f\"Region of Interest has not changed after {timeout} seconds\"\n-            )\n-\n-        return self.__execute_or_timeout(\n-            roi_changed, delay=delay, timeout=timeout, timeout_message=timeout_message\n-        )\n-\n     def find_template_match(\n         self,\n         template: str,\n-        match_mode: template_matching.MatchMode = template_matching.MatchMode.BEST,\n+        match_mode: screen_utils.MatchMode = screen_utils.MatchMode.BEST,\n         threshold: float = 0.9,\n         grayscale: bool = False,\n         use_previous_screenshot: bool = False,\n     ) -> tuple[int, int] | None:\n         template_path = self.get_template_dir_path() / template\n \n-        return template_matching.find_template_match(\n+        return screen_utils.find_template_match(\n             base_image=self.__get_screenshot(\n                 previous_screenshot=use_previous_screenshot\n             ),\n-            template_image=template_matching.load_image(\n+            template_image=screen_utils.load_image(\n                 image_path=template_path,\n                 image_scale_factor=self.get_scale_factor(),\n             ),\n@@ -253,11 +188,11 @@ class Game:\n     ) -> list[tuple[int, int]]:\n         template_path = self.get_template_dir_path() / template\n \n-        return template_matching.find_all_template_matches(\n+        return screen_utils.find_all_template_matches(\n             base_image=self.__get_screenshot(\n                 previous_screenshot=use_previous_screenshot\n             ),\n-            template_image=template_matching.load_image(\n+            template_image=screen_utils.load_image(\n                 image_path=template_path,\n                 image_scale_factor=self.get_scale_factor(),\n             ),\n@@ -405,7 +340,11 @@ class Game:\n         self.swipe(sx=540, sy=sy, ex=540, ey=ey, duration=duration)\n \n     def swipe(self, sx: int, sy: int, ex: int, ey: int, duration: float = 1.0):\n-        sx, sy, ex, ey = self.__scale_coordinates(sx, sy, ex, ey)\n+        if self.get_scale_factor() != 1.0:\n+            sx = int(round(sx * self.get_scale_factor()))\n+            sy = int(round(sy * self.get_scale_factor()))\n+            ex = int(round(ex * self.get_scale_factor()))\n+            ey = int(round(ey * self.get_scale_factor()))\n         self.get_device().swipe(sx=sx, sy=sy, ex=ex, ey=ey, duration=duration)\n         sleep(2)\n \n@@ -441,11 +380,3 @@ class Game:\n \n             if end_time <= time():\n                 end_time_exceeded = True\n-\n-    def __scale_coordinates(self, *coordinates: int) -> tuple[int, ...]:\n-        \"\"\"Scale a variable number of coordinates by the given scale factor.\"\"\"\n-        scale_factor = self.get_scale_factor()\n-        if scale_factor != 1.0:\n-            coordinates = tuple(int(round(c * scale_factor)) for c in coordinates)\n-\n-        return coordinates\ndiff --git a/python/adb_auto_player/template_matching.py b/python/adb_auto_player/screen_utils.py\nsimilarity index 72%\nrename from python/adb_auto_player/template_matching.py\nrename to python/adb_auto_player/screen_utils.py\nindex d7adb538..295634b5 100644\n--- a/python/adb_auto_player/template_matching.py\n+++ b/python/adb_auto_player/screen_utils.py\n@@ -50,55 +50,6 @@ def load_image(image_path: Path, image_scale_factor: float = 1.0) -> Image.Image\n     return image\n \n \n-def compare_roi_similarity(\n-    base_image: Image.Image,\n-    template_image: Image.Image,\n-    roi: tuple[int, int, int, int],\n-    threshold: float = 0.9,\n-    grayscale: bool = False,\n-) -> bool:\n-    \"\"\"\n-    Compares the similarity of a region of interest (ROI) between two images.\n-\n-    Args:\n-        base_image: The reference image.\n-        template_image: The image to compare against.\n-        roi: A tuple (sx, sy, ex, ey) representing the coordinates of the ROI.\n-        threshold: Minimum similarity threshold (0-1). If the similarity is below this,\n-          returns False.\n-        grayscale: Whether to convert both images to grayscale before comparison.\n-\n-    Returns:\n-        bool: True if the ROI in the base_image matches based on the given threshold.\n-    \"\"\"\n-    __validate_threshold(threshold)\n-    sx, sy, ex, ey = roi\n-\n-    if (\n-        base_image.width != template_image.width\n-        or base_image.height != template_image.height\n-    ):\n-        raise ValueError(\n-            \"The dimensions of the base image and template image do not match.\"\n-        )\n-\n-    if not (0 <= sx < ex <= base_image.width and 0 <= sy < ey <= base_image.height):\n-        raise ValueError(\"Invalid ROI coordinates\")\n-\n-    base_cv = cv2.cvtColor(np.array(base_image), cv2.COLOR_RGB2BGR)\n-    template_cv = cv2.cvtColor(np.array(template_image), cv2.COLOR_RGB2BGR)\n-\n-    if grayscale:\n-        base_cv = cv2.cvtColor(base_cv, cv2.COLOR_BGR2GRAY)\n-        template_cv = cv2.cvtColor(template_cv, cv2.COLOR_BGR2GRAY)\n-\n-    roi_base = base_cv[sy:ey, sx:ex]\n-    roi_template = template_cv[sy:ey, sx:ex]\n-\n-    result = cv2.matchTemplate(roi_base, roi_template, method=cv2.TM_CCOEFF_NORMED)\n-    return np.max(result) >= threshold\n-\n-\n def find_template_match(\n     base_image: Image.Image,\n     template_image: Image.Image,\n@@ -119,7 +70,6 @@ def find_template_match(\n     Returns:\n         tuple of (center_x, center_y) coordinates or None if no match found\n     \"\"\"\n-    __validate_threshold(threshold)\n     base_cv = cv2.cvtColor(np.array(base_image), cv2.COLOR_RGB2BGR)\n     template_cv = cv2.cvtColor(np.array(template_image), cv2.COLOR_RGB2BGR)\n \n@@ -170,7 +120,6 @@ def find_all_template_matches(\n     grayscale: bool = False,\n     min_distance: int = 10,\n ) -> list[tuple[int, int]]:\n-    __validate_threshold(threshold)\n     base_cv = cv2.cvtColor(np.array(base_image), cv2.COLOR_RGB2BGR)\n     template_cv = cv2.cvtColor(np.array(template_image), cv2.COLOR_RGB2BGR)\n \n@@ -216,12 +165,3 @@ def __suppress_close_matches(\n         ):\n             suppressed.append(match_tuple)  # type: ignore\n     return suppressed\n-\n-\n-def __validate_threshold(threshold: float) -> None:\n-    \"\"\"\n-    Raises:\n-        ValueError: If the threshold is less than 0 or greater than 1.\n-    \"\"\"\n-    if threshold < 0.0 or threshold > 1.0:\n-        raise ValueError(f\"Threshold must be between 0 and 1, got {threshold}\")\ndiff --git a/python/poetry.lock b/python/poetry.lock\nindex 847faea5..7e5d9097 100644\n--- a/python/poetry.lock\n+++ b/python/poetry.lock\n@@ -311,12 +311,12 @@ files = [\n \n [[package]]\n name = \"nuitka\"\n-version = \"2.6.4\"\n+version = \"2.6.3\"\n description = \"Python compiler with full language support and CPython compatibility\"\n optional = false\n python-versions = \"*\"\n files = [\n-    {file = \"Nuitka-2.6.4.tar.gz\", hash = \"sha256:9fc8d5d16b7fd43520312128cd979eea00f5abbffbce59cc2e6f18cd615dfedb\"},\n+    {file = \"Nuitka-2.6.3.tar.gz\", hash = \"sha256:12638d21de0358ddccd3eaaf694c46d6ad71ac67d9ee7c7c0d948e34fbbb2002\"},\n ]\n \n [package.dependencies]\ndiff --git a/python/tests/data/records_formation_1.png b/python/tests/data/records_formation_1.png\ndeleted file mode 100644\nindex 687c3c73..00000000\nBinary files a/python/tests/data/records_formation_1.png and /dev/null differ\ndiff --git a/python/tests/data/records_formation_2.png b/python/tests/data/records_formation_2.png\ndeleted file mode 100644\nindex 67d97381..00000000\nBinary files a/python/tests/data/records_formation_2.png and /dev/null differ\ndiff --git a/python/tests/data/records_formation_5.png b/python/tests/data/records_formation_5.png\ndeleted file mode 100644\nindex 0bf34ef9..00000000\nBinary files a/python/tests/data/records_formation_5.png and /dev/null differ\ndiff --git a/python/tests/data/records_formation_6.png b/python/tests/data/records_formation_6.png\ndeleted file mode 100644\nindex f573a115..00000000\nBinary files a/python/tests/data/records_formation_6.png and /dev/null differ\ndiff --git a/python/tests/data/records_formation_7.png b/python/tests/data/records_formation_7.png\ndeleted file mode 100644\nindex fbe48232..00000000\nBinary files a/python/tests/data/records_formation_7.png and /dev/null differ\ndiff --git a/python/tests/test_game.py b/python/tests/test_game.py\ndeleted file mode 100644\nindex 0ea70d48..00000000\n--- a/python/tests/test_game.py\n+++ /dev/null\n@@ -1,61 +0,0 @@\n-import unittest\n-from unittest.mock import patch\n-\n-from pydantic import BaseModel\n-from pathlib import Path\n-from adb_auto_player.command import Command\n-from adb_auto_player.exceptions import NotInitializedError\n-from adb_auto_player.game import Game\n-import adb_auto_player.template_matching as template_matching\n-\n-TEST_DATA_DIR = Path(__file__).parent / \"data\"\n-\n-\"\"\"\n-A test double for Game class that provides minimal implementations\n-of abstract methods. Used primarily for testing roi_has_changed functionality\n-where these implementations are not critical.\n-\"\"\"\n-\n-\n-class MockGame(Game):\n-    def get_template_dir_path(self) -> Path:\n-        return Path()\n-\n-    def load_config(self) -> None:\n-        return None\n-\n-    def get_menu_commands(self) -> list[Command]:\n-        return list()\n-\n-    def get_supported_resolutions(self) -> list[str]:\n-        return list()\n-\n-    def get_config(self) -> BaseModel:\n-        return BaseModel()\n-\n-\n-class TestGame(unittest.TestCase):\n-    @patch.object(Game, \"get_screenshot\")\n-    def test_roi_has_changed(self, mock_get_screenshot):\n-        game = Game()\n-        with self.assertRaises(ValueError):\n-            game.roi_has_changed(0, 0, 0, 0)\n-\n-        roi = (450, 280, 780, 400)\n-        f1 = TEST_DATA_DIR / \"records_formation_1.png\"\n-        f2 = TEST_DATA_DIR / \"records_formation_2.png\"\n-\n-        game.previous_screenshot = template_matching.load_image(f1)\n-        mock_get_screenshot.return_value = template_matching.load_image(f1)\n-\n-        # Missing scale factor\n-        with self.assertRaises(NotInitializedError):\n-            game.roi_has_changed(*roi)\n-\n-        game.scale_factor = 1.0\n-\n-        self.assertFalse(game.roi_has_changed(*roi))\n-\n-        mock_get_screenshot.return_value = template_matching.load_image(f2)\n-\n-        self.assertTrue(game.roi_has_changed(*roi))\ndiff --git a/python/tests/test_logging_setup.py b/python/tests/test_logging_setup.py\nindex 63095594..c5cabf1e 100644\n--- a/python/tests/test_logging_setup.py\n+++ b/python/tests/test_logging_setup.py\n@@ -4,6 +4,7 @@ import adb_auto_player.logging_setup\n \n \n class TestSanitizePath(unittest.TestCase):\n+\n     @patch(\"os.path.expanduser\", return_value=r\"C:\\Users\\mockuser\")  # Mock home dir\n     def test_windows_path(self, mock_expanduser):\n         log = r\"adb_path: C:\\Users\\mockuser\\AppData\\Local\\file.txt\"\n@@ -23,3 +24,7 @@ class TestSanitizePath(unittest.TestCase):\n         expected = r\"adb_path: C:\\Users\\<redacted>\\AppData\\file.txt\"\n         r\" and D:\\Users\\<redacted>\\Desktop\\file2.txt\"\n         self.assertEqual(expected, adb_auto_player.logging_setup.sanitize_path(log))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\ndiff --git a/python/tests/test_template_matching.py b/python/tests/test_template_matching.py\ndeleted file mode 100644\nindex 6b195255..00000000\n--- a/python/tests/test_template_matching.py\n+++ /dev/null\n@@ -1,76 +0,0 @@\n-import unittest\n-from pathlib import Path\n-\n-import adb_auto_player.template_matching as template_matching\n-\n-TEST_DATA_DIR = Path(__file__).parent / \"data\"\n-\n-\n-class TestTemplateMatching(unittest.TestCase):\n-    def test_compare_roi_similarity_validation(self):\n-        f1 = TEST_DATA_DIR / \"records_formation_1.png\"\n-\n-        # coordinates cannot be negative\n-        with self.assertRaises(ValueError):\n-            template_matching.compare_roi_similarity(\n-                template_matching.load_image(f1),\n-                template_matching.load_image(f1),\n-                (-1, -1, -1, -1),\n-            )\n-\n-        # sx and sy need to be smaller than ex and ey\n-        with self.assertRaises(ValueError):\n-            template_matching.compare_roi_similarity(\n-                template_matching.load_image(f1),\n-                template_matching.load_image(f1),\n-                (1, 1, 1, 1),\n-            )\n-\n-        # sx and sy greater than ex and ey\n-        with self.assertRaises(ValueError):\n-            template_matching.compare_roi_similarity(\n-                template_matching.load_image(f1),\n-                template_matching.load_image(f1),\n-                (2, 2, 1, 1),\n-            )\n-\n-        # ey larger than base_image height\n-        with self.assertRaises(ValueError):\n-            template_matching.compare_roi_similarity(\n-                template_matching.load_image(f1),\n-                template_matching.load_image(f1),\n-                (0, 0, 1, 1921),\n-            )\n-\n-    def test_compare_roi_similarity_templates(self):\n-        f1 = TEST_DATA_DIR / \"records_formation_1.png\"\n-        f2 = TEST_DATA_DIR / \"records_formation_2.png\"\n-        f5 = TEST_DATA_DIR / \"records_formation_5.png\"\n-        f6 = TEST_DATA_DIR / \"records_formation_6.png\"\n-        f7 = TEST_DATA_DIR / \"records_formation_7.png\"\n-\n-        roi = (450, 280, 780, 400)\n-        result = template_matching.compare_roi_similarity(\n-            template_matching.load_image(f1), template_matching.load_image(f1), roi\n-        )\n-        self.assertTrue(result)\n-\n-        result = template_matching.compare_roi_similarity(\n-            template_matching.load_image(f1), template_matching.load_image(f2), roi\n-        )\n-        self.assertFalse(result)\n-\n-        result = template_matching.compare_roi_similarity(\n-            template_matching.load_image(f5), template_matching.load_image(f6), roi\n-        )\n-        self.assertFalse(result)\n-\n-        result = template_matching.compare_roi_similarity(\n-            template_matching.load_image(f6), template_matching.load_image(f7), roi\n-        )\n-        self.assertFalse(result)\n-\n-        result = template_matching.compare_roi_similarity(\n-            template_matching.load_image(f7), template_matching.load_image(f7), roi\n-        )\n-        self.assertTrue(result)\n", "bytes": 21997, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    python3-dev \\\n    libgl1 \\\n    libglib2.0-0 \\\n    libsm6 \\\n    libxext6 \\\n    libxrender-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone https://github.com/AdbAutoPlayer/AdbAutoPlayer.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 1660fa9561a87148db70bbcbff3abada3ed6243e || git fetch --depth 1 origin \"+refs/pull/9/head:refs/remotes/origin/pr/9\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src/python\n\nENV CI=true\n\n# Install poetry\nRUN curl -sSL https://install.python-poetry.org | python3 - && \\\n    ln -s /root/.local/bin/poetry /usr/local/bin/poetry\n\n# Configure poetry to create venv in project directory\nRUN poetry config virtualenvs.in-project true\n\n# Install project dependencies, then add pytest\nRUN poetry install && \\\n    poetry run pip install pytest\n\n# Apply bug.patch from repo root (patch paths use python/ prefix)\nCOPY bug.patch /tmp/bug.patch\nWORKDIR /app/src\nRUN patch -p1 --batch --no-backup-if-mismatch < /tmp/bug.patch || true && rm /tmp/bug.patch\n\nWORKDIR /app/src/python\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/app/src/python/.venv/bin:${PATH}\"\nWORKDIR /app/src/python\n", "bytes": 1485, "truncated": false}]}, {"task_name": "adrn__thejoker-110", "repo": "adrn/thejoker", "language": "Python", "difficulty": "medium", "area": "library", "topic": "thejoker", "bug_class": "missing-implementation", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "The `samples_analysis` module is missing implementation for several functions used to compute time sampling statistics for samples returned from The Joker. These functions are intended to analyze the sampling coverage and modal properties of orbital period samples.\n\nImplement the following functions in `thejoker/samples_analysis.py` (or equivalent location):\n\n1. `MAP_sample(samples, return_index=False)`:\n   - Input: `samples` (JokerSamples object containing 'ln_likelihood' and 'ln_prior' arrays)\n   - Output: Returns the sample with the maximum posterior probability (ln_likelihood + ln_prior)\n   - If `return_index=True`, also return the index of the maximum sample\n   - Example: If `samples['ln_likelihood'] = [1., 2, 3.]` and `samples['ln_prior'] = [1., 2, 3.]`, the MAP sample has ln_likelihood=3.0\n\n2. `is_P_unimodal(samples, data)`:\n   - Input: `samples` (JokerSamples with 'P' column in units of days), `data` (RVData object with observation times)\n   - Output: Returns True if the posterior distribution of periods appears unimodal, False otherwise\n   - Uses clustering analysis on the period samples\n\n3. `is_P_Kmodal(samples, data, n_clusters=2)`:\n   - Input: `samples` (JokerSamples with 'P' column), `data` (RVData object), `n_clusters` (number of clusters to detect)\n   - Output: Returns a tuple (Kmodal, means, npermode) where:\n     - Kmodal: Boolean indicating if the distribution is K-modal (has multiple modes)\n     - means: Array of cluster center periods\n     - npermode: Array of number of samples per mode\n   - Example: If periods cluster around 8 and 21 days with 32 samples each, returns (True, [8*u.day, 21*u.day], [32, 32])\n\n4. `max_phase_gap(sample, data)`:\n   - Input: `sample` (a single JokerSample with 'P' parameter), `data` (RVData object with observation times)\n   - Output: Returns the maximum phase gap in the sampling coverage as a float\n   - Phase is computed relative to the sample's period and reference time\n   - Example: For observations at phases [0, 0.1, 0.7, 0.8, 0.9], returns approximately 0.6\n\n5. `phase_coverage(sample, data)`:\n   - Input: `sample` (a single JokerSample with 'P' parameter), `data` (RVData object with observation times)\n   - Output: Returns the fraction of the orbital phase that is covered by observations (float between 0 and 1)\n   - Example: For observations at phases [0, 0.1, 0.7, 0.8, 0.9], returns a value between 0.4 and 0.6\n\n6. `periods_spanned(data)`:\n   - Input: `data` (RVData object with observation times)\n   - Output: Returns the total time span of observations in days\n\nThese functions should handle astropy quantities with units (e.g., `u.day`, `u.km/u.s`) and work with the existing `JokerSamples` and `RVData` classes.", "bytes": 2706, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src\n\nexport CI=true\nsource /opt/venv/bin/activate\n\n# Copy test files from /tests into the package test directory\nmkdir -p \"thejoker/tests\"\ncp \"/tests/thejoker/tests/test_sampler.py\" \"thejoker/tests/test_sampler.py\"\ncp \"/tests/thejoker/tests/test_samples.py\" \"thejoker/tests/test_samples.py\"\ncp \"/tests/thejoker/tests/test_samples_analysis.py\" \"thejoker/tests/test_samples_analysis.py\"\ncp \"/tests/thejoker/tests/test_prior.py\" \"thejoker/tests/test_prior.py\"\n\npytest -xvs \\\n    \"thejoker/tests/test_sampler.py\" \\\n    \"thejoker/tests/test_samples.py\" \\\n    \"thejoker/tests/test_samples_analysis.py\"\n\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 768, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/CHANGES.rst b/CHANGES.rst\nindex 1d77428..810b064 100644\n--- a/CHANGES.rst\n+++ b/CHANGES.rst\n@@ -1,5 +1,5 @@\n 1.2 (unreleased)\n-================\n+----------------\n \n - ``TheJoker`` now expects a ``numpy.random.Generator`` instance instead of a\n   ``RandomState`` for controlling random number generation.\n@@ -9,8 +9,11 @@\n \n - Added functionality to compute ``t0``, i.e. the time of phase=0, for samples.\n \n+- Added functions to compute time-sampling statistics for the samples returned\n+  from The Joker (see: ``thejoker.samples_analysis``).\n+\n 1.1 (2020-04-19)\n-================\n+----------------\n \n - Removed ``astropy-helpers`` from the package infrastructure.\n - Switched to using ``tox`` for testing and documentation builds.\ndiff --git a/docs/changes.rst b/docs/changes.rst\nnew file mode 100644\nindex 0000000..11a246b\n--- /dev/null\n+++ b/docs/changes.rst\n@@ -0,0 +1,5 @@\n+*********\n+Changelog\n+*********\n+\n+.. include:: ../CHANGES.rst\ndiff --git a/docs/index.rst b/docs/index.rst\nindex cbd171b..2300afa 100644\n--- a/docs/index.rst\n+++ b/docs/index.rst\n@@ -21,7 +21,7 @@ method and applications.\n     :maxdepth: 1\n \n     install\n-    whats-new\n+    changes\n \n \n Tutorials\ndiff --git a/docs/whats-new.rst b/docs/whats-new.rst\ndeleted file mode 100644\nindex f4c9193..0000000\n--- a/docs/whats-new.rst\n+++ /dev/null\n@@ -1,38 +0,0 @@\n-***********\n-What's new?\n-***********\n-\n-v1.0\n-====\n-\n-TODO: API change! and new features...\n-\n-v0.3\n-====\n-\n-* **thejoker now has a new sampling mode: TheJoker.iterative_rejection_sample**.\n-  The default ``rejection_sample()`` method always uses the full prior samples\n-  library passed to it and will be inefficient when the data are noisy or\n-  uninformative. The new ``iterative_rejection_sample()`` method instead accepts\n-  a requested number of samples, and will try to predict how many prior samples\n-  it needs to use in order to return the desired number of posterior samples.\n-\n-* **thejoker now supports sampling over long-term velocity trends**. The\n-  default assumption for this sampler is that all systems are two-body systems.\n-  However, the sampler now supports hierarchical triple systems in some special\n-  cases. When the orbit of the outer body in a triple system is much longer than\n-  the orbit of the inner binary, and the outer orbital period is not fully\n-  observed, the influence of the outer body can be described by a long-term\n-  trend in the radial velocity that is approximately polynomial in time.\n-  ``thejoker`` now supports sampling over polynomial velocity trends with a\n-  specified polynomial degree. The default still assumes a constant velocity\n-  offset, i.e. that the barycentric velocity of the binary is constant.\n-\n-* **The sampler is ~4–10 times faster**. Thanks to some improvements on the\n-  implementation of the sampler (now in C + Cython), the sampler is\n-  significantly faster than the initial release.\n-\n-* **The sampler now optionally returns the prior and likelihood values of all\n-  posterior samples**. By specifying ``return_logprobs=True`` when running\n-  ``rejection_sample()`` or ``iterative_rejection_sample()``, the sampler now\n-  returns log-prior and log-likelihood values for any returned samples.\ndiff --git a/thejoker/__init__.py b/thejoker/__init__.py\nindex 9eb0fcd..93cdd8c 100644\n--- a/thejoker/__init__.py\n+++ b/thejoker/__init__.py\n@@ -3,11 +3,12 @@ from ._astropy_init import *  # noqa\n \n # For egg_info test builds to pass, put package imports here.\n if not _ASTROPY_SETUP_:  # noqa\n-    from .thejoker import TheJoker\n-    from .data import RVData\n-    from .samples import JokerSamples\n-    from .prior import JokerPrior\n-    from .plot import *\n+    from .thejoker import TheJoker  # noqa\n+    from .data import RVData  # noqa\n+    from .samples import JokerSamples  # noqa\n+    from .prior import JokerPrior  # noqa\n+    from .plot import plot_rv_curves, plot_phase_fold  # noqa\n+    from .samples_analysis import *  # noqa\n \n \n __bibtex__ = __citation__ = \"\"\"@ARTICLE{thejoker,\n@@ -33,12 +34,3 @@ archivePrefix = {arXiv},\n       adsnote = {Provided by the SAO/NASA Astrophysics Data System}\n }\n \"\"\"\n-\n-\n-__all__ = [\n-    'TheJoker',\n-    'RVData',\n-    'JokerSamples',\n-    'JokerPrior',\n-    'plot_rv_curves'\n-]\ndiff --git a/thejoker/likelihood_helpers.py b/thejoker/likelihood_helpers.py\nindex d270761..750d625 100644\n--- a/thejoker/likelihood_helpers.py\n+++ b/thejoker/likelihood_helpers.py\n@@ -210,3 +210,7 @@ def iterative_rejection_inmem(joker_helper, prior_samples_batch, random_state,\n         samples['ln_likelihood'] = all_marg_lls[good_samples_idx]\n \n     return samples\n+\n+\n+def ln_normal(x, mu, var):\n+    return -0.5 * (np.log(2*np.pi * var) + (x - mu)**2 / var)\ndiff --git a/thejoker/samples.py b/thejoker/samples.py\nindex 599d671..a99e467 100644\n--- a/thejoker/samples.py\n+++ b/thejoker/samples.py\n@@ -21,6 +21,7 @@ from .prior_helpers import (validate_poly_trend, validate_n_offsets,\n                             get_v0_offsets_equiv_units)\n from .samples_helpers import write_table_hdf5\n from .exceptions import TheJokerDeprecationWarning\n+from .likelihood_helpers import ln_normal\n \n __all__ = ['JokerSamples']\n \n@@ -527,3 +528,26 @@ class JokerSamples:\n     def copy(self):\n         \"\"\"Return a copy of this instance\"\"\"\n         return self.__class__(self.tbl.copy(), t_ref=self.t_ref)\n+\n+    def ln_unmarginalized_likelihood(self, data):\n+        \"\"\"\n+        Compute the log (unmarginalized) likelihood of the data for each sample\n+        \"\"\"\n+\n+        data_rv = data.rv.value\n+        data_unit = data.rv.unit\n+        data_var = (data.rv_err.to_value(data_unit)) ** 2\n+\n+        if 's' in self.tbl.colnames:\n+            s_vars = self['s'].to_value(data_unit) ** 2\n+        else:\n+            s_vars = np.zeros(len(self))\n+\n+        lls = np.full(len(self), np.nan)\n+        for i, (orbit, s) in enumerate(zip(self.orbits, s_vars)):\n+            model_rv = orbit.radial_velocity(data.t)\n+            lls[i] = ln_normal(model_rv.to_value(data_unit),\n+                               data_rv,\n+                               data_var + s).sum()\n+\n+        return lls\ndiff --git a/thejoker/samples_analysis.py b/thejoker/samples_analysis.py\nnew file mode 100644\nindex 0000000..b1b672f\n--- /dev/null\n+++ b/thejoker/samples_analysis.py\n@@ -0,0 +1,151 @@\n+# Standard library\n+\n+# Third-party\n+import astropy.units as u\n+import numpy as np\n+\n+__all__ = ['MAP_sample', 'is_P_unimodal', 'is_P_Kmodal',\n+           'max_phase_gap', 'phase_coverage', 'periods_spanned',\n+           'phase_coverage_per_period']\n+\n+\n+def MAP_sample(samples, return_index=False):\n+    \"\"\"Return the maximum a posteriori sample.\n+\n+    Parameters\n+    ----------\n+    samples : `~thejoker.JokerSamples`\n+\n+    \"\"\"\n+    if ('ln_prior' not in samples.tbl.colnames\n+            or 'ln_likelihood' not in samples.tbl.colnames):\n+        raise ValueError(\"You must pass in samples that have prior and \"\n+                         \"likelihood information stored; use return_logprobs=\"\n+                         \"True when generating the samples.\")\n+\n+    ln_post = samples['ln_prior'] + samples['ln_likelihood']\n+    idx = np.argmax(ln_post)\n+\n+    if return_index:\n+        return samples[idx], idx\n+    else:\n+        return samples[idx]\n+\n+\n+def is_P_unimodal(samples, data):\n+    \"\"\"\n+    Check whether the samples returned are within one period mode.\n+\n+    Parameters\n+    ----------\n+    samples : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+\n+    Returns\n+    -------\n+    is_unimodal : bool\n+    \"\"\"\n+\n+    P_samples = samples['P'].to(u.day).value\n+    P_min = np.min(P_samples)\n+    T = np.ptp(data.t.tcb.mjd)\n+    delta = 4*P_min**2 / (2*np.pi*T)\n+\n+    return np.ptp(P_samples) < delta\n+\n+\n+def is_P_Kmodal(samples, data, n_clusters=2):\n+    \"\"\"\n+    Experimental!\n+\n+    Parameters\n+    ----------\n+    samples : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+    n_clusters : int (optional)\n+\n+    Returns\n+    -------\n+    is_Kmodal : bool\n+    mode_means : array\n+    n_per_mode : array\n+\n+    \"\"\"\n+    from sklearn.cluster import KMeans\n+    clf = KMeans(n_clusters=n_clusters)\n+\n+    lnP = np.log(samples['P'].value).reshape(-1, 1)\n+    y = clf.fit_predict(lnP)\n+\n+    unimodals = []\n+    means = []\n+    n_per_mode = []\n+    for j in np.unique(y):\n+        sub_samples = samples[y == j]\n+        if len(sub_samples) == 1:\n+            unimodals.append(True)\n+            means.append(sub_samples)\n+        else:\n+            unimodals.append(is_P_unimodal(sub_samples, data))\n+            means.append(MAP_sample(sub_samples)['P'])\n+        n_per_mode.append((y == j).sum())\n+\n+    return all(unimodals), u.Quantity(means), np.array(n_per_mode)\n+\n+\n+def max_phase_gap(sample, data):\n+    \"\"\"Based on the MPG statistic defined here:\n+    https://gea.esac.esa.int/archive/documentation/GDR2/Data_analysis/chap_cu7var/sssec_cu7var_validation_sos_sl/ssec_cu7var_sos_sl_qa.html\n+\n+    Parameters\n+    ----------\n+    sample : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+    \"\"\"\n+    phase = np.sort(data.phase(sample['P']))\n+    phase = np.concatenate((phase, phase))\n+    return (phase[1:] - phase[:-1]).max()\n+\n+\n+def phase_coverage(sample, data, n_bins=10):\n+    \"\"\"Based on the PC statistic defined here:\n+    https://gea.esac.esa.int/archive/documentation/GDR2/Data_analysis/chap_cu7var/sssec_cu7var_validation_sos_sl/ssec_cu7var_sos_sl_qa.html\n+\n+    Parameters\n+    ----------\n+    sample : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+    \"\"\"\n+    P = sample['P']\n+    H, _ = np.histogram(data.phase(P),\n+                        bins=np.linspace(0, 1, n_bins+1))\n+    return (H > 0).sum() / n_bins\n+\n+\n+def periods_spanned(sample, data):\n+    \"\"\"Compute the number of periods spanned by the data\n+\n+    Parameters\n+    ----------\n+    sample : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+    \"\"\"\n+    P = sample['P']\n+    T = data.t.jd.max() - data.t.jd.min()\n+    return T / P.to_value(u.day)\n+\n+\n+def phase_coverage_per_period(sample, data):\n+    \"\"\"The maximum number of data points within a period.\n+\n+    Parameters\n+    ----------\n+    sample : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+    \"\"\"\n+    P = sample['P']\n+    dt = (data.t - data.t_ref).to(u.day)\n+    phase = dt / P\n+    H1, _ = np.histogram(phase, bins=np.arange(0, phase.max()+1, 1))\n+    H2, _ = np.histogram(phase, bins=np.arange(-0.5, phase.max()+1, 1))\n+    return max(H1.max(), H2.max())\ndiff --git a/thejoker/samples_helpers.py b/thejoker/samples_helpers.py\nindex f303565..e29a344 100644\n--- a/thejoker/samples_helpers.py\n+++ b/thejoker/samples_helpers.py\n@@ -7,30 +7,6 @@ from astropy.table.meta import get_header_from_yaml, get_yaml_from_table\n from astropy.io.misc.hdf5 import _encode_mixins, meta_path\n from astropy.utils.exceptions import AstropyUserWarning\n from astropy.utils import metadata\n-import astropy.units as u\n-import numpy as np\n-\n-\n-def is_P_unimodal(samples, data):\n-    \"\"\"\n-    Check whether the samples returned are within one period mode.\n-\n-    Parameters\n-    ----------\n-    samples : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-\n-    Returns\n-    -------\n-    is_unimodal : bool\n-    \"\"\"\n-\n-    P_samples = samples['P'].to(u.day).value\n-    P_min = np.min(P_samples)\n-    T = np.ptp(data.t.tcb.mjd)\n-    delta = 4*P_min**2 / (2*np.pi*T)\n-\n-    return np.ptp(P_samples) < delta\n \n \n def _custom_tbl_dtype_compare(dtype1, dtype2):\ndiff --git a/thejoker/thejoker.py b/thejoker/thejoker.py\nindex 910664f..5531c5c 100644\n--- a/thejoker/thejoker.py\n+++ b/thejoker/thejoker.py\n@@ -8,7 +8,7 @@ import numpy as np\n # Project\n from .logging import logger\n from .data_helpers import validate_prepare_data\n-from .samples_helpers import is_P_unimodal\n+from .samples_analysis import is_P_unimodal\n from .likelihood_helpers import get_trend_design_matrix\n from .prior_helpers import validate_n_offsets, validate_poly_trend\n from .prior import JokerPrior, _validate_model\n", "bytes": 11993, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/CHANGES.rst b/CHANGES.rst\nindex 810b064..1d77428 100644\n--- a/CHANGES.rst\n+++ b/CHANGES.rst\n@@ -1,5 +1,5 @@\n 1.2 (unreleased)\n-----------------\n+================\n \n - ``TheJoker`` now expects a ``numpy.random.Generator`` instance instead of a\n   ``RandomState`` for controlling random number generation.\n@@ -9,11 +9,8 @@\n \n - Added functionality to compute ``t0``, i.e. the time of phase=0, for samples.\n \n-- Added functions to compute time-sampling statistics for the samples returned\n-  from The Joker (see: ``thejoker.samples_analysis``).\n-\n 1.1 (2020-04-19)\n-----------------\n+================\n \n - Removed ``astropy-helpers`` from the package infrastructure.\n - Switched to using ``tox`` for testing and documentation builds.\ndiff --git a/docs/changes.rst b/docs/changes.rst\ndeleted file mode 100644\nindex 11a246b..0000000\n--- a/docs/changes.rst\n+++ /dev/null\n@@ -1,5 +0,0 @@\n-*********\n-Changelog\n-*********\n-\n-.. include:: ../CHANGES.rst\ndiff --git a/docs/index.rst b/docs/index.rst\nindex 2300afa..cbd171b 100644\n--- a/docs/index.rst\n+++ b/docs/index.rst\n@@ -21,7 +21,7 @@ method and applications.\n     :maxdepth: 1\n \n     install\n-    changes\n+    whats-new\n \n \n Tutorials\ndiff --git a/docs/whats-new.rst b/docs/whats-new.rst\nnew file mode 100644\nindex 0000000..f4c9193\n--- /dev/null\n+++ b/docs/whats-new.rst\n@@ -0,0 +1,38 @@\n+***********\n+What's new?\n+***********\n+\n+v1.0\n+====\n+\n+TODO: API change! and new features...\n+\n+v0.3\n+====\n+\n+* **thejoker now has a new sampling mode: TheJoker.iterative_rejection_sample**.\n+  The default ``rejection_sample()`` method always uses the full prior samples\n+  library passed to it and will be inefficient when the data are noisy or\n+  uninformative. The new ``iterative_rejection_sample()`` method instead accepts\n+  a requested number of samples, and will try to predict how many prior samples\n+  it needs to use in order to return the desired number of posterior samples.\n+\n+* **thejoker now supports sampling over long-term velocity trends**. The\n+  default assumption for this sampler is that all systems are two-body systems.\n+  However, the sampler now supports hierarchical triple systems in some special\n+  cases. When the orbit of the outer body in a triple system is much longer than\n+  the orbit of the inner binary, and the outer orbital period is not fully\n+  observed, the influence of the outer body can be described by a long-term\n+  trend in the radial velocity that is approximately polynomial in time.\n+  ``thejoker`` now supports sampling over polynomial velocity trends with a\n+  specified polynomial degree. The default still assumes a constant velocity\n+  offset, i.e. that the barycentric velocity of the binary is constant.\n+\n+* **The sampler is ~4–10 times faster**. Thanks to some improvements on the\n+  implementation of the sampler (now in C + Cython), the sampler is\n+  significantly faster than the initial release.\n+\n+* **The sampler now optionally returns the prior and likelihood values of all\n+  posterior samples**. By specifying ``return_logprobs=True`` when running\n+  ``rejection_sample()`` or ``iterative_rejection_sample()``, the sampler now\n+  returns log-prior and log-likelihood values for any returned samples.\ndiff --git a/thejoker/__init__.py b/thejoker/__init__.py\nindex 93cdd8c..9eb0fcd 100644\n--- a/thejoker/__init__.py\n+++ b/thejoker/__init__.py\n@@ -3,12 +3,11 @@ from ._astropy_init import *  # noqa\n \n # For egg_info test builds to pass, put package imports here.\n if not _ASTROPY_SETUP_:  # noqa\n-    from .thejoker import TheJoker  # noqa\n-    from .data import RVData  # noqa\n-    from .samples import JokerSamples  # noqa\n-    from .prior import JokerPrior  # noqa\n-    from .plot import plot_rv_curves, plot_phase_fold  # noqa\n-    from .samples_analysis import *  # noqa\n+    from .thejoker import TheJoker\n+    from .data import RVData\n+    from .samples import JokerSamples\n+    from .prior import JokerPrior\n+    from .plot import *\n \n \n __bibtex__ = __citation__ = \"\"\"@ARTICLE{thejoker,\n@@ -34,3 +33,12 @@ archivePrefix = {arXiv},\n       adsnote = {Provided by the SAO/NASA Astrophysics Data System}\n }\n \"\"\"\n+\n+\n+__all__ = [\n+    'TheJoker',\n+    'RVData',\n+    'JokerSamples',\n+    'JokerPrior',\n+    'plot_rv_curves'\n+]\ndiff --git a/thejoker/likelihood_helpers.py b/thejoker/likelihood_helpers.py\nindex 750d625..d270761 100644\n--- a/thejoker/likelihood_helpers.py\n+++ b/thejoker/likelihood_helpers.py\n@@ -210,7 +210,3 @@ def iterative_rejection_inmem(joker_helper, prior_samples_batch, random_state,\n         samples['ln_likelihood'] = all_marg_lls[good_samples_idx]\n \n     return samples\n-\n-\n-def ln_normal(x, mu, var):\n-    return -0.5 * (np.log(2*np.pi * var) + (x - mu)**2 / var)\ndiff --git a/thejoker/samples.py b/thejoker/samples.py\nindex a99e467..599d671 100644\n--- a/thejoker/samples.py\n+++ b/thejoker/samples.py\n@@ -21,7 +21,6 @@ from .prior_helpers import (validate_poly_trend, validate_n_offsets,\n                             get_v0_offsets_equiv_units)\n from .samples_helpers import write_table_hdf5\n from .exceptions import TheJokerDeprecationWarning\n-from .likelihood_helpers import ln_normal\n \n __all__ = ['JokerSamples']\n \n@@ -528,26 +527,3 @@ class JokerSamples:\n     def copy(self):\n         \"\"\"Return a copy of this instance\"\"\"\n         return self.__class__(self.tbl.copy(), t_ref=self.t_ref)\n-\n-    def ln_unmarginalized_likelihood(self, data):\n-        \"\"\"\n-        Compute the log (unmarginalized) likelihood of the data for each sample\n-        \"\"\"\n-\n-        data_rv = data.rv.value\n-        data_unit = data.rv.unit\n-        data_var = (data.rv_err.to_value(data_unit)) ** 2\n-\n-        if 's' in self.tbl.colnames:\n-            s_vars = self['s'].to_value(data_unit) ** 2\n-        else:\n-            s_vars = np.zeros(len(self))\n-\n-        lls = np.full(len(self), np.nan)\n-        for i, (orbit, s) in enumerate(zip(self.orbits, s_vars)):\n-            model_rv = orbit.radial_velocity(data.t)\n-            lls[i] = ln_normal(model_rv.to_value(data_unit),\n-                               data_rv,\n-                               data_var + s).sum()\n-\n-        return lls\ndiff --git a/thejoker/samples_analysis.py b/thejoker/samples_analysis.py\ndeleted file mode 100644\nindex b1b672f..0000000\n--- a/thejoker/samples_analysis.py\n+++ /dev/null\n@@ -1,151 +0,0 @@\n-# Standard library\n-\n-# Third-party\n-import astropy.units as u\n-import numpy as np\n-\n-__all__ = ['MAP_sample', 'is_P_unimodal', 'is_P_Kmodal',\n-           'max_phase_gap', 'phase_coverage', 'periods_spanned',\n-           'phase_coverage_per_period']\n-\n-\n-def MAP_sample(samples, return_index=False):\n-    \"\"\"Return the maximum a posteriori sample.\n-\n-    Parameters\n-    ----------\n-    samples : `~thejoker.JokerSamples`\n-\n-    \"\"\"\n-    if ('ln_prior' not in samples.tbl.colnames\n-            or 'ln_likelihood' not in samples.tbl.colnames):\n-        raise ValueError(\"You must pass in samples that have prior and \"\n-                         \"likelihood information stored; use return_logprobs=\"\n-                         \"True when generating the samples.\")\n-\n-    ln_post = samples['ln_prior'] + samples['ln_likelihood']\n-    idx = np.argmax(ln_post)\n-\n-    if return_index:\n-        return samples[idx], idx\n-    else:\n-        return samples[idx]\n-\n-\n-def is_P_unimodal(samples, data):\n-    \"\"\"\n-    Check whether the samples returned are within one period mode.\n-\n-    Parameters\n-    ----------\n-    samples : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-\n-    Returns\n-    -------\n-    is_unimodal : bool\n-    \"\"\"\n-\n-    P_samples = samples['P'].to(u.day).value\n-    P_min = np.min(P_samples)\n-    T = np.ptp(data.t.tcb.mjd)\n-    delta = 4*P_min**2 / (2*np.pi*T)\n-\n-    return np.ptp(P_samples) < delta\n-\n-\n-def is_P_Kmodal(samples, data, n_clusters=2):\n-    \"\"\"\n-    Experimental!\n-\n-    Parameters\n-    ----------\n-    samples : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-    n_clusters : int (optional)\n-\n-    Returns\n-    -------\n-    is_Kmodal : bool\n-    mode_means : array\n-    n_per_mode : array\n-\n-    \"\"\"\n-    from sklearn.cluster import KMeans\n-    clf = KMeans(n_clusters=n_clusters)\n-\n-    lnP = np.log(samples['P'].value).reshape(-1, 1)\n-    y = clf.fit_predict(lnP)\n-\n-    unimodals = []\n-    means = []\n-    n_per_mode = []\n-    for j in np.unique(y):\n-        sub_samples = samples[y == j]\n-        if len(sub_samples) == 1:\n-            unimodals.append(True)\n-            means.append(sub_samples)\n-        else:\n-            unimodals.append(is_P_unimodal(sub_samples, data))\n-            means.append(MAP_sample(sub_samples)['P'])\n-        n_per_mode.append((y == j).sum())\n-\n-    return all(unimodals), u.Quantity(means), np.array(n_per_mode)\n-\n-\n-def max_phase_gap(sample, data):\n-    \"\"\"Based on the MPG statistic defined here:\n-    https://gea.esac.esa.int/archive/documentation/GDR2/Data_analysis/chap_cu7var/sssec_cu7var_validation_sos_sl/ssec_cu7var_sos_sl_qa.html\n-\n-    Parameters\n-    ----------\n-    sample : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-    \"\"\"\n-    phase = np.sort(data.phase(sample['P']))\n-    phase = np.concatenate((phase, phase))\n-    return (phase[1:] - phase[:-1]).max()\n-\n-\n-def phase_coverage(sample, data, n_bins=10):\n-    \"\"\"Based on the PC statistic defined here:\n-    https://gea.esac.esa.int/archive/documentation/GDR2/Data_analysis/chap_cu7var/sssec_cu7var_validation_sos_sl/ssec_cu7var_sos_sl_qa.html\n-\n-    Parameters\n-    ----------\n-    sample : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-    \"\"\"\n-    P = sample['P']\n-    H, _ = np.histogram(data.phase(P),\n-                        bins=np.linspace(0, 1, n_bins+1))\n-    return (H > 0).sum() / n_bins\n-\n-\n-def periods_spanned(sample, data):\n-    \"\"\"Compute the number of periods spanned by the data\n-\n-    Parameters\n-    ----------\n-    sample : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-    \"\"\"\n-    P = sample['P']\n-    T = data.t.jd.max() - data.t.jd.min()\n-    return T / P.to_value(u.day)\n-\n-\n-def phase_coverage_per_period(sample, data):\n-    \"\"\"The maximum number of data points within a period.\n-\n-    Parameters\n-    ----------\n-    sample : `~thejoker.JokerSamples`\n-    data : `~thejoker.RVData`\n-    \"\"\"\n-    P = sample['P']\n-    dt = (data.t - data.t_ref).to(u.day)\n-    phase = dt / P\n-    H1, _ = np.histogram(phase, bins=np.arange(0, phase.max()+1, 1))\n-    H2, _ = np.histogram(phase, bins=np.arange(-0.5, phase.max()+1, 1))\n-    return max(H1.max(), H2.max())\ndiff --git a/thejoker/samples_helpers.py b/thejoker/samples_helpers.py\nindex e29a344..f303565 100644\n--- a/thejoker/samples_helpers.py\n+++ b/thejoker/samples_helpers.py\n@@ -7,6 +7,30 @@ from astropy.table.meta import get_header_from_yaml, get_yaml_from_table\n from astropy.io.misc.hdf5 import _encode_mixins, meta_path\n from astropy.utils.exceptions import AstropyUserWarning\n from astropy.utils import metadata\n+import astropy.units as u\n+import numpy as np\n+\n+\n+def is_P_unimodal(samples, data):\n+    \"\"\"\n+    Check whether the samples returned are within one period mode.\n+\n+    Parameters\n+    ----------\n+    samples : `~thejoker.JokerSamples`\n+    data : `~thejoker.RVData`\n+\n+    Returns\n+    -------\n+    is_unimodal : bool\n+    \"\"\"\n+\n+    P_samples = samples['P'].to(u.day).value\n+    P_min = np.min(P_samples)\n+    T = np.ptp(data.t.tcb.mjd)\n+    delta = 4*P_min**2 / (2*np.pi*T)\n+\n+    return np.ptp(P_samples) < delta\n \n \n def _custom_tbl_dtype_compare(dtype1, dtype2):\ndiff --git a/thejoker/tests/test_sampler.py b/thejoker/tests/test_sampler.py\nindex f1b2452..af3fa4c 100644\n--- a/thejoker/tests/test_sampler.py\n+++ b/thejoker/tests/test_sampler.py\n@@ -141,11 +141,6 @@ def test_rejection_sample(tmpdir, prior):\n         samples = joker.rejection_sample(flat_data, _samples)\n         assert len(samples) > 10  # HACK: this should generally be true...\n \n-        # Probably the wrong place to do this, but check that computing the log\n-        # unmarginalized likelihood works:\n-        lls = samples.ln_unmarginalized_likelihood(flat_data)\n-        assert np.isfinite(lls).all()\n-\n     samples, lls = joker.rejection_sample(flat_data, prior_samples,\n                                           return_all_logprobs=True)\n     assert len(lls) == len(prior_samples)\ndiff --git a/thejoker/tests/test_samples.py b/thejoker/tests/test_samples.py\nindex c303966..61cacda 100644\n--- a/thejoker/tests/test_samples.py\n+++ b/thejoker/tests/test_samples.py\n@@ -8,7 +8,7 @@ import pytest\n \n # Project\n from ..samples import JokerSamples\n-from ..samples_helpers import _custom_tbl_dtype_compare\n+from ..samples_helpers import _custom_tbl_dtype_compare, is_P_unimodal\n \n \n def test_joker_samples(tmpdir):\ndiff --git a/thejoker/tests/test_samples_analysis.py b/thejoker/tests/test_samples_analysis.py\ndeleted file mode 100644\nindex 4a13c58..0000000\n--- a/thejoker/tests/test_samples_analysis.py\n+++ /dev/null\n@@ -1,101 +0,0 @@\n-# Third-party\n-import astropy.units as u\n-import numpy as np\n-\n-# Project\n-from ..data import RVData\n-from ..samples import JokerSamples\n-from ..samples_analysis import (\n-    MAP_sample, is_P_unimodal, is_P_Kmodal,\n-    max_phase_gap, phase_coverage, periods_spanned)\n-\n-\n-def test_MAP_sample():\n-    samples = JokerSamples()\n-    samples['ln_likelihood'] = np.array([1., 2, 3.])\n-    samples['ln_prior'] = np.array([1., 2, 3.])\n-\n-    sample = MAP_sample(samples)\n-    assert sample['ln_likelihood'] == 3.\n-\n-    sample, i = MAP_sample(samples, return_index=True)\n-    assert i == 2\n-\n-\n-def test_is_P_unimodal():\n-    np.random.seed(42)\n-\n-    samples = JokerSamples()\n-    samples['P'] = np.random.normal(8, 1e-3, size=32) * u.day\n-\n-    size = 8\n-    t = 56000 + np.random.uniform(0, 100, size=size)\n-    rv = np.random.normal(0, 10, size=size) * u.km/u.s\n-    rv_err = np.random.uniform(0.1, 0.3, size=size) * u.km/u.s\n-    data = RVData(t=t, rv=rv, rv_err=rv_err)\n-\n-    assert is_P_unimodal(samples, data)\n-\n-\n-def test_is_P_Kmodal():\n-    np.random.seed(42)\n-\n-    samples = JokerSamples()\n-    samples['P'] = np.concatenate((np.random.normal(8, 1e-3, size=32),\n-                                   np.random.normal(21, 1e-3, size=32))) * u.day\n-    samples['ln_likelihood'] = np.random.uniform(size=len(samples['P']))\n-    samples['ln_prior'] = np.random.uniform(size=len(samples['P']))\n-\n-    size = 8\n-    t = 56000 + np.random.uniform(0, 100, size=size)\n-    rv = np.random.normal(0, 10, size=size) * u.km/u.s\n-    rv_err = np.random.uniform(0.1, 0.3, size=size) * u.km/u.s\n-    data = RVData(t=t, rv=rv, rv_err=rv_err)\n-\n-    Kmodal, means, npermode = is_P_Kmodal(samples, data, n_clusters=2)\n-    assert Kmodal\n-    assert np.min(np.abs(means - 8.*u.day)) < 0.1*u.day\n-    assert np.min(np.abs(means - 21.*u.day)) < 0.1*u.day\n-    assert np.all(npermode == 32)\n-\n-\n-def test_max_phase_gap():\n-    samples = JokerSamples()\n-    samples['P'] = 6.243 * np.ones(8) * u.day\n-\n-    phase = np.array([0, 0.1, 0.7, 0.8, 0.9])\n-    t = 56000 + phase * samples['P'][0].value\n-    rv = np.random.normal(0, 10, size=len(phase)) * u.km/u.s\n-    rv_err = np.random.uniform(0.1, 0.3, size=len(phase)) * u.km/u.s\n-    data = RVData(t=t, rv=rv, rv_err=rv_err)\n-\n-    assert np.isclose(max_phase_gap(samples[0], data), 0.6, atol=1e-5)\n-\n-\n-def test_phase_coverage():\n-    samples = JokerSamples()\n-    samples['P'] = 6.243 * np.ones(8) * u.day\n-\n-    phase = np.array([0, 0.1, 0.7, 0.8, 0.9])\n-    t = 56000 + phase * samples['P'][0].value\n-    rv = np.random.normal(0, 10, size=len(phase)) * u.km/u.s\n-    rv_err = np.random.uniform(0.1, 0.3, size=len(phase)) * u.km/u.s\n-    data = RVData(t=t, rv=rv, rv_err=rv_err)\n-\n-    phasecov = phase_coverage(samples[0], data)\n-    assert phasecov >= 0.4\n-    assert phasecov <= 0.6\n-\n-\n-def test_periods_spanned():\n-    samples = JokerSamples()\n-    samples['P'] = 6.243 * np.ones(8) * u.day\n-\n-    phase = np.linspace(0, 3.3, 16)\n-    t = 56000 + phase * samples['P'][0].value\n-    rv = np.random.normal(0, 10, size=len(phase)) * u.km/u.s\n-    rv_err = np.random.uniform(0.1, 0.3, size=len(phase)) * u.km/u.s\n-    data = RVData(t=t, rv=rv, rv_err=rv_err)\n-\n-    span = periods_spanned(samples[0], data)\n-    assert np.isclose(span, phase[-1], atol=1e-5)\ndiff --git a/thejoker/thejoker.py b/thejoker/thejoker.py\nindex 5531c5c..910664f 100644\n--- a/thejoker/thejoker.py\n+++ b/thejoker/thejoker.py\n@@ -8,7 +8,7 @@ import numpy as np\n # Project\n from .logging import logger\n from .data_helpers import validate_prepare_data\n-from .samples_analysis import is_P_unimodal\n+from .samples_helpers import is_P_unimodal\n from .likelihood_helpers import get_trend_design_matrix\n from .prior_helpers import validate_n_offsets, validate_poly_trend\n from .prior import JokerPrior, _validate_model\n", "bytes": 16734, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    python3-dev \\\n    libffi-dev \\\n    libssl-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone https://github.com/adrn/thejoker.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 9a8511fe76b91b6b0979ae441106895b59f355f0 || git fetch --depth 1 origin \"+refs/pull/110/head:refs/remotes/origin/pr/110\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\nENV CI=true\n\n# Create venv and install build tooling\n# Then add sitecustomize to patch SafeConfigParser for Python 3.12 / theano-pymc 1.1.2 compat\nRUN uv venv /opt/venv && \\\n    uv pip install --python /opt/venv/bin/python \\\n        setuptools setuptools_scm wheel extension-helpers \\\n        \"cython>=3.0\" oldest-supported-numpy twobody && \\\n    printf 'import configparser; configparser.SafeConfigParser = configparser.ConfigParser\\n' \\\n        > /opt/venv/lib/python3.12/site-packages/sitecustomize.py\n\n# Install thejoker with --no-build-isolation (uses newer Cython)\n# sitecustomize.py ensures SafeConfigParser exists for theano-pymc build\nRUN uv pip install --python /opt/venv/bin/python \\\n    --no-build-isolation -e .\n\n# Install runtime + test deps\nRUN uv pip install --python /opt/venv/bin/python \\\n    numpy astropy scipy h5py schwimmbad \\\n    pymc3 exoplanet tables \\\n    scikit-learn \\\n    pytest pytest-astropy pytest-cov \\\n    corner astroML pyyaml ipython jupyter_client\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1901, "truncated": false}]}, {"task_name": "adrn__thejoker-71", "repo": "adrn/thejoker", "language": "Python", "difficulty": "medium", "area": "library", "topic": "mcmc", "bug_class": "emcee", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "Implement MCMC sampling capabilities for the `TheJoker` class using `emcee`. This involves adding a method to perform standard MCMC sampling on the posterior distribution of the orbital parameters.\n\nSpecifically:\n\n1. **Add MCMC Sampling Method**: Add a method to `TheJoker` (e.g., `sample_mcmc` or similar) that uses `emcee` to sample the posterior. It should accept data and configuration options for the sampler (number of steps, walkers, etc.).\n\n2. **MCMC Parameter Translation**: Implement utility methods in `TheJokerMCMCModel` to translate between the internal parameterization (`to_mcmc_params` and `from_mcmc_params`) and the standard MCMC parameter vector. The MCMC parameter vector should include orbital parameters (Period P, Mean Anomaly M0, Eccentricity e, Omega omega) plus amplitude K, systemic velocity v0, and a log-jitter term. Ensure that `to_mcmc_params` and `from_mcmc_params` are inverse operations (round-trip preservation).\n\n3. **Log-Posterior Evaluation**: Implement `ln_prior`, `ln_likelihood`, and `ln_posterior` methods in `TheJokerMCMCModel`. \n   - `ln_prior` should return a finite log-probability for valid parameters.\n   - `ln_likelihood` should return an array of finite log-probabilities per data point.\n   - `ln_posterior` should combine prior and likelihood correctly, accounting for the log-jitter term if present in the MCMC parameters (e.g., removing the jitter index from the MCMC params list before calling `ln_posterior` logic that expects only orbital + amplitude params).\n\n4. **Jitter Handling**: Ensure that the jitter parameter is handled correctly during MCMC sampling. If a fixed jitter is specified in `JokerParams`, it should be used consistently in prior sampling, rejection sampling, and MCMC sampling.\n\n5. **Integration**: The new MCMC functionality should integrate seamlessly with the existing `sample_prior` and `rejection_sample` methods. The `TheJoker` class should validate inputs (params, random_state) with appropriate `TypeError` or `ValueError` exceptions for invalid types, consistent with existing behavior.", "bytes": 2072, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src\n\nexport CI=true\nsource /opt/venv/bin/activate\n\n# Copy HEAD test files from /tests (overwrites BASE state)\nmkdir -p \"thejoker/sampler/tests\"\ncp \"/tests/thejoker/sampler/tests/test_mcmc.py\" \"thejoker/sampler/tests/test_mcmc.py\"\nmkdir -p \"thejoker/sampler/tests\"\ncp \"/tests/thejoker/sampler/tests/test_sampler.py\" \"thejoker/sampler/tests/test_sampler.py\"\n\nuv pip install -e . --no-deps >/dev/null 2>&1 || true\npytest -q \"thejoker/sampler/tests/test_mcmc.py\" \"thejoker/sampler/tests/test_sampler.py\"\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 671, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/scripts/run-sampler.py b/scripts/run-sampler.py\nindex a467a2d..28ecf40 100644\n--- a/scripts/run-sampler.py\n+++ b/scripts/run-sampler.py\n@@ -3,7 +3,6 @@ import os\n import sys\n \n # Third-party\n-from astropy import log as logger\n import astropy.units as u\n import h5py\n import numpy as np\n@@ -11,14 +10,8 @@ import six\n from schwimmbad import choose_pool\n \n # Project\n-from thejoker import Paths\n-paths = Paths()\n from thejoker.data import RVData\n-from thejoker.units import default_units\n-# from thejoker.pool import choose_pool\n-from thejoker.celestialmechanics import OrbitalParams\n-from thejoker.sampler import get_good_samples, samples_to_orbital_params, sample_prior\n-from thejoker import config\n+from thejoker.log import log as logger\n \n def main(data_file, pool, tmp_prior_filename, n_samples=1, seed=42, hdf5_key=None,\n          cache_filename=None, overwrite=False, continue_sampling=False,\ndiff --git a/setup.cfg b/setup.cfg\nindex a1c20b3..59419e7 100644\n--- a/setup.cfg\n+++ b/setup.cfg\n@@ -13,9 +13,10 @@ upload-dir = docs/_build/html\n show-response = 1\n \n [tool:pytest]\n-minversion = 2.2\n+minversion = 3.0\n norecursedirs = build docs/_build\n doctest_plus = enabled\n+addopts = -p no:warnings\n \n [ah_bootstrap]\n auto_use = True\ndiff --git a/thejoker/data.py b/thejoker/data.py\nindex f6c2ae0..29f23cc 100644\n--- a/thejoker/data.py\n+++ b/thejoker/data.py\n@@ -161,7 +161,8 @@ class RVData:\n \n     # ---\n \n-    def plot(self, ax=None, rv_unit=None, time_format='mjd', **kwargs):\n+    def plot(self, ax=None, rv_unit=None, time_format='mjd', phase_fold=None,\n+             **kwargs):\n         \"\"\"\n         Plot the data points.\n \n@@ -179,6 +180,10 @@ class RVData:\n             the `~astropy.time.Time` object, or it can be a callable (e.g.,\n             function) that does more complex things (for example:\n             ``time_format=lambda t: t.datetime.day``).\n+        phase_fold : bool (optional)\n+            Plot the phase instead of the time by folding on the specified\n+            period.\n+        `~astropy.units.Quantity`\n         **kwargs\n             All other keyword arguments are passed to the\n             `~matplotlib.pyplot.errorbar` (if errors were provided) or\ndiff --git a/thejoker/sampler/likelihood.py b/thejoker/sampler/likelihood.py\nindex 3b4d2d3..c9fdb1a 100644\n--- a/thejoker/sampler/likelihood.py\n+++ b/thejoker/sampler/likelihood.py\n@@ -55,7 +55,7 @@ def design_matrix(nonlinear_p, data, joker_params):\n \n     # TODO: right now, we only support a constant N=1 velocity trend!\n     A1 = np.vander(t, N=1, increasing=True)\n-    A = np.hstack((zdot[:,None], A1))\n+    A = np.hstack((zdot[:, None], A1))\n \n     return A\n \ndiff --git a/thejoker/sampler/mcmc.py b/thejoker/sampler/mcmc.py\nindex 8039ebc..f7d8863 100644\n--- a/thejoker/sampler/mcmc.py\n+++ b/thejoker/sampler/mcmc.py\n@@ -2,266 +2,264 @@\n import astropy.units as u\n import numpy as np\n from scipy.stats import beta, norm\n-from twobody.wrap import cy_rv_from_elements\n \n # Project\n-from .likelihood import get_ivar\n+from .likelihood import get_ivar, design_matrix\n+from .params import JokerParams\n+from .samples import JokerSamples\n+from ..data import RVData\n \n-__all__ = ['to_mcmc_params', 'from_mcmc_params',\n-           'pack_samples', 'pack_samples_mcmc', 'unpack_samples',\n-           'unpack_samples_mcmc',\n-           'ln_likelihood', 'ln_prior', 'ln_posterior']\n+__all__ = ['TheJokerMCMCModel']\n \n log_2pi = np.log(2 * np.pi)\n \n \n-def to_mcmc_params(p):\n-    r\"\"\"\n-    MCMC internal function.\n-\n-    Transform from linear orbital parameter values to standard\n-    variables for MCMC sampling:\n-\n-    .. math::\n-\n-        \\ln P \\\\\n-        \\sqrt{K}\\,\\cos\\phi_0, \\sqrt{K}\\,\\sin\\phi_0 \\\\\n-        \\sqrt{e}\\,\\cos\\omega, \\sqrt{e}\\,\\sin\\omega \\\\\n-        \\ln s \\\\\n-        v_0,..., v_n\n-\n-    Parameters\n-    ----------\n-    p : iterable\n-        A packed parameter vector containing the orbital parameters\n-        and long-term velocity trend parameters.\n-\n-    \"\"\"\n-    P, M0, ecc, omega, s, K, *v_terms = p\n-    return np.vstack([np.log(P),\n-                      np.sqrt(K) * np.cos(M0), np.sqrt(K) * np.sin(M0),\n-                      np.sqrt(ecc) * np.cos(omega), np.sqrt(ecc) * np.sin(omega),\n-                      2*np.log(s)] + list(v_terms))\n-\n-\n-def from_mcmc_params(p):\n-    \"\"\"\n-    MCMC internal function.\n-\n-    Transform from the standard MCMC parameters to the linear\n-    values of the orbital parameters.\n-\n-    Parameters\n-    ----------\n-    p : iterable\n-        A packed parameter vector containing the MCMC-transforemd\n-        versions of the orbital parameters and long-term velocity\n-        trend parameters.\n-\n-    \"\"\"\n-    (ln_P,\n-     sqrtK_cos_M0, sqrtK_sin_M0,\n-     sqrte_cos_omega, sqrte_sin_omega,\n-     log_s2, *v_terms) = p\n-\n-    return np.vstack([np.exp(ln_P),\n-                      np.arctan2(sqrtK_sin_M0, sqrtK_cos_M0) % (2*np.pi),\n-                      sqrte_cos_omega**2 + sqrte_sin_omega**2,\n-                      np.arctan2(sqrte_sin_omega, sqrte_cos_omega) % (2*np.pi),\n-                      np.sqrt(np.exp(log_s2)),\n-                      (sqrtK_cos_M0**2 + sqrtK_sin_M0**2)] + v_terms)\n-\n-\n-def pack_samples(samples, params, data):\n-    \"\"\"\n-    Pack a dictionary of samples as Quantity objects into a 2D array.\n-\n-    Parameters\n-    ----------\n-    samples : dict\n-        Dictionary of `~astropy.units.Quantity` objects for period,\n-        M0, etc.\n-    params : `~thejoker.sampler.params.JokerParams`\n-        Object specifying hyper-parameters for The Joker.\n-    data : `~thejoker.data.RVData`\n-        The radial velocity data.\n-\n-    Returns\n-    -------\n-    arr : `numpy.ndarray`\n-        A 2D numpy array with shape `(nsamples, ndim)`.\n-    \"\"\"\n-    if 'jitter' in samples:\n-        jitter = samples['jitter'].to(data.rv.unit).value\n-    else:\n-        jitter = np.zeros_like(samples['P'].value)\n-\n-    arr = [samples['P'].to(u.day).value,\n-           samples['M0'].to(u.radian).value,\n-           np.asarray(samples['e']),\n-           samples['omega'].to(u.radian).value,\n-           jitter,\n-           samples['K'].to(data.rv.unit).value,\n-           samples['v0'].to(data.rv.unit).value]\n-    # TODO: assumes only constant velocity offset\n-\n-    return np.array(arr).T\n-\n-\n-def pack_samples_mcmc(samples, params, data):\n-    \"\"\"\n-    Pack a dictionary of samples as Quantity objects into a 2D array,\n-    transformed to the parametrization used by the MCMC functions.\n-\n-    Parameters\n-    ----------\n-    samples : dict\n-        Dictionary of `~astropy.units.Quantity` objects for period,\n-        M0, etc.\n-    params : `~thejoker.sampler.params.JokerParams`\n-        Object specifying hyper-parameters for The Joker.\n-    data : `~thejoker.data.RVData`\n-        The radial velocity data.\n-\n-    Returns\n-    -------\n-    arr : `numpy.ndarray`\n-        A 2D numpy array with shape `(nsamples, ndim)`.\n-    \"\"\"\n-    samples_vec = pack_samples(samples, params, data)\n-    samples_mcmc = to_mcmc_params(samples_vec.T)\n-\n-    if params._fixed_jitter:\n-        samples_mcmc = np.delete(samples_mcmc, 5, axis=0)\n-\n-    return np.array(samples_mcmc).T\n-\n-\n-def unpack_samples(samples_arr, params, data):\n-    \"\"\"\n-    Unpack a 2D array of samples into a dictionary of samples as Quantity objects.\n-\n-    Parameters\n-    ----------\n-    samples_arr : `numpy.ndarray`\n-        A 2D numpy array with shape `(nsamples, ndim)` containing the values.\n-    params : `~thejoker.sampler.params.JokerParams`\n-        Object specifying hyper-parameters for The Joker.\n-    data : `~thejoker.data.RVData`\n-        The radial velocity data.\n-\n-    Returns\n-    -------\n-    samples : dict\n-        Dictionary of `~astropy.units.Quantity` objects for period,\n-        M0, etc.\n-    \"\"\"\n-    samples = dict()\n-    samples['P'] = samples_arr.T[0] * u.day\n-    samples['M0'] = samples_arr.T[1] * u.radian\n-    samples['e'] = samples_arr.T[2] * u.one\n-    samples['omega'] = samples_arr.T[3] * u.radian\n-\n-    if not params._fixed_jitter:\n-        samples['jitter'] = samples_arr.T[4] * data.rv.unit\n-        shift = 1\n-    else:\n-        samples['jitter'] = np.zeros_like(samples_arr.T[0]) * data.rv.unit\n-        shift = 0\n-\n-    samples['K'] = samples_arr.T[4+shift] * data.rv.unit\n-\n-    # TODO: assumes only constant velocity offset\n-    samples['v0'] = samples_arr.T[5+shift] * data.rv.unit\n-\n-    return samples\n-\n-\n-def unpack_samples_mcmc(samples_arr, params, data):\n-    \"\"\"\n-    Unpack a 2D array of samples transformed to the parametrization used by the\n-    MCMC functions into a dictionary of samples as Quantity objects in the\n-    standard parametrization (i.e. period, angles, ..).\n-\n-    Parameters\n-    ----------\n-    samples_arr : `numpy.ndarray`\n-        A 2D numpy array with shape `(nsamples, ndim)` containing the\n-        values in the MCMC coordinates.\n-    params : `~thejoker.sampler.params.JokerParams`\n-        Object specifying hyper-parameters for The Joker.\n-    data : `~thejoker.data.RVData`\n-        The radial velocity data.\n-\n-    Returns\n-    -------\n-    samples : dict\n-        Dictionary of `~astropy.units.Quantity` objects for period,\n-        M0, etc.\n-    \"\"\"\n-    samples_arr = from_mcmc_params(samples_arr.T).T\n-    return unpack_samples(samples_arr, params, data)\n-\n-\n-def ln_likelihood(p, joker_params, data):\n-    P, M0, ecc, omega, s, K, *v_terms = p\n-\n-    # a little repeated code here...\n-\n-    t = data._t_bmjd\n-    zdot = cy_rv_from_elements(t, P, 1., ecc,\n-                               omega, M0, data._t0_bmjd,\n-                               joker_params.anomaly_tol,\n-                               joker_params.anomaly_maxiter)\n-\n-    # TODO: right now, we only support a constant systemic velocity\n-    A1 = np.vander(t, N=1, increasing=True)\n-    A = np.hstack((zdot[:,None], A1))\n-    p = np.array([K] + v_terms)\n-    ivar = get_ivar(data, s)\n-\n-    dy = A.dot(p) - data.rv.value\n-\n-    return 0.5 * (-dy**2 * ivar - log_2pi + np.log(ivar))\n-\n-\n-def ln_prior(p, joker_params):\n-    P, M0, ecc, omega, s, K, *v_terms = p\n-\n-    lnp = 0.\n-\n-    # TODO: more repeated code here and hard-coded priors\n-    if ecc < 0 or ecc > 1:\n-        return -np.inf\n-\n-    lnp += beta.logpdf(ecc, 0.867, 3.03) # Kipping et al. 2013\n-\n-    # TODO: do we need P_min, P_max here?\n-\n-    if not joker_params._fixed_jitter:\n-        # DFM's idea: wide, Gaussian prior in log(s^2)\n-        # lnp += norm.logpdf(np.log(s), ) # TODO: put in hyper-parameters\n-        # TODO:\n-        pass\n-\n-    return lnp\n+class TheJokerMCMCModel:\n+\n+    def __init__(self, joker_params, data):\n+        \"\"\"\n+\n+        Parameters\n+        ----------\n+        data : `~thejoker.data.RVData`\n+            The radial velocity data.\n+        \"\"\"\n+\n+        # check if a JokerParams instance was passed in to specify the state\n+        if not isinstance(joker_params, JokerParams):\n+            raise TypeError(\"Parameter specification must be a JokerParams \"\n+                            \"instance, not a '{0}'\".format(type(joker_params)))\n+        self.params = joker_params\n+\n+        if not isinstance(data, RVData):\n+            raise TypeError(\"Data must be a valid RVData object.\")\n+        self.data = data\n+\n+        # various cached things:\n+        self._P_min = self.params.P_min.to(u.day).value\n+        self._P_max = self.params.P_max.to(u.day).value\n+        self._rv_unit = self.data.rv.unit\n+        self._jitter_factor = self._rv_unit.to(self.params._jitter_unit)\n+\n+        if self.params._fixed_jitter:\n+            s = self.params.jitter\n+            self._y_jitter = 2 * np.log(s.to(self._rv_unit).value)\n+\n+    @classmethod\n+    def to_mcmc_params(cls, p):\n+        r\"\"\"MCMC internal function.\n+\n+        Transform from linear orbital parameter values to standard\n+        variables for MCMC sampling:\n+\n+        .. math::\n+\n+            \\ln P \\\\\n+            \\sqrt{K}\\,\\cos\\phi_0, \\sqrt{K}\\,\\sin\\phi_0 \\\\\n+            \\sqrt{e}\\,\\cos\\omega, \\sqrt{e}\\,\\sin\\omega \\\\\n+            \\ln s^2 \\\\\n+            v_0,..., v_n\n+\n+        Parameters\n+        ----------\n+        p : iterable\n+            A packed parameter vector containing the orbital parameters\n+            and long-term velocity trend parameters.\n+\n+        \"\"\"\n+        P, M0, e, omega, s, K, *v_terms = p\n+        return np.vstack([np.log(P),\n+                          np.sqrt(K) * np.cos(M0), np.sqrt(K) * np.sin(M0),\n+                          np.sqrt(e) * np.cos(omega), np.sqrt(e) * np.sin(omega),\n+                          2*np.log(s)] + list(v_terms))\n+\n+    @classmethod\n+    def from_mcmc_params(cls, p):\n+        \"\"\"MCMC internal function.\n+\n+        Transform from the standard MCMC parameters to the linear\n+        values of the orbital parameters.\n+\n+        Parameters\n+        ----------\n+        p : iterable\n+            A packed parameter vector containing the MCMC-transforemd\n+            versions of the orbital parameters and long-term velocity\n+            trend parameters.\n+\n+        \"\"\"\n+        (ln_P,\n+         sqrtK_cos_M0, sqrtK_sin_M0,\n+         sqrte_cos_omega, sqrte_sin_omega,\n+         log_s2, *v_terms) = p\n+\n+        return np.vstack([np.exp(ln_P),\n+                          np.arctan2(sqrtK_sin_M0, sqrtK_cos_M0) % (2*np.pi),\n+                          sqrte_cos_omega**2 + sqrte_sin_omega**2,\n+                          np.arctan2(sqrte_sin_omega, sqrte_cos_omega) % (2*np.pi),\n+                          np.sqrt(np.exp(log_s2)),\n+                          (sqrtK_cos_M0**2 + sqrtK_sin_M0**2)] + v_terms)\n+\n+    def pack_samples(self, samples):\n+        \"\"\"Pack a dictionary of samples as Quantity objects into a 2D array.\n+\n+        Parameters\n+        ----------\n+        samples : dict\n+            Dictionary of `~astropy.units.Quantity` objects for period,\n+            M0, etc.\n+\n+        Returns\n+        -------\n+        arr : `numpy.ndarray`\n+            A 2D numpy array with shape `(nsamples, ndim)`.\n+        \"\"\"\n+        if 'jitter' in samples:\n+            jitter = samples['jitter'].to(self._rv_unit).value\n+        else:\n+            jitter = np.zeros_like(samples['P'].value)\n+\n+        arr = [samples['P'].to(u.day).value,\n+               samples['M0'].to(u.radian).value,\n+               np.asarray(samples['e']),\n+               samples['omega'].to(u.radian).value,\n+               jitter,\n+               samples['K'].to(self._rv_unit).value,\n+               samples['v0'].to(self._rv_unit).value]\n+        # TODO: assumes only constant velocity offset\n+\n+        return np.array(arr).T\n+\n+    def pack_samples_mcmc(self, samples):\n+        \"\"\"Pack a dictionary of samples as Quantity objects into a 2D array,\n+        transformed to the parametrization used by the MCMC functions.\n+\n+        Parameters\n+        ----------\n+        samples : dict\n+            Dictionary of `~astropy.units.Quantity` objects for period,\n+            M0, etc.\n+\n+        Returns\n+        -------\n+        arr : `numpy.ndarray`\n+            A 2D numpy array with shape `(nsamples, ndim)`.\n+        \"\"\"\n+        samples_vec = self.pack_samples(samples, self.params)\n+        samples_mcmc = self.to_mcmc_params(samples_vec.T)\n+\n+        if self.params._fixed_jitter:\n+            samples_mcmc = np.delete(samples_mcmc, 5, axis=0)\n+\n+        return np.array(samples_mcmc).T\n+\n+    def unpack_samples(self, samples_arr):\n+        \"\"\"Unpack a 2D array of samples into a dictionary of samples as\n+        Quantity objects.\n+\n+        Parameters\n+        ----------\n+        samples_arr : `numpy.ndarray`\n+            A 2D numpy array with shape `(nsamples, ndim)` containing the values.\n+\n+        Returns\n+        -------\n+        samples : dict\n+            Dictionary of `~astropy.units.Quantity` objects for period,\n+            M0, etc.\n+        \"\"\"\n+        samples = JokerSamples()\n+        samples['P'] = samples_arr.T[0] * u.day\n+        samples['M0'] = samples_arr.T[1] * u.radian\n+        samples['e'] = samples_arr.T[2] * u.one\n+        samples['omega'] = samples_arr.T[3] * u.radian\n+\n+        if not self.params._fixed_jitter:\n+            samples['jitter'] = samples_arr.T[4] * self._rv_unit\n+            shift = 1\n+        else:\n+            samples['jitter'] = np.zeros_like(samples_arr.T[0]) * self._rv_unit\n+            shift = 0\n+\n+        samples['K'] = samples_arr.T[4+shift] * self._rv_unit\n+\n+        # TODO: assumes only constant velocity offset\n+        samples['v0'] = samples_arr.T[5+shift] * self._rv_unit\n+\n+        return samples\n+\n+    def unpack_samples_mcmc(self, samples_arr):\n+        \"\"\"Unpack a 2D array of samples transformed to the parametrization used\n+        by the MCMC functions into a dictionary of samples as Quantity objects\n+        in the standard parametrization (i.e. period, angles, ...).\n+\n+        Parameters\n+        ----------\n+        samples_arr : `numpy.ndarray`\n+            A 2D numpy array with shape `(nsamples, ndim)` containing the\n+            values in the MCMC coordinates.\n+\n+        Returns\n+        -------\n+        samples : `thejoker.JokerSamples`\n+        \"\"\"\n+        samples_arr = self.from_mcmc_params(samples_arr.T).T\n+        return self.unpack_samples(samples_arr)\n+\n+    def ln_likelihood(self, p):\n+        P, M0, ecc, omega, s, K, *v_terms = p\n+\n+        # a little repeated code here...\n+\n+        A = design_matrix([P, M0, ecc, omega], self.data, self.params)\n+        p2 = np.array([K] + v_terms)\n+        ivar = get_ivar(self.data, s)\n+        dy = A.dot(p2) - self.data.rv.value\n+\n+        return 0.5 * (-dy**2 * ivar - log_2pi + np.log(ivar))\n+\n+    def ln_prior(self, p):\n+        # TODO: repeated code here and hard-coded priors\n+\n+        P, M0, ecc, omega, s, K, *v_terms = p\n+\n+        lnp = 0.\n+\n+        if ecc < 0 or ecc > 1:\n+            return -np.inf\n+\n+        lnp += beta.logpdf(ecc, 0.867, 3.03) # Kipping et al. 2013\n+\n+        # uniform in ln(P) - we don't need the jacobian because we sample in lnP\n+        if P < self._P_min or P > self._P_max:\n+            return -np.inf\n+        # lnp += -np.log(P)\n+\n+        if not self.params._fixed_jitter:\n+            # Gaussian prior in ln(s^2) - don't need Jacobian because we are\n+            # actually sampling in y = ln(s^2)\n+            s_scaled = s * self._jitter_factor\n+            y = 2 * np.log(s_scaled)\n+            lnp += norm.logpdf(y, loc=self.params.jitter[0],\n+                               scale=self.params.jitter[1])\n \n+        return lnp\n \n-def ln_posterior(mcmc_p, joker_params, data):\n-    if joker_params._fixed_jitter:\n-        mcmc_p = list(mcmc_p)\n-        mcmc_p.insert(5, -np.inf) # HACK: whoa, major hackage!\n+    def ln_posterior(self, mcmc_p):\n+        if self.params._fixed_jitter:\n+            mcmc_p = list(mcmc_p)\n+            mcmc_p.insert(5, self._y_jitter) # whoa, major hackage!\n \n-    p = from_mcmc_params(mcmc_p).reshape(len(mcmc_p))\n+        p = self.from_mcmc_params(mcmc_p).reshape(len(mcmc_p))\n \n-    lnp = ln_prior(p, joker_params)\n-    if np.isinf(lnp):\n-        return lnp\n+        lnp = self.ln_prior(p)\n+        if np.isinf(lnp):\n+            return lnp\n \n-    lnl = ln_likelihood(p, joker_params, data)\n-    lnprob = lnp + lnl.sum()\n+        lnl = self.ln_likelihood(p)\n+        lnprob = lnp + lnl.sum()\n \n-    if np.isnan(lnprob):\n-        return -np.inf\n+        if np.isnan(lnprob):\n+            return -np.inf\n \n-    return lnprob\n+        return lnprob\ndiff --git a/thejoker/sampler/params.py b/thejoker/sampler/params.py\nindex 3a1d7f1..204c676 100644\n--- a/thejoker/sampler/params.py\n+++ b/thejoker/sampler/params.py\n@@ -86,6 +86,7 @@ class JokerParams(object):\n \n         else:\n             self._fixed_jitter = True\n+            self._jitter_unit = jitter.unit\n             self.jitter = jitter\n \n     @property\ndiff --git a/thejoker/sampler/sampler.py b/thejoker/sampler/sampler.py\nindex b62535f..f4453c0 100644\n--- a/thejoker/sampler/sampler.py\n+++ b/thejoker/sampler/sampler.py\n@@ -17,6 +17,7 @@ from .multiproc_helpers import (get_good_sample_indices, compute_likelihoods,\n                                 sample_indices_to_full_samples)\n from .io import save_prior_samples\n from .samples import JokerSamples\n+from .mcmc import TheJokerMCMCModel\n \n __all__ = ['TheJoker']\n \n@@ -41,6 +42,7 @@ class TheJoker(object):\n         sample caches, you may need to set this to a larger number (e.g.,\n         ``100*pool.size``) to avoid memory issues.\n     \"\"\"\n+\n     def __init__(self, params, pool=None, random_state=None, n_batches=None):\n \n         # set the processing pool\n@@ -77,17 +79,6 @@ class TheJoker(object):\n \n         self.n_batches = n_batches\n \n-        # Used for caching\n-        self._cache = dict()\n-\n-        # Files to be closed when _cleanup() is called\n-        self._cache['tempfiles'] = []\n-\n-    def _cleanup(self):\n-        # Close any temp. files:\n-        for f in self._cache['tempfiles']:\n-            f.close()\n-\n     def sample_prior(self, size=1, return_logprobs=False):\n         \"\"\"Generate samples from the prior. Logarithmic in period, uniform in\n         phase and argument of pericenter, Beta distribution in eccentricity.\n@@ -147,13 +138,14 @@ class TheJoker(object):\n         if not self.params._fixed_jitter:\n             # Gaussian prior in log(s^2)\n             log_s2 = rnd.normal(*self.params.jitter, size=size)\n-            samples['jitter'] = np.sqrt(np.exp(log_s2)) * self.params._jitter_unit\n+            samples['jitter'] = np.sqrt(\n+                np.exp(log_s2)) * self.params._jitter_unit\n \n             if return_logprobs:\n-                Jac = (2 / samples['jitter'].value) # Jacobian\n+                Jac = np.log(2 / samples['jitter'].value)  # Jacobian\n                 ln_prior_val += norm.logpdf(log_s2,\n                                             loc=self.params.jitter[0],\n-                                            scale=self.params.jitter[1]) * Jac\n+                                            scale=self.params.jitter[1]) + Jac\n \n         else:\n             samples['jitter'] = np.ones(size) * self.params.jitter\n@@ -204,8 +196,8 @@ class TheJoker(object):\n         for k, key in enumerate(nonlin_params):\n             samples[key] = samples_arr[:, k] * prior_units[k]\n \n-        samples['K'] = samples_arr[:, k+1] * prior_units[-1] # jitter unit\n-        samples['v0'] = samples_arr[:, k+2] * prior_units[-1] # jitter unit\n+        samples['K'] = samples_arr[:, k + 1] * prior_units[-1]  # jitter unit\n+        samples['v0'] = samples_arr[:, k + 2] * prior_units[-1]  # jitter unit\n \n         if return_logprobs:\n             return samples, ln_prior\n@@ -375,8 +367,8 @@ class TheJoker(object):\n \n         # There are some magic numbers below used to control how fast the\n         # iterative batches grow in size\n-        safety_factor = 2 # MAGIC NUMBER\n-        n_process = magic_fudge * n_requested_samples # MAGIC NUMBER\n+        safety_factor = 2  # MAGIC NUMBER\n+        n_process = magic_fudge * n_requested_samples  # MAGIC NUMBER\n \n         if n_process > n_prior_samples:\n             raise ValueError(\"Prior sample library not big enough! For \"\n@@ -403,7 +395,7 @@ class TheJoker(object):\n                                                  data.rv.unit)\n \n             maxiter = 128\n-            for i in range(maxiter): # we just need to iterate for a long time\n+            for i in range(maxiter):  # we just need to iterate for a long time\n                 logger.log(1, \"The Joker iteration {0}, computing {1} \"\n                            \"likelihoods\".format(i, n_process))\n                 marg_lls = compute_likelihoods(n_process, prior_cache_file,\n@@ -452,3 +444,105 @@ class TheJoker(object):\n \n         return self._unpack_full_samples(result, prior_units, t0=data.t0,\n                                          return_logprobs=return_logprobs)\n+\n+    # ========================================================================\n+    # MCMC\n+\n+    def mcmc_sample(self, data, samples0, n_steps=1024,\n+                    n_requested_samples=None, n_walkers=128, n_burn=8192,\n+                   \n\n… truncated at 24,000 bytes of 27,883", "bytes": 27883, "truncated": true}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/scripts/run-sampler.py b/scripts/run-sampler.py\nindex 28ecf40..a467a2d 100644\n--- a/scripts/run-sampler.py\n+++ b/scripts/run-sampler.py\n@@ -3,6 +3,7 @@ import os\n import sys\n \n # Third-party\n+from astropy import log as logger\n import astropy.units as u\n import h5py\n import numpy as np\n@@ -10,8 +11,14 @@ import six\n from schwimmbad import choose_pool\n \n # Project\n+from thejoker import Paths\n+paths = Paths()\n from thejoker.data import RVData\n-from thejoker.log import log as logger\n+from thejoker.units import default_units\n+# from thejoker.pool import choose_pool\n+from thejoker.celestialmechanics import OrbitalParams\n+from thejoker.sampler import get_good_samples, samples_to_orbital_params, sample_prior\n+from thejoker import config\n \n def main(data_file, pool, tmp_prior_filename, n_samples=1, seed=42, hdf5_key=None,\n          cache_filename=None, overwrite=False, continue_sampling=False,\ndiff --git a/setup.cfg b/setup.cfg\nindex 59419e7..a1c20b3 100644\n--- a/setup.cfg\n+++ b/setup.cfg\n@@ -13,10 +13,9 @@ upload-dir = docs/_build/html\n show-response = 1\n \n [tool:pytest]\n-minversion = 3.0\n+minversion = 2.2\n norecursedirs = build docs/_build\n doctest_plus = enabled\n-addopts = -p no:warnings\n \n [ah_bootstrap]\n auto_use = True\ndiff --git a/thejoker/data.py b/thejoker/data.py\nindex 29f23cc..f6c2ae0 100644\n--- a/thejoker/data.py\n+++ b/thejoker/data.py\n@@ -161,8 +161,7 @@ class RVData:\n \n     # ---\n \n-    def plot(self, ax=None, rv_unit=None, time_format='mjd', phase_fold=None,\n-             **kwargs):\n+    def plot(self, ax=None, rv_unit=None, time_format='mjd', **kwargs):\n         \"\"\"\n         Plot the data points.\n \n@@ -180,10 +179,6 @@ class RVData:\n             the `~astropy.time.Time` object, or it can be a callable (e.g.,\n             function) that does more complex things (for example:\n             ``time_format=lambda t: t.datetime.day``).\n-        phase_fold : bool (optional)\n-            Plot the phase instead of the time by folding on the specified\n-            period.\n-        `~astropy.units.Quantity`\n         **kwargs\n             All other keyword arguments are passed to the\n             `~matplotlib.pyplot.errorbar` (if errors were provided) or\ndiff --git a/thejoker/sampler/likelihood.py b/thejoker/sampler/likelihood.py\nindex c9fdb1a..3b4d2d3 100644\n--- a/thejoker/sampler/likelihood.py\n+++ b/thejoker/sampler/likelihood.py\n@@ -55,7 +55,7 @@ def design_matrix(nonlinear_p, data, joker_params):\n \n     # TODO: right now, we only support a constant N=1 velocity trend!\n     A1 = np.vander(t, N=1, increasing=True)\n-    A = np.hstack((zdot[:, None], A1))\n+    A = np.hstack((zdot[:,None], A1))\n \n     return A\n \ndiff --git a/thejoker/sampler/mcmc.py b/thejoker/sampler/mcmc.py\nindex f7d8863..8039ebc 100644\n--- a/thejoker/sampler/mcmc.py\n+++ b/thejoker/sampler/mcmc.py\n@@ -2,264 +2,266 @@\n import astropy.units as u\n import numpy as np\n from scipy.stats import beta, norm\n+from twobody.wrap import cy_rv_from_elements\n \n # Project\n-from .likelihood import get_ivar, design_matrix\n-from .params import JokerParams\n-from .samples import JokerSamples\n-from ..data import RVData\n+from .likelihood import get_ivar\n \n-__all__ = ['TheJokerMCMCModel']\n+__all__ = ['to_mcmc_params', 'from_mcmc_params',\n+           'pack_samples', 'pack_samples_mcmc', 'unpack_samples',\n+           'unpack_samples_mcmc',\n+           'ln_likelihood', 'ln_prior', 'ln_posterior']\n \n log_2pi = np.log(2 * np.pi)\n \n \n-class TheJokerMCMCModel:\n-\n-    def __init__(self, joker_params, data):\n-        \"\"\"\n-\n-        Parameters\n-        ----------\n-        data : `~thejoker.data.RVData`\n-            The radial velocity data.\n-        \"\"\"\n-\n-        # check if a JokerParams instance was passed in to specify the state\n-        if not isinstance(joker_params, JokerParams):\n-            raise TypeError(\"Parameter specification must be a JokerParams \"\n-                            \"instance, not a '{0}'\".format(type(joker_params)))\n-        self.params = joker_params\n-\n-        if not isinstance(data, RVData):\n-            raise TypeError(\"Data must be a valid RVData object.\")\n-        self.data = data\n-\n-        # various cached things:\n-        self._P_min = self.params.P_min.to(u.day).value\n-        self._P_max = self.params.P_max.to(u.day).value\n-        self._rv_unit = self.data.rv.unit\n-        self._jitter_factor = self._rv_unit.to(self.params._jitter_unit)\n-\n-        if self.params._fixed_jitter:\n-            s = self.params.jitter\n-            self._y_jitter = 2 * np.log(s.to(self._rv_unit).value)\n-\n-    @classmethod\n-    def to_mcmc_params(cls, p):\n-        r\"\"\"MCMC internal function.\n-\n-        Transform from linear orbital parameter values to standard\n-        variables for MCMC sampling:\n-\n-        .. math::\n-\n-            \\ln P \\\\\n-            \\sqrt{K}\\,\\cos\\phi_0, \\sqrt{K}\\,\\sin\\phi_0 \\\\\n-            \\sqrt{e}\\,\\cos\\omega, \\sqrt{e}\\,\\sin\\omega \\\\\n-            \\ln s^2 \\\\\n-            v_0,..., v_n\n-\n-        Parameters\n-        ----------\n-        p : iterable\n-            A packed parameter vector containing the orbital parameters\n-            and long-term velocity trend parameters.\n-\n-        \"\"\"\n-        P, M0, e, omega, s, K, *v_terms = p\n-        return np.vstack([np.log(P),\n-                          np.sqrt(K) * np.cos(M0), np.sqrt(K) * np.sin(M0),\n-                          np.sqrt(e) * np.cos(omega), np.sqrt(e) * np.sin(omega),\n-                          2*np.log(s)] + list(v_terms))\n-\n-    @classmethod\n-    def from_mcmc_params(cls, p):\n-        \"\"\"MCMC internal function.\n-\n-        Transform from the standard MCMC parameters to the linear\n-        values of the orbital parameters.\n-\n-        Parameters\n-        ----------\n-        p : iterable\n-            A packed parameter vector containing the MCMC-transforemd\n-            versions of the orbital parameters and long-term velocity\n-            trend parameters.\n-\n-        \"\"\"\n-        (ln_P,\n-         sqrtK_cos_M0, sqrtK_sin_M0,\n-         sqrte_cos_omega, sqrte_sin_omega,\n-         log_s2, *v_terms) = p\n-\n-        return np.vstack([np.exp(ln_P),\n-                          np.arctan2(sqrtK_sin_M0, sqrtK_cos_M0) % (2*np.pi),\n-                          sqrte_cos_omega**2 + sqrte_sin_omega**2,\n-                          np.arctan2(sqrte_sin_omega, sqrte_cos_omega) % (2*np.pi),\n-                          np.sqrt(np.exp(log_s2)),\n-                          (sqrtK_cos_M0**2 + sqrtK_sin_M0**2)] + v_terms)\n-\n-    def pack_samples(self, samples):\n-        \"\"\"Pack a dictionary of samples as Quantity objects into a 2D array.\n-\n-        Parameters\n-        ----------\n-        samples : dict\n-            Dictionary of `~astropy.units.Quantity` objects for period,\n-            M0, etc.\n-\n-        Returns\n-        -------\n-        arr : `numpy.ndarray`\n-            A 2D numpy array with shape `(nsamples, ndim)`.\n-        \"\"\"\n-        if 'jitter' in samples:\n-            jitter = samples['jitter'].to(self._rv_unit).value\n-        else:\n-            jitter = np.zeros_like(samples['P'].value)\n-\n-        arr = [samples['P'].to(u.day).value,\n-               samples['M0'].to(u.radian).value,\n-               np.asarray(samples['e']),\n-               samples['omega'].to(u.radian).value,\n-               jitter,\n-               samples['K'].to(self._rv_unit).value,\n-               samples['v0'].to(self._rv_unit).value]\n-        # TODO: assumes only constant velocity offset\n-\n-        return np.array(arr).T\n-\n-    def pack_samples_mcmc(self, samples):\n-        \"\"\"Pack a dictionary of samples as Quantity objects into a 2D array,\n-        transformed to the parametrization used by the MCMC functions.\n-\n-        Parameters\n-        ----------\n-        samples : dict\n-            Dictionary of `~astropy.units.Quantity` objects for period,\n-            M0, etc.\n-\n-        Returns\n-        -------\n-        arr : `numpy.ndarray`\n-            A 2D numpy array with shape `(nsamples, ndim)`.\n-        \"\"\"\n-        samples_vec = self.pack_samples(samples, self.params)\n-        samples_mcmc = self.to_mcmc_params(samples_vec.T)\n-\n-        if self.params._fixed_jitter:\n-            samples_mcmc = np.delete(samples_mcmc, 5, axis=0)\n-\n-        return np.array(samples_mcmc).T\n-\n-    def unpack_samples(self, samples_arr):\n-        \"\"\"Unpack a 2D array of samples into a dictionary of samples as\n-        Quantity objects.\n-\n-        Parameters\n-        ----------\n-        samples_arr : `numpy.ndarray`\n-            A 2D numpy array with shape `(nsamples, ndim)` containing the values.\n-\n-        Returns\n-        -------\n-        samples : dict\n-            Dictionary of `~astropy.units.Quantity` objects for period,\n-            M0, etc.\n-        \"\"\"\n-        samples = JokerSamples()\n-        samples['P'] = samples_arr.T[0] * u.day\n-        samples['M0'] = samples_arr.T[1] * u.radian\n-        samples['e'] = samples_arr.T[2] * u.one\n-        samples['omega'] = samples_arr.T[3] * u.radian\n-\n-        if not self.params._fixed_jitter:\n-            samples['jitter'] = samples_arr.T[4] * self._rv_unit\n-            shift = 1\n-        else:\n-            samples['jitter'] = np.zeros_like(samples_arr.T[0]) * self._rv_unit\n-            shift = 0\n-\n-        samples['K'] = samples_arr.T[4+shift] * self._rv_unit\n-\n-        # TODO: assumes only constant velocity offset\n-        samples['v0'] = samples_arr.T[5+shift] * self._rv_unit\n-\n-        return samples\n-\n-    def unpack_samples_mcmc(self, samples_arr):\n-        \"\"\"Unpack a 2D array of samples transformed to the parametrization used\n-        by the MCMC functions into a dictionary of samples as Quantity objects\n-        in the standard parametrization (i.e. period, angles, ...).\n-\n-        Parameters\n-        ----------\n-        samples_arr : `numpy.ndarray`\n-            A 2D numpy array with shape `(nsamples, ndim)` containing the\n-            values in the MCMC coordinates.\n-\n-        Returns\n-        -------\n-        samples : `thejoker.JokerSamples`\n-        \"\"\"\n-        samples_arr = self.from_mcmc_params(samples_arr.T).T\n-        return self.unpack_samples(samples_arr)\n-\n-    def ln_likelihood(self, p):\n-        P, M0, ecc, omega, s, K, *v_terms = p\n-\n-        # a little repeated code here...\n-\n-        A = design_matrix([P, M0, ecc, omega], self.data, self.params)\n-        p2 = np.array([K] + v_terms)\n-        ivar = get_ivar(self.data, s)\n-        dy = A.dot(p2) - self.data.rv.value\n-\n-        return 0.5 * (-dy**2 * ivar - log_2pi + np.log(ivar))\n-\n-    def ln_prior(self, p):\n-        # TODO: repeated code here and hard-coded priors\n-\n-        P, M0, ecc, omega, s, K, *v_terms = p\n-\n-        lnp = 0.\n-\n-        if ecc < 0 or ecc > 1:\n-            return -np.inf\n-\n-        lnp += beta.logpdf(ecc, 0.867, 3.03) # Kipping et al. 2013\n-\n-        # uniform in ln(P) - we don't need the jacobian because we sample in lnP\n-        if P < self._P_min or P > self._P_max:\n-            return -np.inf\n-        # lnp += -np.log(P)\n-\n-        if not self.params._fixed_jitter:\n-            # Gaussian prior in ln(s^2) - don't need Jacobian because we are\n-            # actually sampling in y = ln(s^2)\n-            s_scaled = s * self._jitter_factor\n-            y = 2 * np.log(s_scaled)\n-            lnp += norm.logpdf(y, loc=self.params.jitter[0],\n-                               scale=self.params.jitter[1])\n+def to_mcmc_params(p):\n+    r\"\"\"\n+    MCMC internal function.\n+\n+    Transform from linear orbital parameter values to standard\n+    variables for MCMC sampling:\n+\n+    .. math::\n+\n+        \\ln P \\\\\n+        \\sqrt{K}\\,\\cos\\phi_0, \\sqrt{K}\\,\\sin\\phi_0 \\\\\n+        \\sqrt{e}\\,\\cos\\omega, \\sqrt{e}\\,\\sin\\omega \\\\\n+        \\ln s \\\\\n+        v_0,..., v_n\n+\n+    Parameters\n+    ----------\n+    p : iterable\n+        A packed parameter vector containing the orbital parameters\n+        and long-term velocity trend parameters.\n+\n+    \"\"\"\n+    P, M0, ecc, omega, s, K, *v_terms = p\n+    return np.vstack([np.log(P),\n+                      np.sqrt(K) * np.cos(M0), np.sqrt(K) * np.sin(M0),\n+                      np.sqrt(ecc) * np.cos(omega), np.sqrt(ecc) * np.sin(omega),\n+                      2*np.log(s)] + list(v_terms))\n+\n+\n+def from_mcmc_params(p):\n+    \"\"\"\n+    MCMC internal function.\n+\n+    Transform from the standard MCMC parameters to the linear\n+    values of the orbital parameters.\n+\n+    Parameters\n+    ----------\n+    p : iterable\n+        A packed parameter vector containing the MCMC-transforemd\n+        versions of the orbital parameters and long-term velocity\n+        trend parameters.\n+\n+    \"\"\"\n+    (ln_P,\n+     sqrtK_cos_M0, sqrtK_sin_M0,\n+     sqrte_cos_omega, sqrte_sin_omega,\n+     log_s2, *v_terms) = p\n+\n+    return np.vstack([np.exp(ln_P),\n+                      np.arctan2(sqrtK_sin_M0, sqrtK_cos_M0) % (2*np.pi),\n+                      sqrte_cos_omega**2 + sqrte_sin_omega**2,\n+                      np.arctan2(sqrte_sin_omega, sqrte_cos_omega) % (2*np.pi),\n+                      np.sqrt(np.exp(log_s2)),\n+                      (sqrtK_cos_M0**2 + sqrtK_sin_M0**2)] + v_terms)\n+\n+\n+def pack_samples(samples, params, data):\n+    \"\"\"\n+    Pack a dictionary of samples as Quantity objects into a 2D array.\n+\n+    Parameters\n+    ----------\n+    samples : dict\n+        Dictionary of `~astropy.units.Quantity` objects for period,\n+        M0, etc.\n+    params : `~thejoker.sampler.params.JokerParams`\n+        Object specifying hyper-parameters for The Joker.\n+    data : `~thejoker.data.RVData`\n+        The radial velocity data.\n+\n+    Returns\n+    -------\n+    arr : `numpy.ndarray`\n+        A 2D numpy array with shape `(nsamples, ndim)`.\n+    \"\"\"\n+    if 'jitter' in samples:\n+        jitter = samples['jitter'].to(data.rv.unit).value\n+    else:\n+        jitter = np.zeros_like(samples['P'].value)\n+\n+    arr = [samples['P'].to(u.day).value,\n+           samples['M0'].to(u.radian).value,\n+           np.asarray(samples['e']),\n+           samples['omega'].to(u.radian).value,\n+           jitter,\n+           samples['K'].to(data.rv.unit).value,\n+           samples['v0'].to(data.rv.unit).value]\n+    # TODO: assumes only constant velocity offset\n+\n+    return np.array(arr).T\n+\n+\n+def pack_samples_mcmc(samples, params, data):\n+    \"\"\"\n+    Pack a dictionary of samples as Quantity objects into a 2D array,\n+    transformed to the parametrization used by the MCMC functions.\n+\n+    Parameters\n+    ----------\n+    samples : dict\n+        Dictionary of `~astropy.units.Quantity` objects for period,\n+        M0, etc.\n+    params : `~thejoker.sampler.params.JokerParams`\n+        Object specifying hyper-parameters for The Joker.\n+    data : `~thejoker.data.RVData`\n+        The radial velocity data.\n+\n+    Returns\n+    -------\n+    arr : `numpy.ndarray`\n+        A 2D numpy array with shape `(nsamples, ndim)`.\n+    \"\"\"\n+    samples_vec = pack_samples(samples, params, data)\n+    samples_mcmc = to_mcmc_params(samples_vec.T)\n+\n+    if params._fixed_jitter:\n+        samples_mcmc = np.delete(samples_mcmc, 5, axis=0)\n+\n+    return np.array(samples_mcmc).T\n+\n+\n+def unpack_samples(samples_arr, params, data):\n+    \"\"\"\n+    Unpack a 2D array of samples into a dictionary of samples as Quantity objects.\n+\n+    Parameters\n+    ----------\n+    samples_arr : `numpy.ndarray`\n+        A 2D numpy array with shape `(nsamples, ndim)` containing the values.\n+    params : `~thejoker.sampler.params.JokerParams`\n+        Object specifying hyper-parameters for The Joker.\n+    data : `~thejoker.data.RVData`\n+        The radial velocity data.\n+\n+    Returns\n+    -------\n+    samples : dict\n+        Dictionary of `~astropy.units.Quantity` objects for period,\n+        M0, etc.\n+    \"\"\"\n+    samples = dict()\n+    samples['P'] = samples_arr.T[0] * u.day\n+    samples['M0'] = samples_arr.T[1] * u.radian\n+    samples['e'] = samples_arr.T[2] * u.one\n+    samples['omega'] = samples_arr.T[3] * u.radian\n+\n+    if not params._fixed_jitter:\n+        samples['jitter'] = samples_arr.T[4] * data.rv.unit\n+        shift = 1\n+    else:\n+        samples['jitter'] = np.zeros_like(samples_arr.T[0]) * data.rv.unit\n+        shift = 0\n+\n+    samples['K'] = samples_arr.T[4+shift] * data.rv.unit\n+\n+    # TODO: assumes only constant velocity offset\n+    samples['v0'] = samples_arr.T[5+shift] * data.rv.unit\n+\n+    return samples\n+\n+\n+def unpack_samples_mcmc(samples_arr, params, data):\n+    \"\"\"\n+    Unpack a 2D array of samples transformed to the parametrization used by the\n+    MCMC functions into a dictionary of samples as Quantity objects in the\n+    standard parametrization (i.e. period, angles, ..).\n+\n+    Parameters\n+    ----------\n+    samples_arr : `numpy.ndarray`\n+        A 2D numpy array with shape `(nsamples, ndim)` containing the\n+        values in the MCMC coordinates.\n+    params : `~thejoker.sampler.params.JokerParams`\n+        Object specifying hyper-parameters for The Joker.\n+    data : `~thejoker.data.RVData`\n+        The radial velocity data.\n+\n+    Returns\n+    -------\n+    samples : dict\n+        Dictionary of `~astropy.units.Quantity` objects for period,\n+        M0, etc.\n+    \"\"\"\n+    samples_arr = from_mcmc_params(samples_arr.T).T\n+    return unpack_samples(samples_arr, params, data)\n \n-        return lnp\n \n-    def ln_posterior(self, mcmc_p):\n-        if self.params._fixed_jitter:\n-            mcmc_p = list(mcmc_p)\n-            mcmc_p.insert(5, self._y_jitter) # whoa, major hackage!\n+def ln_likelihood(p, joker_params, data):\n+    P, M0, ecc, omega, s, K, *v_terms = p\n+\n+    # a little repeated code here...\n+\n+    t = data._t_bmjd\n+    zdot = cy_rv_from_elements(t, P, 1., ecc,\n+                               omega, M0, data._t0_bmjd,\n+                               joker_params.anomaly_tol,\n+                               joker_params.anomaly_maxiter)\n+\n+    # TODO: right now, we only support a constant systemic velocity\n+    A1 = np.vander(t, N=1, increasing=True)\n+    A = np.hstack((zdot[:,None], A1))\n+    p = np.array([K] + v_terms)\n+    ivar = get_ivar(data, s)\n+\n+    dy = A.dot(p) - data.rv.value\n+\n+    return 0.5 * (-dy**2 * ivar - log_2pi + np.log(ivar))\n+\n+\n+def ln_prior(p, joker_params):\n+    P, M0, ecc, omega, s, K, *v_terms = p\n+\n+    lnp = 0.\n+\n+    # TODO: more repeated code here and hard-coded priors\n+    if ecc < 0 or ecc > 1:\n+        return -np.inf\n \n-        p = self.from_mcmc_params(mcmc_p).reshape(len(mcmc_p))\n+    lnp += beta.logpdf(ecc, 0.867, 3.03) # Kipping et al. 2013\n \n-        lnp = self.ln_prior(p)\n-        if np.isinf(lnp):\n-            return lnp\n+    # TODO: do we need P_min, P_max here?\n+\n+    if not joker_params._fixed_jitter:\n+        # DFM's idea: wide, Gaussian prior in log(s^2)\n+        # lnp += norm.logpdf(np.log(s), ) # TODO: put in hyper-parameters\n+        # TODO:\n+        pass\n+\n+    return lnp\n+\n+\n+def ln_posterior(mcmc_p, joker_params, data):\n+    if joker_params._fixed_jitter:\n+        mcmc_p = list(mcmc_p)\n+        mcmc_p.insert(5, -np.inf) # HACK: whoa, major hackage!\n+\n+    p = from_mcmc_params(mcmc_p).reshape(len(mcmc_p))\n+\n+    lnp = ln_prior(p, joker_params)\n+    if np.isinf(lnp):\n+        return lnp\n \n-        lnl = self.ln_likelihood(p)\n-        lnprob = lnp + lnl.sum()\n+    lnl = ln_likelihood(p, joker_params, data)\n+    lnprob = lnp + lnl.sum()\n \n-        if np.isnan(lnprob):\n-            return -np.inf\n+    if np.isnan(lnprob):\n+        return -np.inf\n \n-        return lnprob\n+    return lnprob\ndiff --git a/thejoker/sampler/params.py b/thejoker/sampler/params.py\nindex 204c676..3a1d7f1 100644\n--- a/thejoker/sampler/params.py\n+++ b/thejoker/sampler/params.py\n@@ -86,7 +86,6 @@ class JokerParams(object):\n \n         else:\n             self._fixed_jitter = True\n-            self._jitter_unit = jitter.unit\n             self.jitter = jitter\n \n     @property\ndiff --git a/thejoker/sampler/sampler.py b/thejoker/sampler/sampler.py\nindex f4453c0..b62535f 100644\n--- a/thejoker/sampler/sampler.py\n+++ b/thejoker/sampler/sampler.py\n@@ -17,7 +17,6 @@ from .multiproc_helpers import (get_good_sample_indices, compute_likelihoods,\n                                 sample_indices_to_full_samples)\n from .io import save_prior_samples\n from .samples import JokerSamples\n-from .mcmc import TheJokerMCMCModel\n \n __all__ = ['TheJoker']\n \n@@ -42,7 +41,6 @@ class TheJoker(object):\n         sample caches, you may need to set this to a larger number (e.g.,\n         ``100*pool.size``) to avoid memory issues.\n     \"\"\"\n-\n     def __init__(self, params, pool=None, random_state=None, n_batches=None):\n \n         # set the processing pool\n@@ -79,6 +77,17 @@ class TheJoker(object):\n \n         self.n_batches = n_batches\n \n+        # Used for caching\n+        self._cache = dict()\n+\n+        # Files to be closed when _cleanup() is called\n+        self._cache['tempfiles'] = []\n+\n+    def _cleanup(self):\n+        # Close any temp. files:\n+        for f in self._cache['tempfiles']:\n+            f.close()\n+\n     def sample_prior(self, size=1, return_logprobs=False):\n         \"\"\"Generate samples from the prior. Logarithmic in period, uniform in\n         phase and argument of pericenter, Beta distribution in eccentricity.\n@@ -138,14 +147,13 @@ class TheJoker(object):\n         if not self.params._fixed_jitter:\n             # Gaussian prior in log(s^2)\n             log_s2 = rnd.normal(*self.params.jitter, size=size)\n-            samples['jitter'] = np.sqrt(\n-                np.exp(log_s2)) * self.params._jitter_unit\n+            samples['jitter'] = np.sqrt(np.exp(log_s2)) * self.params._jitter_unit\n \n             if return_logprobs:\n-                Jac = np.log(2 / samples['jitter'].value)  # Jacobian\n+                Jac = (2 / samples['jitter'].value) # Jacobian\n                 ln_prior_val += norm.logpdf(log_s2,\n                                             loc=self.params.jitter[0],\n-                                            scale=self.params.jitter[1]) + Jac\n+                                            scale=self.params.jitter[1]) * Jac\n \n         else:\n             samples['jitter'] = np.ones(size) * self.params.jitter\n@@ -196,8 +204,8 @@ class TheJoker(object):\n         for k, key in enumerate(nonlin_params):\n             samples[key] = samples_arr[:, k] * prior_units[k]\n \n-        samples['K'] = samples_arr[:, k + 1] * prior_units[-1]  # jitter unit\n-        samples['v0'] = samples_arr[:, k + 2] * prior_units[-1]  # jitter unit\n+        samples['K'] = samples_arr[:, k+1] * prior_units[-1] # jitter unit\n+        samples['v0'] = samples_arr[:, k+2] * prior_units[-1] # jitter unit\n \n         if return_logprobs:\n             return samples, ln_prior\n@@ -367,8 +375,8 @@ class TheJoker(object):\n \n         # There are some magic numbers below used to control how fast the\n         # iterative batches grow in size\n-        safety_factor = 2  # MAGIC NUMBER\n-        n_process = magic_fudge * n_requested_samples  # MAGIC NUMBER\n+        safety_factor = 2 # MAGIC NUMBER\n+        n_process = magic_fudge * n_requested_samples # MAGIC NUMBER\n \n         if n_process > n_prior_samples:\n             raise ValueError(\"Prior sample library not big enough! For \"\n@@ -395,7 +403,7 @@ class TheJoker(object):\n                                                  data.rv.unit)\n \n             maxiter = 128\n-            for i in range(maxiter):  # we just need to iterate for a long time\n+            for i in range(maxiter): # we just need to iterate for a long time\n                 logger.log(1, \"The Joker iteration {0}, computing {1} \"\n                            \"likelihoods\".format(i, n_process))\n                 marg_lls = compute_likelihoods(n_process, prior_cache_file,\n@@ -444,105 +452,3 @@ class TheJoker(object):\n \n         return self._unpack_full_samples(result, prior_units, t0=data.t0,\n                                          return_logprobs=return_logprobs)\n-\n-    # ========================================================================\n-    # MCMC\n-\n-    def mcmc_sample(self, data, samples0, n_steps=1024,\n-                    n_requested_samples=None, n_walkers=128, n_burn=8192,\n-                   \n\n… truncated at 24,000 bytes of 32,538", "bytes": 32538, "truncated": true}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    libffi-dev \\\n    libssl-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv && \\\n    uv python install 3.8\n\nWORKDIR /app\n\nRUN git clone https://github.com/adrn/thejoker.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 854d15313c27531d4a560a72f7df310935963629 || git fetch --depth 1 origin \"+refs/pull/71/head:refs/remotes/origin/pr/71\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\nENV CI=true\n\n# Remove Cython .pyx before install - the C extension can't compile with modern SciPy\n# and is not needed for the tests we run\nRUN rm -f thejoker/sampler/fast_likelihood.pyx\n\nRUN uv venv /opt/venv --python python3.8 && \\\n    uv pip install --python /opt/venv/bin/python \\\n        setuptools \\\n        wheel \\\n        numpy \\\n        extension-helpers \\\n        six \\\n        python-dateutil \\\n        && \\\n    uv pip install --python /opt/venv/bin/python --no-build-isolation -e . && \\\n    uv pip install --python /opt/venv/bin/python \\\n        pytest \\\n        emcee \\\n        astropy-ci-extras\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1485, "truncated": false}]}, {"task_name": "adrn__thejoker-81", "repo": "adrn/thejoker", "language": "Python", "difficulty": "medium", "area": "library", "topic": "temporary-files", "bug_class": "missing-parameter", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "The `TheJoker` class currently uses the operating system's default temporary directory for storing prior samples when `rejection_sample` is called without a pre-generated cache file. If this default directory has limited storage, the operation fails with errors.\n\nImplement a `tempfile_path` argument in the `TheJoker.__init__` method. This argument should allow users to specify a custom directory for the temporary prior cache files. \n\nBehavior requirements:\n1. The `TheJoker` class must accept an optional keyword argument `tempfile_path` during initialization.\n2. If `tempfile_path` is provided, the temporary file created internally during `rejection_sample` (e.g., via `tempfile.NamedTemporaryFile`) must be created within the specified directory.\n3. If `tempfile_path` is not provided, the default OS temporary directory behavior should remain unchanged (or simply not be explicitly set in the tempfile constructor).\n\nExample usage:\n```python\nimport tempfile\nfrom thejoker.sampler import TheJoker\n\n# Specify a custom path for temporary files\njoker = TheJoker(params, tempfile_path='/custom/temp/path')\n```", "bytes": 1112, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src\n\nexport CI=true\n\n# Copy HEAD test files from /tests (overwrites BASE state)\nmkdir -p \"thejoker/sampler/tests\"\ncp \"/tests/thejoker/sampler/tests/test_sampler.py\" \"thejoker/sampler/tests/test_sampler.py\"\n\npytest -xvs \"thejoker/sampler/tests/test_sampler.py\"\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 431, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/.travis.yml b/.travis.yml\nindex b990b63..d2416bd 100644\n--- a/.travis.yml\n+++ b/.travis.yml\n@@ -33,8 +33,7 @@ env:\n         - MAIN_CMD='python setup.py'\n         - SETUP_CMD='test'\n         # - PIP_DEPENDENCIES='emcee schwimmbad nbsphinx twobody'\n-        # Dev version of twobody!\n-        - PIP_DEPENDENCIES='emcee schwimmbad nbsphinx git+https://github.com/adrn/twobody'\n+        - PIP_DEPENDENCIES='emcee schwimmbad nbsphinx twobody>=0.4'\n         - CONDA_CHANNELS='astropy-ci-extras astropy'\n         - CONDA_DEPENDENCIES='cython scipy h5py matplotlib pandoc nbconvert ipykernel'\n \ndiff --git a/setup.cfg b/setup.cfg\nindex 59419e7..f8c3c65 100644\n--- a/setup.cfg\n+++ b/setup.cfg\n@@ -46,7 +46,7 @@ license = MIT\n url = https://github.com/adrn/thejoker\n edit_on_github = False\n github_project = adrn/thejoker\n-install_requires = astropy twobody>=0.2 scipy h5py schwimmbad\n+install_requires = astropy twobody>=0.4 scipy h5py schwimmbad\n # version should be PEP440 compatible (http://www.python.org/dev/peps/pep-0440)\n version = 0.2.dev\n \ndiff --git a/thejoker/sampler/likelihood.py b/thejoker/sampler/likelihood.py\nindex c9fdb1a..6b3640f 100644\n--- a/thejoker/sampler/likelihood.py\n+++ b/thejoker/sampler/likelihood.py\n@@ -1,3 +1,7 @@\n+\"\"\"\n+NOTE: this is only used for testing the cython / c implementation.\n+\"\"\"\n+\n # Third-party\n import numpy as np\n from twobody.wrap import cy_rv_from_elements\n@@ -53,7 +57,7 @@ def design_matrix(nonlinear_p, data, joker_params):\n                                joker_params.anomaly_tol,\n                                joker_params.anomaly_maxiter)\n \n-    # TODO: right now, we only support a constant N=1 velocity trend!\n+    # here we only support a constant N=1 velocity trend!\n     A1 = np.vander(t, N=1, increasing=True)\n     A = np.hstack((zdot[:, None], A1))\n \ndiff --git a/thejoker/sampler/sampler.py b/thejoker/sampler/sampler.py\nindex b4cbf13..3aa78d8 100644\n--- a/thejoker/sampler/sampler.py\n+++ b/thejoker/sampler/sampler.py\n@@ -42,9 +42,13 @@ class TheJoker:\n         ``pool.size``, meaning equal work to each worker. For very large prior\n         sample caches, you may need to set this to a larger number (e.g.,\n         ``100*pool.size``) to avoid memory issues.\n+    tempfile_path : str (optional)\n+        Path to create temporary files needed for executing the sampler.\n+        Defaults to whatever ``tempfile`` thinks is a good temporary directory.\n     \"\"\"\n \n-    def __init__(self, params, pool=None, random_state=None, n_batches=None):\n+    def __init__(self, params, pool=None, random_state=None, n_batches=None,\n+                 tempfile_path=None):\n \n         # set the processing pool\n         if pool is None:\n@@ -79,6 +83,7 @@ class TheJoker:\n         self.params = params\n \n         self.n_batches = n_batches\n+        self.tempfile_path = tempfile_path\n \n     def sample_prior(self, size=1, return_logprobs=False):\n         \"\"\"Generate samples from the prior. Logarithmic in period, uniform in\n@@ -323,7 +328,8 @@ class TheJoker:\n                 return_logprobs=return_logprobs)\n \n         else:\n-            with tempfile.NamedTemporaryFile(mode='r+') as f:\n+            with tempfile.NamedTemporaryFile(mode='r+',\n+                                             dir=self.tempfile_path) as f:\n                 prior_cache_file = f.name\n \n                 # first do prior sampling, cache to temporary file\n@@ -408,7 +414,7 @@ class TheJoker:\n             close_f = False\n \n         else:\n-            f = tempfile.NamedTemporaryFile(mode='r+')\n+            f = tempfile.NamedTemporaryFile(mode='r+', dir=self.tempfile_path)\n             close_f = True\n             prior_cache_file = f.name\n             logger.log(1, \"Cache file not found - creating prior samples \"\n@@ -462,15 +468,15 @@ class TheJoker:\n             # We should never get here!!\n             raise RuntimeError(\"Hit maximum number of iterations!\")\n \n-        if close_f:\n-            logger.log(1, \"Closing prior cache tempfile\")\n-            f.close()\n-\n         result = sample_indices_to_full_samples(\n             good_samples_idx[:n_requested_samples], prior_cache_file, data,\n             self.params, pool=self.pool, global_seed=seed,\n             return_logprobs=return_logprobs)\n \n+        if close_f:\n+            logger.log(1, \"Closing prior cache tempfile\")\n+            f.close()\n+\n         return self._unpack_full_samples(result, prior_units, t0=data.t0,\n                                          return_logprobs=return_logprobs)\n \n", "bytes": 4500, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/.travis.yml b/.travis.yml\nindex d2416bd..b990b63 100644\n--- a/.travis.yml\n+++ b/.travis.yml\n@@ -33,7 +33,8 @@ env:\n         - MAIN_CMD='python setup.py'\n         - SETUP_CMD='test'\n         # - PIP_DEPENDENCIES='emcee schwimmbad nbsphinx twobody'\n-        - PIP_DEPENDENCIES='emcee schwimmbad nbsphinx twobody>=0.4'\n+        # Dev version of twobody!\n+        - PIP_DEPENDENCIES='emcee schwimmbad nbsphinx git+https://github.com/adrn/twobody'\n         - CONDA_CHANNELS='astropy-ci-extras astropy'\n         - CONDA_DEPENDENCIES='cython scipy h5py matplotlib pandoc nbconvert ipykernel'\n \ndiff --git a/setup.cfg b/setup.cfg\nindex f8c3c65..59419e7 100644\n--- a/setup.cfg\n+++ b/setup.cfg\n@@ -46,7 +46,7 @@ license = MIT\n url = https://github.com/adrn/thejoker\n edit_on_github = False\n github_project = adrn/thejoker\n-install_requires = astropy twobody>=0.4 scipy h5py schwimmbad\n+install_requires = astropy twobody>=0.2 scipy h5py schwimmbad\n # version should be PEP440 compatible (http://www.python.org/dev/peps/pep-0440)\n version = 0.2.dev\n \ndiff --git a/thejoker/sampler/likelihood.py b/thejoker/sampler/likelihood.py\nindex 6b3640f..c9fdb1a 100644\n--- a/thejoker/sampler/likelihood.py\n+++ b/thejoker/sampler/likelihood.py\n@@ -1,7 +1,3 @@\n-\"\"\"\n-NOTE: this is only used for testing the cython / c implementation.\n-\"\"\"\n-\n # Third-party\n import numpy as np\n from twobody.wrap import cy_rv_from_elements\n@@ -57,7 +53,7 @@ def design_matrix(nonlinear_p, data, joker_params):\n                                joker_params.anomaly_tol,\n                                joker_params.anomaly_maxiter)\n \n-    # here we only support a constant N=1 velocity trend!\n+    # TODO: right now, we only support a constant N=1 velocity trend!\n     A1 = np.vander(t, N=1, increasing=True)\n     A = np.hstack((zdot[:, None], A1))\n \ndiff --git a/thejoker/sampler/sampler.py b/thejoker/sampler/sampler.py\nindex 3aa78d8..b4cbf13 100644\n--- a/thejoker/sampler/sampler.py\n+++ b/thejoker/sampler/sampler.py\n@@ -42,13 +42,9 @@ class TheJoker:\n         ``pool.size``, meaning equal work to each worker. For very large prior\n         sample caches, you may need to set this to a larger number (e.g.,\n         ``100*pool.size``) to avoid memory issues.\n-    tempfile_path : str (optional)\n-        Path to create temporary files needed for executing the sampler.\n-        Defaults to whatever ``tempfile`` thinks is a good temporary directory.\n     \"\"\"\n \n-    def __init__(self, params, pool=None, random_state=None, n_batches=None,\n-                 tempfile_path=None):\n+    def __init__(self, params, pool=None, random_state=None, n_batches=None):\n \n         # set the processing pool\n         if pool is None:\n@@ -83,7 +79,6 @@ class TheJoker:\n         self.params = params\n \n         self.n_batches = n_batches\n-        self.tempfile_path = tempfile_path\n \n     def sample_prior(self, size=1, return_logprobs=False):\n         \"\"\"Generate samples from the prior. Logarithmic in period, uniform in\n@@ -328,8 +323,7 @@ class TheJoker:\n                 return_logprobs=return_logprobs)\n \n         else:\n-            with tempfile.NamedTemporaryFile(mode='r+',\n-                                             dir=self.tempfile_path) as f:\n+            with tempfile.NamedTemporaryFile(mode='r+') as f:\n                 prior_cache_file = f.name\n \n                 # first do prior sampling, cache to temporary file\n@@ -414,7 +408,7 @@ class TheJoker:\n             close_f = False\n \n         else:\n-            f = tempfile.NamedTemporaryFile(mode='r+', dir=self.tempfile_path)\n+            f = tempfile.NamedTemporaryFile(mode='r+')\n             close_f = True\n             prior_cache_file = f.name\n             logger.log(1, \"Cache file not found - creating prior samples \"\n@@ -468,15 +462,15 @@ class TheJoker:\n             # We should never get here!!\n             raise RuntimeError(\"Hit maximum number of iterations!\")\n \n+        if close_f:\n+            logger.log(1, \"Closing prior cache tempfile\")\n+            f.close()\n+\n         result = sample_indices_to_full_samples(\n             good_samples_idx[:n_requested_samples], prior_cache_file, data,\n             self.params, pool=self.pool, global_seed=seed,\n             return_logprobs=return_logprobs)\n \n-        if close_f:\n-            logger.log(1, \"Closing prior cache tempfile\")\n-            f.close()\n-\n         return self._unpack_full_samples(result, prior_units, t0=data.t0,\n                                          return_logprobs=return_logprobs)\n \ndiff --git a/thejoker/sampler/tests/test_sampler.py b/thejoker/sampler/tests/test_sampler.py\nindex 592c843..922eaf6 100644\n--- a/thejoker/sampler/tests/test_sampler.py\n+++ b/thejoker/sampler/tests/test_sampler.py\n@@ -1,6 +1,3 @@\n-# Standard library\n-from os import path\n-\n # Third-party\n import astropy.units as u\n from astropy.tests.helper import quantity_allclose\n@@ -50,9 +47,6 @@ class TestSampler(object):\n         with pytest.raises(TypeError):\n             TheJoker(pars)\n \n-        j = TheJoker(self.joker_params['binary'],\n-                     tempfile_path=path.abspath('.'))\n-\n     def test_sample_prior(self):\n         rnd = np.random.RandomState(42)\n         joker = TheJoker(self.joker_params['binary'], random_state=rnd)\n@@ -72,13 +66,6 @@ class TestSampler(object):\n \n         joker.rejection_sample(data, n_prior_samples=128)\n \n-        # Now try specifying the tempfile path\n-        data = self.data['binary']\n-        joker = TheJoker(self.joker_params['binary'],\n-                         random_state=rnd, tempfile_path=path.abspath('.'))\n-\n-        _ = joker.rejection_sample(data, n_prior_samples=128)\n-\n         # Now re-run with jitter set, check that it's always the fixed value\n         jitter = 5.*u.m/u.s\n         params = JokerParams(P_min=8*u.day, P_max=128*u.day, jitter=jitter)\n", "bytes": 5813, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    python3-dev \\\n    libffi-dev \\\n    libssl-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone https://github.com/adrn/thejoker.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 5b5ea24784cbb07a704420111f913ea9e584780a || git fetch --depth 1 origin \"+refs/pull/81/head:refs/remotes/origin/pr/81\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\n# Patch ah_bootstrap.py for Python 3.12 compatibility (imp module removed)\nRUN sed -i 's/^import imp$/import importlib as imp_module/' ah_bootstrap.py && \\\n    sed -i 's/imp\\.reload/importlib.reload/' ah_bootstrap.py\n\n# Patch ez_setup.py for Python 3.12 (get_content_charset returns None)\nRUN sed -i \"s/charset = resp.info().get_content_charset()/charset = resp.info().get_content_charset() or 'UTF-8'/\" ez_setup.py\n\nENV CI=true\nRUN uv venv /opt/venv && \\\n    uv pip install --python /opt/venv/bin/python setuptools==69.5.1 wheel && \\\n    uv pip install --python /opt/venv/bin/python --no-build-isolation astropy scipy h5py schwimmbad twobody emcee && \\\n    uv pip install --python /opt/venv/bin/python --no-build-isolation -e . && \\\n    uv pip install --python /opt/venv/bin/python pytest\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1682, "truncated": false}]}, {"task_name": "allenai__s2_fos-8", "repo": "allenai/s2_fos", "language": "Python", "difficulty": "medium", "area": "backend", "topic": "s2-fos", "bug_class": "incorrect-output-format", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "The CI tests for the s2-fos repository are failing due to mismatches between the expected and actual structure of prediction outputs. Specifically, the `make_inference_text` function in the `model.utils` module is not handling the `Instance` object fields correctly under all conditions.\n\nThe function `make_inference_text(instance: Instance, use_abstract: bool)` is expected to construct a text string for inference based on the `Instance`'s `title` and `abstract` attributes:\n\n1. When `use_abstract` is `False`, the output should be strictly the `title`. If `abstract` is `None` or empty, this should still just return the `title`.\n2. When `use_abstract` is `True` and an `abstract` is available, the output should be a concatenation of the `title` and `abstract` separated by the string `|||||` (e.g., `\"title ||||| abstract\"`).\n3. When `use_abstract` is `True` but the `abstract` is `None`, the function should gracefully fall back to returning just the `title`.\n\nThe current implementation fails these checks, likely due to incorrect handling of `None` values for abstracts or incorrect concatenation logic. Ensure that the function correctly handles cases where the abstract is missing and correctly formats the combined string when both title and abstract are present.", "bytes": 1275, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\n\ncd /app/src\n\nexport CI=true\nsource /opt/venv/bin/activate\n\n# Copy test files from /tests into the repo\nmkdir -p tests/model/fixtures/example_labeled_data\ncp \"/tests/model/test_utils.py\" tests/model/test_utils.py\ncp \"/tests/model/fixtures/example_labeled_data/1.jsonl\" tests/model/fixtures/example_labeled_data/1.jsonl\ncp \"/tests/model/fixtures/example_labeled_data/2.jsonl\" tests/model/fixtures/example_labeled_data/2.jsonl\n\n# Ensure the package is installed so imports resolve\nuv pip install -e . --no-deps >/dev/null 2>&1 || true\n\npytest -xvs tests/model/test_utils.py\ntest_status=$?\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 734, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/entrypoint.py b/entrypoint.py\nindex 8ce9b38..5bf22e8 100644\n--- a/entrypoint.py\n+++ b/entrypoint.py\n@@ -1,5 +1,3 @@\n-import json\n-import logging\n import logging.config\n import os\n import signal\n@@ -8,12 +6,6 @@ import sys\n \n import click\n \n-from model.eval import EvalSettings, generate_metrics\n-from model.predictor import Predictor, PredictorConfig\n-from model.training import build_and_train_model, TrainingConfig\n-from model.utils import load_labeled_data, save_model\n-\n-\n dir = os.path.dirname(os.path.realpath(__file__))\n logging.config.fileConfig(os.path.join(dir, \"server\", \"logging.conf\"))\n logger = logging.getLogger(__name__)\n@@ -84,65 +76,5 @@ def serve():\n     logger.info(\"Inference server exiting\")\n \n \n-@cli.command()\n-def train():\n-    logger.info(\"BEGINNING TRAINING ROUTINE\")\n-\n-    training_config = TrainingConfig()\n-    hyperparameters = training_config.load_hyperparameters()\n-    training_examples = load_labeled_data(training_config.training_data_dir())\n-\n-    logger.info(\n-        f\"\"\"\n-        TRAINING WITH\n-        data channel: {training_config.channel_name}\n-        hyperparameters: {hyperparameters.json()}\n-    \"\"\"\n-    )\n-    trained_model = build_and_train_model(training_examples, hyperparameters)\n-\n-    logger.info(\"MODEL TRAINING COMPLETE, SAVING TO DISK\")\n-    logger.info(f\"MODEL VERSION: {training_config.model_version}\")\n-    save_model(training_config.target_artifacts_dir(), hyperparameters, trained_model)\n-    logger.info(\"MODEL SAVED, EXITING\")\n-\n-\n-@cli.command()\n-def evaluate():\n-    logger.info(\"BEGINNING EVAL ROUTINE\")\n-\n-    eval_settings = EvalSettings()\n-    eval_examples = load_labeled_data(eval_settings.eval_data_dir())\n-\n-    predictor_config = PredictorConfig()\n-    predictor = Predictor(predictor_config)\n-\n-    logger.info(\n-        f\"\"\"\n-        RUNNING EVAL FOR\n-        model version: {eval_settings.model_version}\n-        dataset: {eval_settings.channel_name}\n-    \"\"\"\n-    )\n-\n-    predictions = predictor.predict_batch([ex.instance for ex in eval_examples])\n-    metrics = generate_metrics(eval_examples, predictions)\n-\n-    metrics_dir = eval_settings.metrics_output_dir()\n-\n-    if not os.path.exists(metrics_dir):\n-        os.makedirs(metrics_dir)\n-\n-    logging.info(\n-        f\"\"\"\n-        METRICS:\n-        {metrics}\n-        \"\"\"\n-    )\n-\n-    with open(os.path.join(metrics_dir, \"metrics.json\"), \"w\") as f:\n-        f.write(json.dumps(metrics, indent=4))\n-\n-\n if __name__ == \"__main__\":\n     cli()\ndiff --git a/model/decisionscore.py b/model/decisionscore.py\nnew file mode 100644\nindex 0000000..09ad0d8\n--- /dev/null\n+++ b/model/decisionscore.py\n@@ -0,0 +1,8 @@\n+from pydantic import BaseModel, Field\n+\n+\n+class DecisionScore(BaseModel):\n+    \"\"\"Represents decision score predicted for a given field of study for a given paper\"\"\"\n+\n+    label: str\n+    score: float\ndiff --git a/model/decisionscores.py b/model/decisionscores.py\ndeleted file mode 100644\nindex a5c57ca..0000000\n--- a/model/decisionscores.py\n+++ /dev/null\n@@ -1,9 +0,0 @@\n-from typing import List\n-\n-from pydantic import BaseModel, Field\n-\n-\n-class DecisionScores(BaseModel):\n-    \"\"\"Represents decision scores predicted for fields of study for a given paper\"\"\"\n-\n-    scores: dict = Field(description=\"Descision scores for all fields of study\")\ndiff --git a/model/eval.py b/model/eval.py\ndeleted file mode 100644\nindex de0065f..0000000\n--- a/model/eval.py\n+++ /dev/null\n@@ -1,55 +0,0 @@\n-import os\n-from typing import Any, Dict, List, Optional\n-\n-from pydantic import BaseSettings, Field\n-\n-from model.example import Example\n-from model.prediction import Prediction\n-from model.decisionscores import DecisionScores\n-\n-\n-class EvalSettings(BaseSettings):\n-    artifacts_dir: str = Field(\n-        description=\"Directory to find model artifacts such as learned model parameters, etc\",\n-        default=\"/opt/ml/model\",\n-    )\n-    input_data_dir: str = Field(\n-        description=\"Directory to find data files\", default=\"/opt/ml/input/data\"\n-    )\n-    channel_name: str = Field(\n-        description=\"Name of data channel to run evaluation against. Name of subdirectory under `input_data_dir`\",\n-    )\n-    output_data_dir: str = Field(\n-        description=\"Directory to write metrics results into\", default=\"/opt/ml/output\"\n-    )\n-    model_version: Optional[str] = Field(\n-        description=\"Logical name for a model version or experiment, to segment and retrieve artifacts\",\n-        default=None,\n-    )\n-\n-    def eval_data_dir(self) -> str:\n-        return os.path.join(self.input_data_dir, self.channel_name)\n-\n-    def target_artifacts_dir(self) -> str:\n-        \"\"\"Specifies full directory path for location of model artifacts\"\"\"\n-\n-        if self.model_version is None or self.model_version == \"\":\n-            return self.artifacts_dir\n-\n-        return os.path.join(self.artifacts_dir, self.model_version)\n-\n-    def metrics_output_dir(self) -> str:\n-        \"\"\"Specifies full directory path for saving out evaluation metrics\"\"\"\n-\n-        if self.model_version is None or self.model_version == \"\":\n-            return os.path.join(self.artifacts_dir, self.channel_name)\n-\n-        return os.path.join(self.output_data_dir, self.model_version, self.channel_name)\n-\n-\n-def generate_metrics(\n-    eval_examples: List[Example], decisionscores: List[DecisionScores]\n-) -> Dict[str, Any]:\n-    # TODO: what metrics do we need to generate?\n-\n-    return {\"asdf\": \"fdsa\"}\ndiff --git a/model/prediction.py b/model/prediction.py\nindex 9317115..09dcc76 100644\n--- a/model/prediction.py\n+++ b/model/prediction.py\n@@ -1,9 +1,12 @@\n from typing import List\n \n+from model.decisionscore import DecisionScore\n from pydantic import BaseModel, Field\n \n \n class Prediction(BaseModel):\n-    \"\"\"Represents predicted fields of study for a given paper\"\"\"\n+    \"\"\"Represents predicted scores for all fields of study for a given paper\"\"\"\n \n-    foses: List[str] = Field(description=\"Predicted fields of study\")\n+    scores: List[DecisionScore] = Field(\n+        description=\"Decision scores for all fields of study\"\n+    )\ndiff --git a/model/predictor.py b/model/predictor.py\nindex 568fb1a..3a7fc35 100644\n--- a/model/predictor.py\n+++ b/model/predictor.py\n@@ -1,6 +1,6 @@\n from model.multioutput import MultiOutputClassifierWithDecision\n from model.prediction import Prediction\n-from model.decisionscores import DecisionScores\n+from model.decisionscore import DecisionScore\n from model.instance import Instance\n import logging\n import os\n@@ -72,7 +72,7 @@ class Predictor:\n         self.mlb_inverse_dict = {v: k for k, v in self._mlb._cached_dict.items()}\n \n     # works for single text at a time\n-    def get_decision_scores(self, original_text):\n+    def get_decision_scores(self, original_text) -> List[DecisionScore]:\n \n         # Skip the prediction process if the input text is not in english\n         is_english = utils.detect_language(self._fasttext, original_text)[1]\n@@ -83,19 +83,19 @@ class Predictor:\n         featurized_text = self._feature_pipe.transform([original_text])[0]\n         decision_scores = self._classifier.decision_function(featurized_text)\n \n-        model_predictions = {\n-            label: decision_scores[int(index)]\n+        # mlb_inverse_dict: {0: 'Agricultural and Food sciences', 1: 'Art', 2: 'Biology',... 21: 'Psychology', 22: 'Sociology'}\n+\n+        model_predictions: List[DecisionScore] = [\n+            DecisionScore(label=label, score=decision_scores[int(index)])\n             for index, label in self.mlb_inverse_dict.items()\n-        }\n+        ]\n \n         return model_predictions\n \n-    def predict_batch(self, instances: List[Instance]) -> List[DecisionScores]:\n+    def predict_batch(self, instances: List[Instance]) -> List[Prediction]:\n         texts = [\n             utils.make_inference_text(instance, self._hyperparameters.use_abstract)\n             for instance in instances\n         ]\n \n-        return [\n-            DecisionScores(scores=self.get_decision_scores(text)) for text in texts\n-        ]\n+        return [Prediction(scores=self.get_decision_scores(text)) for text in texts]\ndiff --git a/model/training.py b/model/training.py\ndeleted file mode 100644\nindex cc0d969..0000000\n--- a/model/training.py\n+++ /dev/null\n@@ -1,108 +0,0 @@\n-import logging\n-import os\n-from typing import List, Optional, Tuple\n-\n-import numpy as np\n-from pydantic import BaseModel, BaseSettings, Field\n-from sklearn.feature_extraction.text import TfidfVectorizer\n-from sklearn.multioutput import MultiOutputClassifier\n-from sklearn.pipeline import Pipeline\n-from sklearn.preprocessing import MaxAbsScaler\n-from sklearn.svm import LinearSVC\n-\n-from model.hyperparameters import ModelHyperparameters\n-from model.example import Example\n-from model.utils import labels_to_multihot, make_inference_text\n-\n-\n-logger = logging.getLogger(__name__)\n-RANDOM_SEED = 1337  # as is tradition\n-\n-\n-class TrainingConfig(BaseSettings):\n-    \"\"\"\n-    The set of configuration parameters required to run a training routine.\n-\n-    Values for these config fields can be provided as environment variables, see:\n-    `./docker.env`\n-    \"\"\"\n-\n-    artifacts_dir: str = Field(\n-        description=\"Directory to find model artifacts such as learned model parameters, etc\",\n-        default=\"/opt/ml/model\",\n-    )\n-    input_data_dir: str = Field(\n-        description=\"Directory to find data files\", default=\"/opt/ml/input/data\"\n-    )\n-    channel_name: str = Field(\n-        description=\"Name of data channel to use for training. Name of subdirectory under `input_data_dir`\",\n-        default=\"training\",\n-    )\n-    input_config_dir: str = Field(\n-        description=\"Directory to find configuration files like hyperparameters\",\n-        default=\"/opt/ml/input/config\",\n-    )\n-    hyperparameters_file: str = Field(\n-        description=\"Filename of hyperparameters json file, following specification in `model.hyperparameters`\"\n-    )\n-    model_version: Optional[str] = Field(\n-        description=\"Logical name for a model version or experiment, to segment and retrieve artifacts\",\n-        default=None,\n-    )\n-\n-    def training_data_dir(self) -> str:\n-        return os.path.join(self.input_data_dir, self.channel_name)\n-\n-    def target_artifacts_dir(self) -> str:\n-        if self.model_version is None or self.model_version == \"\":\n-            return self.artifacts_dir\n-\n-        return os.path.join(self.artifacts_dir, self.model_version)\n-\n-    def load_hyperparameters(self) -> ModelHyperparameters:\n-        filepath = os.path.join(self.input_config_dir, self.hyperparameters_file)\n-        return ModelHyperparameters.parse_file(filepath)\n-\n-\n-def build_and_train_model(\n-    training_examples: List[Example], hyperparameters: ModelHyperparameters\n-) -> Pipeline:\n-    \"\"\"\n-    From a set of training examples, fits an ngram TFIDF vectorizer and a multilabel SVM leveraging its output\n-    \"\"\"\n-\n-    vectorizer = TfidfVectorizer(\n-        strip_accents=\"ascii\",\n-        analyzer=\"char\",\n-        ngram_range=(\n-            hyperparameters.ngram_lower_bound,\n-            hyperparameters.ngram_upper_bound,\n-        ),\n-        max_features=hyperparameters.max_tfidf_features,\n-    )\n-    svm = LinearSVC(loss=\"squared_hinge\", C=hyperparameters.C, random_state=RANDOM_SEED)\n-    clf = MultiOutputClassifier(svm)\n-\n-    if hyperparameters.scale_features:\n-        steps = [\n-            (\"tfidf\", vectorizer),\n-            (\"scaler\", MaxAbsScaler(copy=False)),\n-            (\"classifier\", clf),\n-        ]\n-    else:\n-        steps = [(\"tfidf\", vectorizer), (\"classifier\", clf)]\n-\n-    final_model = Pipeline(steps=steps)\n-\n-    training_texts = [\n-        make_inference_text(ex.instance, hyperparameters.use_abstract)\n-        for ex in training_examples\n-    ]\n-    labels = np.array(\n-        [labels_to_multihot(ex.labels.foses) for ex in training_examples],\n-        dtype=np.uint,\n-    )\n-\n-    final_model.fit(training_texts, labels)\n-\n-    return final_model\ndiff --git a/model/utils.py b/model/utils.py\nindex c8bd868..89431a1 100644\n--- a/model/utils.py\n+++ b/model/utils.py\n@@ -69,64 +69,6 @@ def concat_text(instance: Instance, sep=\"|\", sep_num=5):\n     return f\"{title} {sep * sep_num} {abstract}\"\n \n \n-def labels_to_multihot(fields_of_study: List[str]) -> List[bool]:\n-    \"\"\"Generates a multi-hot vector for Fields of Studies\"\"\"\n-    label_set = set(fields_of_study)\n-    return [label in label_set for label in LABELS]\n-\n-\n-def multihot_to_labels(multihot: np.ndarray) -> List[str]:\n-    \"\"\"Converts model output vector to str label list\"\"\"\n-    labels = []\n-\n-    for index, label in enumerate(LABELS):\n-        if multihot[index] == 1:\n-            labels.append(label)\n-\n-    return labels\n-\n-\n-def load_labeled_data(labeled_data_dir: str) -> List[Example]:\n-    \"\"\"\n-    Given a directory of labeled data JSONL files, loads\n-    a list of examples.\n-    \"\"\"\n-\n-    examples: List[Example] = []\n-\n-    training_files = [\n-        f\n-        for f in os.listdir(labeled_data_dir)\n-        if os.path.isfile(os.path.join(labeled_data_dir, f)) and f.endswith(\".jsonl\")\n-    ]\n-\n-    for training_file in training_files:\n-        logger.info(\"Loading training data from `{f}`\")\n-        with open(os.path.join(labeled_data_dir, training_file), \"r\") as f:\n-            for line in f:\n-                example = Example.parse_raw(line)\n-                examples.append(example)\n-\n-    return examples\n-\n-\n-def save_model(\n-    target_dir: str, hyperparameters: ModelHyperparameters, classifier: Pipeline\n-) -> None:\n-    \"\"\"\n-    Saves out model hyperparameters and trained classifier to a target directory.\n-    \"\"\"\n-\n-    if not os.path.exists(target_dir):\n-        os.makedirs(target_dir)\n-\n-    with open(os.path.join(target_dir, HYPERPARAMETERS_FNAME), \"w\") as fhyper:\n-        fhyper.write(hyperparameters.json(indent=4))\n-\n-    with open(os.path.join(target_dir, CLASSIFIER_FNAME), \"wb\") as fclassifier:\n-        pickle.dump(classifier, fclassifier)\n-\n-\n def load_model(\n     artifacts_dir: str,\n ):\ndiff --git a/server/api.py b/server/api.py\nindex 0efa6f5..f9a8a90 100644\n--- a/server/api.py\n+++ b/server/api.py\n@@ -9,7 +9,6 @@ from pydantic import BaseModel, Field\n \n from model.instance import Instance\n from model.prediction import Prediction\n-from model.decisionscores import DecisionScores\n from model.predictor import Predictor, PredictorConfig\n \n \n@@ -52,7 +51,7 @@ def make_app(batch_size: int = 1):\n     class InvocationsResponse(BaseModel):\n         \"\"\"The results of inference over each passed instance\"\"\"\n \n-        predictions: List[DecisionScores] = Field(description=\"The decision scores\")\n+        predictions: List[Prediction] = Field(description=\"The decision scores\")\n \n     app = FastAPI()\n \n", "bytes": 14728, "truncated": false}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/entrypoint.py b/entrypoint.py\nindex 5bf22e8..8ce9b38 100644\n--- a/entrypoint.py\n+++ b/entrypoint.py\n@@ -1,3 +1,5 @@\n+import json\n+import logging\n import logging.config\n import os\n import signal\n@@ -6,6 +8,12 @@ import sys\n \n import click\n \n+from model.eval import EvalSettings, generate_metrics\n+from model.predictor import Predictor, PredictorConfig\n+from model.training import build_and_train_model, TrainingConfig\n+from model.utils import load_labeled_data, save_model\n+\n+\n dir = os.path.dirname(os.path.realpath(__file__))\n logging.config.fileConfig(os.path.join(dir, \"server\", \"logging.conf\"))\n logger = logging.getLogger(__name__)\n@@ -76,5 +84,65 @@ def serve():\n     logger.info(\"Inference server exiting\")\n \n \n+@cli.command()\n+def train():\n+    logger.info(\"BEGINNING TRAINING ROUTINE\")\n+\n+    training_config = TrainingConfig()\n+    hyperparameters = training_config.load_hyperparameters()\n+    training_examples = load_labeled_data(training_config.training_data_dir())\n+\n+    logger.info(\n+        f\"\"\"\n+        TRAINING WITH\n+        data channel: {training_config.channel_name}\n+        hyperparameters: {hyperparameters.json()}\n+    \"\"\"\n+    )\n+    trained_model = build_and_train_model(training_examples, hyperparameters)\n+\n+    logger.info(\"MODEL TRAINING COMPLETE, SAVING TO DISK\")\n+    logger.info(f\"MODEL VERSION: {training_config.model_version}\")\n+    save_model(training_config.target_artifacts_dir(), hyperparameters, trained_model)\n+    logger.info(\"MODEL SAVED, EXITING\")\n+\n+\n+@cli.command()\n+def evaluate():\n+    logger.info(\"BEGINNING EVAL ROUTINE\")\n+\n+    eval_settings = EvalSettings()\n+    eval_examples = load_labeled_data(eval_settings.eval_data_dir())\n+\n+    predictor_config = PredictorConfig()\n+    predictor = Predictor(predictor_config)\n+\n+    logger.info(\n+        f\"\"\"\n+        RUNNING EVAL FOR\n+        model version: {eval_settings.model_version}\n+        dataset: {eval_settings.channel_name}\n+    \"\"\"\n+    )\n+\n+    predictions = predictor.predict_batch([ex.instance for ex in eval_examples])\n+    metrics = generate_metrics(eval_examples, predictions)\n+\n+    metrics_dir = eval_settings.metrics_output_dir()\n+\n+    if not os.path.exists(metrics_dir):\n+        os.makedirs(metrics_dir)\n+\n+    logging.info(\n+        f\"\"\"\n+        METRICS:\n+        {metrics}\n+        \"\"\"\n+    )\n+\n+    with open(os.path.join(metrics_dir, \"metrics.json\"), \"w\") as f:\n+        f.write(json.dumps(metrics, indent=4))\n+\n+\n if __name__ == \"__main__\":\n     cli()\ndiff --git a/model/decisionscore.py b/model/decisionscore.py\ndeleted file mode 100644\nindex 09ad0d8..0000000\n--- a/model/decisionscore.py\n+++ /dev/null\n@@ -1,8 +0,0 @@\n-from pydantic import BaseModel, Field\n-\n-\n-class DecisionScore(BaseModel):\n-    \"\"\"Represents decision score predicted for a given field of study for a given paper\"\"\"\n-\n-    label: str\n-    score: float\ndiff --git a/model/decisionscores.py b/model/decisionscores.py\nnew file mode 100644\nindex 0000000..a5c57ca\n--- /dev/null\n+++ b/model/decisionscores.py\n@@ -0,0 +1,9 @@\n+from typing import List\n+\n+from pydantic import BaseModel, Field\n+\n+\n+class DecisionScores(BaseModel):\n+    \"\"\"Represents decision scores predicted for fields of study for a given paper\"\"\"\n+\n+    scores: dict = Field(description=\"Descision scores for all fields of study\")\ndiff --git a/model/eval.py b/model/eval.py\nnew file mode 100644\nindex 0000000..de0065f\n--- /dev/null\n+++ b/model/eval.py\n@@ -0,0 +1,55 @@\n+import os\n+from typing import Any, Dict, List, Optional\n+\n+from pydantic import BaseSettings, Field\n+\n+from model.example import Example\n+from model.prediction import Prediction\n+from model.decisionscores import DecisionScores\n+\n+\n+class EvalSettings(BaseSettings):\n+    artifacts_dir: str = Field(\n+        description=\"Directory to find model artifacts such as learned model parameters, etc\",\n+        default=\"/opt/ml/model\",\n+    )\n+    input_data_dir: str = Field(\n+        description=\"Directory to find data files\", default=\"/opt/ml/input/data\"\n+    )\n+    channel_name: str = Field(\n+        description=\"Name of data channel to run evaluation against. Name of subdirectory under `input_data_dir`\",\n+    )\n+    output_data_dir: str = Field(\n+        description=\"Directory to write metrics results into\", default=\"/opt/ml/output\"\n+    )\n+    model_version: Optional[str] = Field(\n+        description=\"Logical name for a model version or experiment, to segment and retrieve artifacts\",\n+        default=None,\n+    )\n+\n+    def eval_data_dir(self) -> str:\n+        return os.path.join(self.input_data_dir, self.channel_name)\n+\n+    def target_artifacts_dir(self) -> str:\n+        \"\"\"Specifies full directory path for location of model artifacts\"\"\"\n+\n+        if self.model_version is None or self.model_version == \"\":\n+            return self.artifacts_dir\n+\n+        return os.path.join(self.artifacts_dir, self.model_version)\n+\n+    def metrics_output_dir(self) -> str:\n+        \"\"\"Specifies full directory path for saving out evaluation metrics\"\"\"\n+\n+        if self.model_version is None or self.model_version == \"\":\n+            return os.path.join(self.artifacts_dir, self.channel_name)\n+\n+        return os.path.join(self.output_data_dir, self.model_version, self.channel_name)\n+\n+\n+def generate_metrics(\n+    eval_examples: List[Example], decisionscores: List[DecisionScores]\n+) -> Dict[str, Any]:\n+    # TODO: what metrics do we need to generate?\n+\n+    return {\"asdf\": \"fdsa\"}\ndiff --git a/model/prediction.py b/model/prediction.py\nindex 09dcc76..9317115 100644\n--- a/model/prediction.py\n+++ b/model/prediction.py\n@@ -1,12 +1,9 @@\n from typing import List\n \n-from model.decisionscore import DecisionScore\n from pydantic import BaseModel, Field\n \n \n class Prediction(BaseModel):\n-    \"\"\"Represents predicted scores for all fields of study for a given paper\"\"\"\n+    \"\"\"Represents predicted fields of study for a given paper\"\"\"\n \n-    scores: List[DecisionScore] = Field(\n-        description=\"Decision scores for all fields of study\"\n-    )\n+    foses: List[str] = Field(description=\"Predicted fields of study\")\ndiff --git a/model/predictor.py b/model/predictor.py\nindex 3a7fc35..568fb1a 100644\n--- a/model/predictor.py\n+++ b/model/predictor.py\n@@ -1,6 +1,6 @@\n from model.multioutput import MultiOutputClassifierWithDecision\n from model.prediction import Prediction\n-from model.decisionscore import DecisionScore\n+from model.decisionscores import DecisionScores\n from model.instance import Instance\n import logging\n import os\n@@ -72,7 +72,7 @@ class Predictor:\n         self.mlb_inverse_dict = {v: k for k, v in self._mlb._cached_dict.items()}\n \n     # works for single text at a time\n-    def get_decision_scores(self, original_text) -> List[DecisionScore]:\n+    def get_decision_scores(self, original_text):\n \n         # Skip the prediction process if the input text is not in english\n         is_english = utils.detect_language(self._fasttext, original_text)[1]\n@@ -83,19 +83,19 @@ class Predictor:\n         featurized_text = self._feature_pipe.transform([original_text])[0]\n         decision_scores = self._classifier.decision_function(featurized_text)\n \n-        # mlb_inverse_dict: {0: 'Agricultural and Food sciences', 1: 'Art', 2: 'Biology',... 21: 'Psychology', 22: 'Sociology'}\n-\n-        model_predictions: List[DecisionScore] = [\n-            DecisionScore(label=label, score=decision_scores[int(index)])\n+        model_predictions = {\n+            label: decision_scores[int(index)]\n             for index, label in self.mlb_inverse_dict.items()\n-        ]\n+        }\n \n         return model_predictions\n \n-    def predict_batch(self, instances: List[Instance]) -> List[Prediction]:\n+    def predict_batch(self, instances: List[Instance]) -> List[DecisionScores]:\n         texts = [\n             utils.make_inference_text(instance, self._hyperparameters.use_abstract)\n             for instance in instances\n         ]\n \n-        return [Prediction(scores=self.get_decision_scores(text)) for text in texts]\n+        return [\n+            DecisionScores(scores=self.get_decision_scores(text)) for text in texts\n+        ]\ndiff --git a/model/training.py b/model/training.py\nnew file mode 100644\nindex 0000000..cc0d969\n--- /dev/null\n+++ b/model/training.py\n@@ -0,0 +1,108 @@\n+import logging\n+import os\n+from typing import List, Optional, Tuple\n+\n+import numpy as np\n+from pydantic import BaseModel, BaseSettings, Field\n+from sklearn.feature_extraction.text import TfidfVectorizer\n+from sklearn.multioutput import MultiOutputClassifier\n+from sklearn.pipeline import Pipeline\n+from sklearn.preprocessing import MaxAbsScaler\n+from sklearn.svm import LinearSVC\n+\n+from model.hyperparameters import ModelHyperparameters\n+from model.example import Example\n+from model.utils import labels_to_multihot, make_inference_text\n+\n+\n+logger = logging.getLogger(__name__)\n+RANDOM_SEED = 1337  # as is tradition\n+\n+\n+class TrainingConfig(BaseSettings):\n+    \"\"\"\n+    The set of configuration parameters required to run a training routine.\n+\n+    Values for these config fields can be provided as environment variables, see:\n+    `./docker.env`\n+    \"\"\"\n+\n+    artifacts_dir: str = Field(\n+        description=\"Directory to find model artifacts such as learned model parameters, etc\",\n+        default=\"/opt/ml/model\",\n+    )\n+    input_data_dir: str = Field(\n+        description=\"Directory to find data files\", default=\"/opt/ml/input/data\"\n+    )\n+    channel_name: str = Field(\n+        description=\"Name of data channel to use for training. Name of subdirectory under `input_data_dir`\",\n+        default=\"training\",\n+    )\n+    input_config_dir: str = Field(\n+        description=\"Directory to find configuration files like hyperparameters\",\n+        default=\"/opt/ml/input/config\",\n+    )\n+    hyperparameters_file: str = Field(\n+        description=\"Filename of hyperparameters json file, following specification in `model.hyperparameters`\"\n+    )\n+    model_version: Optional[str] = Field(\n+        description=\"Logical name for a model version or experiment, to segment and retrieve artifacts\",\n+        default=None,\n+    )\n+\n+    def training_data_dir(self) -> str:\n+        return os.path.join(self.input_data_dir, self.channel_name)\n+\n+    def target_artifacts_dir(self) -> str:\n+        if self.model_version is None or self.model_version == \"\":\n+            return self.artifacts_dir\n+\n+        return os.path.join(self.artifacts_dir, self.model_version)\n+\n+    def load_hyperparameters(self) -> ModelHyperparameters:\n+        filepath = os.path.join(self.input_config_dir, self.hyperparameters_file)\n+        return ModelHyperparameters.parse_file(filepath)\n+\n+\n+def build_and_train_model(\n+    training_examples: List[Example], hyperparameters: ModelHyperparameters\n+) -> Pipeline:\n+    \"\"\"\n+    From a set of training examples, fits an ngram TFIDF vectorizer and a multilabel SVM leveraging its output\n+    \"\"\"\n+\n+    vectorizer = TfidfVectorizer(\n+        strip_accents=\"ascii\",\n+        analyzer=\"char\",\n+        ngram_range=(\n+            hyperparameters.ngram_lower_bound,\n+            hyperparameters.ngram_upper_bound,\n+        ),\n+        max_features=hyperparameters.max_tfidf_features,\n+    )\n+    svm = LinearSVC(loss=\"squared_hinge\", C=hyperparameters.C, random_state=RANDOM_SEED)\n+    clf = MultiOutputClassifier(svm)\n+\n+    if hyperparameters.scale_features:\n+        steps = [\n+            (\"tfidf\", vectorizer),\n+            (\"scaler\", MaxAbsScaler(copy=False)),\n+            (\"classifier\", clf),\n+        ]\n+    else:\n+        steps = [(\"tfidf\", vectorizer), (\"classifier\", clf)]\n+\n+    final_model = Pipeline(steps=steps)\n+\n+    training_texts = [\n+        make_inference_text(ex.instance, hyperparameters.use_abstract)\n+        for ex in training_examples\n+    ]\n+    labels = np.array(\n+        [labels_to_multihot(ex.labels.foses) for ex in training_examples],\n+        dtype=np.uint,\n+    )\n+\n+    final_model.fit(training_texts, labels)\n+\n+    return final_model\ndiff --git a/model/utils.py b/model/utils.py\nindex 89431a1..c8bd868 100644\n--- a/model/utils.py\n+++ b/model/utils.py\n@@ -69,6 +69,64 @@ def concat_text(instance: Instance, sep=\"|\", sep_num=5):\n     return f\"{title} {sep * sep_num} {abstract}\"\n \n \n+def labels_to_multihot(fields_of_study: List[str]) -> List[bool]:\n+    \"\"\"Generates a multi-hot vector for Fields of Studies\"\"\"\n+    label_set = set(fields_of_study)\n+    return [label in label_set for label in LABELS]\n+\n+\n+def multihot_to_labels(multihot: np.ndarray) -> List[str]:\n+    \"\"\"Converts model output vector to str label list\"\"\"\n+    labels = []\n+\n+    for index, label in enumerate(LABELS):\n+        if multihot[index] == 1:\n+            labels.append(label)\n+\n+    return labels\n+\n+\n+def load_labeled_data(labeled_data_dir: str) -> List[Example]:\n+    \"\"\"\n+    Given a directory of labeled data JSONL files, loads\n+    a list of examples.\n+    \"\"\"\n+\n+    examples: List[Example] = []\n+\n+    training_files = [\n+        f\n+        for f in os.listdir(labeled_data_dir)\n+        if os.path.isfile(os.path.join(labeled_data_dir, f)) and f.endswith(\".jsonl\")\n+    ]\n+\n+    for training_file in training_files:\n+        logger.info(\"Loading training data from `{f}`\")\n+        with open(os.path.join(labeled_data_dir, training_file), \"r\") as f:\n+            for line in f:\n+                example = Example.parse_raw(line)\n+                examples.append(example)\n+\n+    return examples\n+\n+\n+def save_model(\n+    target_dir: str, hyperparameters: ModelHyperparameters, classifier: Pipeline\n+) -> None:\n+    \"\"\"\n+    Saves out model hyperparameters and trained classifier to a target directory.\n+    \"\"\"\n+\n+    if not os.path.exists(target_dir):\n+        os.makedirs(target_dir)\n+\n+    with open(os.path.join(target_dir, HYPERPARAMETERS_FNAME), \"w\") as fhyper:\n+        fhyper.write(hyperparameters.json(indent=4))\n+\n+    with open(os.path.join(target_dir, CLASSIFIER_FNAME), \"wb\") as fclassifier:\n+        pickle.dump(classifier, fclassifier)\n+\n+\n def load_model(\n     artifacts_dir: str,\n ):\ndiff --git a/server/api.py b/server/api.py\nindex f9a8a90..0efa6f5 100644\n--- a/server/api.py\n+++ b/server/api.py\n@@ -9,6 +9,7 @@ from pydantic import BaseModel, Field\n \n from model.instance import Instance\n from model.prediction import Prediction\n+from model.decisionscores import DecisionScores\n from model.predictor import Predictor, PredictorConfig\n \n \n@@ -51,7 +52,7 @@ def make_app(batch_size: int = 1):\n     class InvocationsResponse(BaseModel):\n         \"\"\"The results of inference over each passed instance\"\"\"\n \n-        predictions: List[Prediction] = Field(description=\"The decision scores\")\n+        predictions: List[DecisionScores] = Field(description=\"The decision scores\")\n \n     app = FastAPI()\n \ndiff --git a/tests/model/test_training.py b/tests/model/test_training.py\nnew file mode 100644\nindex 0000000..67d35b6\n--- /dev/null\n+++ b/tests/model/test_training.py\n@@ -0,0 +1,38 @@\n+import os\n+import unittest\n+\n+from model.training import TrainingConfig\n+from model.hyperparameters import ModelHyperparameters\n+\n+\n+FIXTURE_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), \"fixtures\")\n+\n+\n+class TestTrainingConfig(unittest.TestCase):\n+    def test_training_data_dir(self):\n+        config = TrainingConfig(input_data_dir=\"/asdf/fdsa\", channel_name=\"foobar\")\n+        self.assertEqual(\"/asdf/fdsa/foobar\", config.training_data_dir())\n+\n+    def test_target_artifacts_dir(self):\n+        config = TrainingConfig(artifacts_dir=\"/some/artifacts\", model_version=None)\n+        self.assertEqual(\"/some/artifacts\", config.target_artifacts_dir())\n+\n+        config = TrainingConfig(artifacts_dir=\"/some/artifacts\", model_version=\"\")\n+        self.assertEqual(\"/some/artifacts\", config.target_artifacts_dir())\n+\n+    def test_load_hyperparameters(self):\n+        config = TrainingConfig(\n+            input_config_dir=FIXTURE_DIR,\n+            hyperparameters_file=\"example_hyperparameters.json\",\n+        )\n+        hyperparameters = config.load_hyperparameters()\n+        expected = ModelHyperparameters(\n+            ngram_lower_bound=1,\n+            ngram_upper_bound=3,\n+            max_tfidf_features=10000,\n+            scale_features=False,\n+            use_abstract=True,\n+            C=1.2,\n+        )\n+\n+        self.assertEqual(expected, hyperparameters)\ndiff --git a/tests/model/test_utils.py b/tests/model/test_utils.py\nindex f309471..2df44a7 100644\n--- a/tests/model/test_utils.py\n+++ b/tests/model/test_utils.py\n@@ -1,7 +1,12 @@\n import os\n import unittest\n \n+import numpy as np\n+\n+from model.example import Example\n from model.instance import Instance\n+from model.labels import LABELS\n+from model.prediction import Prediction\n from model import utils\n \n \n@@ -22,4 +27,69 @@ class TestUtils(unittest.TestCase):\n     def test_make_inference_text__yield_concatenated_text(self):\n         instance = Instance(title=\"asdf\", abstract=\"fdsa\")\n \n-        self.assertEqual(utils.make_inference_text(instance, True), \"asdf ||||| fdsa\")\n+        self.assertEqual(utils.make_inference_text(instance, True), \"asdf fdsa\")\n+\n+    def test_labels_to_multihot(self):\n+        labels = [\"Philosophy\", \"Geology\", \"Economics\", \"Art\"]\n+\n+        multihot = utils.labels_to_multihot(labels)\n+\n+        self.assertEqual(\n+            [\n+                True,\n+                False,\n+                False,\n+                False,\n+                False,\n+                True,\n+                False,\n+                False,\n+                False,\n+                False,\n+                True,\n+                False,\n+                False,\n+                False,\n+                False,\n+                False,\n+                False,\n+                True,\n+                False,\n+                False,\n+                False,\n+                False,\n+            ],\n+            multihot,\n+        )\n+\n+    def test_multihot_to_labels(self):\n+        allhot = np.array([1] * len(LABELS))\n+        self.assertEqual(LABELS, utils.multihot_to_labels(allhot))\n+\n+        nonehot = np.array([0] * len(LABELS))\n+        self.assertEqual([], utils.multihot_to_labels(nonehot))\n+\n+        halfhot = np.array(\n+            ([1] * (len(LABELS) // 2)) + ([0] * (len(LABELS) - len(LABELS) // 2))\n+        )\n+        self.assertEqual(LABELS[: len(LABELS) // 2], utils.multihot_to_labels(halfhot))\n+\n+    def test_load_labeled_data(self):\n+        fixture_data = os.path.join(FIXTURE_DIR, \"example_labeled_data\")\n+        results = utils.load_labeled_data(fixture_data)\n+        expected = [\n+            Example(\n+                instance=Instance(title=\"asdf\", abstract=None),\n+                labels=Prediction(foses=[\"Art\"]),\n+            ),\n+            Example(\n+                instance=Instance(title=\"fdsa\", abstract=\"some abstract\"),\n+                labels=Prediction(foses=[\"Biology\", \"Sociology\"]),\n+            ),\n+            Example(\n+                instance=Instance(title=\"qwer\", abstract=\"some other abstract\"),\n+                labels=Prediction(foses=[\"Psychology\", \"Business\"]),\n+            ),\n+        ]\n+\n+        self.assertEqual(expected, results)\n", "bytes": 19093, "truncated": false}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    build-essential \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    python3-dev \\\n    libffi-dev \\\n    libssl-dev \\\n    wget \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone https://github.com/allenai/s2_fos.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 2d83b7d302f9881dc8d3736894a39d0d11be15ad || git fetch --depth 1 origin \"+refs/pull/8/head:refs/remotes/origin/pr/8\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\nENV CI=true\nENV SKLEARN_ALLOW_DEPRECATED_SKLEARN_PACKAGE_INSTALL=True\nRUN uv venv /opt/venv && \\\n    uv pip install --python /opt/venv/bin/python setuptools wheel && \\\n    uv pip install --python /opt/venv/bin/python \\\n        pydantic \\\n        scikit-learn \\\n        \"numpy<2\" \\\n        pandas \\\n        text-unidecode \\\n        pybind11 \\\n        fasttext \\\n        pycld2 \\\n        uvicorn \\\n        python-json-logger \\\n        click \\\n    && \\\n    uv pip install --python /opt/venv/bin/python --no-deps --no-build-isolation -e . && \\\n    uv pip install --python /opt/venv/bin/python pytest\n\nRUN git reset --hard\n\nCOPY bug.patch /tmp/bug.patch\nRUN patch -p1 < /tmp/bug.patch && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 1520, "truncated": false}]}, {"task_name": "anssi-fr__orc2timeline-5", "repo": "anssi-fr/orc2timeline", "language": "Python", "difficulty": "hard", "area": "backend", "topic": "forensics", "bug_class": "data-parsing", "parts": [{"label": "Instruction", "file": "instruction.md", "text": "The orc2timeline tool processes ORC (Open Research Collection) artifact archives to generate forensic timelines. This update introduces improved reliability in artifact parsing to ensure accurate processing and timeline generation. Additionally, new parsing plugins have been added for:\n\n1. Firefox history (places.sqlite files) - Extract browsing history data.\n2. Recycle Bin data (resident files related to deleted items) - Recover details of deleted files.\n3. UserAssist registry keys - Provide evidence of program execution based on Windows registry entries.\n4. AmCache data - Extract details about installed programs and drivers.\n\nThe core `process` function accepts a list of file paths (e.g., 7z archives), an output CSV path, a machine name, and a job count for parallel processing. It must handle various archive types and null values in CSV files gracefully, ensuring the output is consistent regardless of the number of processing jobs used.\n\nDevelopers need to implement the parsing logic for these new data sources (Firefox history, Recycle Bin, UserAssist, AmCache) and ensure the main processing pipeline correctly integrates them into the timeline output. The implementation should be robust against malformed data or null values in the source artifacts to maintain output integrity.", "bytes": 1299, "truncated": false}, {"label": "Verifier", "file": "tests/test.sh", "text": "#!/bin/bash\nset -euo pipefail\n\ncd /app/src\n\nexport CI=true\nsource /opt/venv/bin/activate\n\n# Copy HEAD test data and test code from /tests mount into the working tree\nmkdir -p tests/data/conf_7_archives tests/data/null_csv tests\ncp \"/tests/data/conf_7_archives/\"*.7z \"tests/data/conf_7_archives/\" 2>/dev/null || true\ncp \"/tests/data/null_csv/\"*.7z \"tests/data/null_csv/\" 2>/dev/null || true\ncp \"/tests/test_cli.py\" \"tests/test_cli.py\"\ncp \"/tests/test_core.py\" \"tests/test_core.py\"\n\n# Run only the specific test files from this PR\nset +e\npytest -xvs tests/test_cli.py tests/test_core.py\ntest_status=$?\nset -e\n\nif [ $test_status -eq 0 ]; then\n  echo 1 > /logs/verifier/reward.txt\nelse\n  echo 0 > /logs/verifier/reward.txt\nfi\nexit \"$test_status\"\n", "bytes": 742, "truncated": false}, {"label": "Reference fix", "file": "solution/fix.patch", "text": "diff --git a/src/orc2timeline/conf/Orc2Timeline.yaml b/src/orc2timeline/conf/Orc2Timeline.yaml\nindex 789cf3c..6a201c7 100644\n--- a/src/orc2timeline/conf/Orc2Timeline.yaml\n+++ b/src/orc2timeline/conf/Orc2Timeline.yaml\n@@ -28,3 +28,27 @@ Plugins:\n       sub_archives: [\"NTFSInfo_i30Info.7z\"]\n       match_pattern: \"^I30Info.*\\\\.csv$\"\n       sourcetype: \"I30\"\n+\n+  - FirefoxHistoryToTimeline:\n+      archives: [\"Browsers\", \"General\", \"Offline\"]\n+      sub_archives: [\"Browsers_history.7z\", \"GetBrowsers_History.7z\", \"Browsers_complet.7z\"]\n+      match_pattern: \".*places\\\\.sqlite.*data$\"\n+      sourcetype: \"FirefoxHistory\"\n+\n+  - RecycleBinToTimeline:\n+      archives: [\"General\"]\n+      sub_archives: [\"Residents.7z\"]\n+      match_pattern: \"(.*fichiers_residents/.*_\\\\$I.*data$)\"\n+      sourcetype: \"RecycleBin\"\n+\n+  - UserAssistToTimeline:\n+      archives: [\"Detail\", \"Offline\"]\n+      sub_archives: [\"UserHives.7z\"]\n+      match_pattern: \".*NTUSER\\\\.DAT.*\"\n+      sourcetype: \"UserAssist\"\n+\n+  - AmCacheToTimeline:\n+      archives: [\"Little\", \"Detail\", \"Offline\"]\n+      sub_archives: [\"SystemHives_little.7z\", \"SystemHives.7z\"]\n+      match_pattern: \".*AmCache.hve.*data$\"\n+      sourcetype: \"AmCache\"\ndiff --git a/src/orc2timeline/core.py b/src/orc2timeline/core.py\nindex 7ea40aa..e37539d 100644\n--- a/src/orc2timeline/core.py\n+++ b/src/orc2timeline/core.py\n@@ -31,7 +31,7 @@ def _add_header_to_csv_file(output_path: str) -> None:\n     \"\"\"Add header at the beginning of csv file.\"\"\"\n     header = [\"Timestamp\", \"Hostname\", \"SourceType\", \"Description\", \"SourceFile\"]\n     with gzip.open(output_path, \"wt\", newline=\"\") as f:\n-        csv_dict_writer = csv.DictWriter(f, delimiter=\",\", quotechar='\"', fieldnames=header)\n+        csv_dict_writer = csv.DictWriter(f, delimiter=\",\", quotechar='\"', fieldnames=header, escapechar=\"`\")\n         csv_dict_writer.writeheader()\n \n \ndiff --git a/src/orc2timeline/plugins/AmCacheToTimeline.py b/src/orc2timeline/plugins/AmCacheToTimeline.py\nnew file mode 100644\nindex 0000000..4e22674\n--- /dev/null\n+++ b/src/orc2timeline/plugins/AmCacheToTimeline.py\n@@ -0,0 +1,221 @@\n+\"\"\"Plugin to parse AmCache Registry Hives.\"\"\"\n+\n+from __future__ import annotations\n+\n+from datetime import datetime, timezone\n+from typing import TYPE_CHECKING, Any\n+\n+from dfwinreg import regf as dfwinreg_regf\n+from dfwinreg import registry as dfwinreg_registry\n+\n+if TYPE_CHECKING:\n+    from pathlib import Path\n+    from threading import Lock\n+\n+    from orc2timeline.config import PluginConfig\n+\n+from orc2timeline.plugins.GenericToTimeline import Event\n+from orc2timeline.plugins.RegistryToTimeline import RegistryToTimeline\n+\n+EPOCH_AS_FILETIME = 116444736000000000\n+HUNDREDS_OF_NANOSECONDS = 10000000\n+\n+\n+class AmCacheToTimeline(RegistryToTimeline):\n+    def __init__(\n+        self,\n+        config: PluginConfig,\n+        orclist: list[str],\n+        output_file_path: str,\n+        hostname: str,\n+        tmp_dir: str,\n+        lock: Lock,\n+    ) -> None:\n+        \"\"\"Construct.\"\"\"\n+        super().__init__(config, orclist, output_file_path, hostname, tmp_dir, lock)\n+\n+    def _parse_inventory_application_file(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n+        event = Event(\n+            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n+            source=self.originalPath[artefact.name],\n+        )\n+        desc = [f\"KeyPath: {key.path}\"]\n+        filename = key.GetValueByName(\"Name\")\n+        if filename:\n+            desc.append(f\"Name: {filename.GetDataAsObject()}\")\n+        path = key.GetValueByName(\"LowerCaseLongPath\")\n+        if path:\n+            desc.append(f\"ExecPath: {path.GetDataAsObject()}\")\n+        file_id = key.GetValueByName(\"FileId\")\n+        if file_id:\n+            desc.append(f\"SHA1: {file_id.GetDataAsObject()[4:]}\")\n+        file_size = key.GetValueByName(\"Size\")\n+        if file_size:\n+            desc.append(f\"FileSize: {file_size.GetDataAsObject()}\")\n+        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n+        self._add_event(event)\n+        # check if there is a compilation date to add another line\n+        lnkdate = key.GetValueByName(\"LinkDate\")\n+        if lnkdate:\n+            epoch_timestamp = datetime.strptime(\n+                lnkdate.GetDataAsObject(),\n+                \"%m/%d/%Y %H:%M:%S\",\n+            ).replace(tzinfo=timezone.utc)\n+            eventlnk = Event(\n+                timestamp=epoch_timestamp,\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventlnk.description = \"Compilation timestamp - \" + \" - \".join(desc)\n+            self._add_event(eventlnk)\n+\n+    def _parse_inventory_driver_binary(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n+        event = Event(\n+            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n+            source=self.originalPath[artefact.name],\n+        )\n+        desc = [f\"KeyPath: {key.path}\"]\n+        filename = key.GetValueByName(\"DriverName\")\n+        if filename:\n+            desc.append(f\"Name: {filename.GetDataAsObject()}\")\n+        path = key.GetValueByName(\"LowerCaseLongPath\")\n+        if path:\n+            desc.append(f\"DriverPath: {path.GetDataAsObject()}\")\n+        file_id = key.GetValueByName(\"DriverId\")\n+        # sha1 can be either in one of the value or the name of the key\n+        if file_id:\n+            desc.append(f\"SHA1: {file_id.GetDataAsObject()[4:]}\")\n+        elif key.name.startswith(\"0000\"):\n+            desc.append(f\"SHA1: {key.name[4:]}\")\n+        file_size = key.GetValueByName(\"ImageSize\")\n+        if file_size:\n+            desc.append(f\"FileSize: {file_size.GetDataAsObject()}\")\n+        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n+        self._add_event(event)\n+        # check if there is a compilation date to add another line\n+        dlwt = key.GetValueByName(\"DriverLastWriteTime\")\n+        if dlwt:\n+            eventlnk = Event(\n+                timestamp=datetime.strptime(dlwt.GetDataAsObject(), \"%m/%d/%Y %H:%M:%S\").replace(tzinfo=timezone.utc),\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventlnk.description = \"Driver Last Write time - \" + \" - \".join(desc)\n+            self._add_event(eventlnk)\n+\n+    def _parse_file_key(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n+        event = Event(\n+            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n+            source=self.originalPath[artefact.name],\n+        )\n+        desc = [f\"KeyPath: {key.path}\"]\n+        path = key.GetValueByName(\"15\")\n+        if path:\n+            desc.append(f\"ExecPath: {path.GetDataAsObject()}\")\n+        file_id = key.GetValueByName(\"101\")\n+        if file_id:\n+            desc.append(f\"SHA1: {file_id.GetDataAsObject()[4:]}\")\n+        file_size = key.GetValueByName(\"6\")\n+        if file_size:\n+            desc.append(f\"FileSize: {file_size.GetDataAsObject()}\")\n+        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n+        self._add_event(event)\n+        # check if there is a modification date to add another line\n+        moddate = key.GetValueByName(\"17\")\n+        if moddate:\n+            epoch_timestamp = (int(moddate.GetDataAsObject()) - EPOCH_AS_FILETIME) / HUNDREDS_OF_NANOSECONDS\n+            eventmod = Event(\n+                timestamp=datetime.fromtimestamp(epoch_timestamp, tz=timezone.utc),\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventmod.description = \"Modification time - \" + \" - \".join(desc)\n+            self._add_event(eventmod)\n+        # check if there is a creation date to add another line\n+        moddate = key.GetValueByName(\"12\")\n+        if moddate:\n+            epoch_timestamp = (int(moddate.GetDataAsObject()) - EPOCH_AS_FILETIME) / HUNDREDS_OF_NANOSECONDS\n+            eventbirth = Event(\n+                timestamp=datetime.fromtimestamp(epoch_timestamp, tz=timezone.utc),\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventbirth.description = \"Creation time - \" + \" - \".join(desc)\n+            self._add_event(eventbirth)\n+        # check if there is a compilation date to add another line\n+        lnkdate = key.GetValueByName(\"f\")\n+        if lnkdate:\n+            eventlnk = Event(\n+                timestamp=datetime.fromtimestamp(int(lnkdate.GetDataAsObject()), timezone.utc),\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventlnk.description = \"Compilation time - \" + \" - \".join(desc)\n+            self._add_event(eventlnk)\n+\n+    def _parse_programs_key(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n+        event = Event(\n+            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n+            source=self.originalPath[artefact.name],\n+        )\n+        desc = [f\"KeyPath: {key.path}\"]\n+        name = key.GetValueByName(\"0\")\n+        if name:\n+            desc.append(f\"Name: {name.GetDataAsObject()}\")\n+        version = key.GetValueByName(\"1\")\n+        if version:\n+            desc.append(f\"Version: {version.GetDataAsObject()}\")\n+        publisher = key.GetValueByName(\"2\")\n+        if publisher:\n+            desc.append(f\"Publisher: {publisher.GetDataAsObject()}\")\n+        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n+        self._add_event(event)\n+        # check if there is an installation date to add another line\n+        installdate = key.GetValueByName(\"a\")\n+        if installdate:\n+            eventinst = Event(\n+                timestamp=datetime.fromtimestamp(int(installdate.GetDataAsObject()), timezone.utc),\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventinst.description = \"Installation time - \" + \" - \".join(desc)\n+            self._add_event(eventinst)\n+        # check if there is a creation date to add another line\n+        uninstalldate = key.GetValueByName(\"b\")\n+        if uninstalldate and int(uninstalldate.GetDataAsObject()) != 0:\n+            eventuninst = Event(\n+                timestamp=datetime.fromtimestamp(int(uninstalldate.GetDataAsObject()), timezone.utc),\n+                source=self.originalPath[artefact.name],\n+            )\n+            eventuninst.description = \"Uninstallation time - \" + \" - \".join(desc)\n+            self._add_event(eventuninst)\n+\n+    def _parse_artefact(self, artefact: Path) -> None:\n+        with artefact.open(\"rb\") as f:\n+            try:\n+                reg_file = dfwinreg_regf.REGFWinRegistryFile(emulate_virtual_keys=False)\n+                reg_file.Open(f)\n+                win_registry = dfwinreg_registry.WinRegistry()\n+                key_path_prefix = win_registry.GetRegistryFileMapping(reg_file)\n+                reg_file.SetKeyPathPrefix(key_path_prefix)\n+                inventory_application_file = reg_file.GetKeyByPath(\n+                    \"\\\\Root\\\\InventoryApplicationFile\",\n+                )\n+                if inventory_application_file:\n+                    for iaf in inventory_application_file.RecurseKeys():\n+                        self._parse_inventory_application_file(iaf, artefact)\n+                inventory_driver_binary = reg_file.GetKeyByPath(\n+                    \"\\\\Root\\\\InventoryDriverBinary\",\n+                )\n+                if inventory_driver_binary:\n+                    for idb in inventory_driver_binary.RecurseKeys():\n+                        self._parse_inventory_driver_binary(idb, artefact)\n+                file_key = reg_file.GetKeyByPath(\n+                    \"\\\\Root\\\\File\",\n+                )\n+                if file_key:\n+                    for disk_guid in file_key.RecurseKeys():\n+                        for entry in disk_guid.RecurseKeys():\n+                            self._parse_file_key(entry, artefact)\n+                programs_key = reg_file.GetKeyByPath(\n+                    \"\\\\Root\\\\Programs\",\n+                )\n+                if programs_key:\n+                    for entry in programs_key.RecurseKeys():\n+                        self._parse_programs_key(entry, artefact)\n+            except Exception as e:  # noqa: BLE001\n+                self.logger.warning(\"Error while parsing %s : %s\", artefact.name, e)\ndiff --git a/src/orc2timeline/plugins/FirefoxHistoryToTimeline.py b/src/orc2timeline/plugins/FirefoxHistoryToTimeline.py\nnew file mode 100644\nindex 0000000..291ff35\n--- /dev/null\n+++ b/src/orc2timeline/plugins/FirefoxHistoryToTimeline.py\n@@ -0,0 +1,132 @@\n+\"\"\"Generic plugin to parse Firefox histories (places.sqlite).\"\"\"\n+\n+from __future__ import annotations\n+\n+import sqlite3\n+from datetime import datetime, timedelta\n+from typing import TYPE_CHECKING\n+\n+import pytz\n+\n+if TYPE_CHECKING:\n+    from pathlib import Path\n+    from threading import Lock\n+\n+    from orc2timeline.config import PluginConfig\n+\n+from orc2timeline.plugins.GenericToTimeline import Event, GenericToTimeline\n+\n+\n+def _reverse_hostname(hostname: str) -> str:\n+\n+    if not hostname:\n+        return \"\"\n+\n+    if len(hostname) <= 1:\n+        return hostname\n+\n+    if hostname[-1] == \".\":\n+        return hostname[::-1][1:]\n+\n+    return hostname[::-1][0:]\n+\n+\n+class FirefoxHistoryToTimeline(GenericToTimeline):\n+    def __init__(\n+        self,\n+        config: PluginConfig,\n+        orclist: list[str],\n+        output_file_path: str,\n+        hostname: str,\n+        tmp_dir: str,\n+        lock: Lock,\n+    ) -> None:\n+        \"\"\"Construct.\"\"\"\n+        super().__init__(config, orclist, output_file_path, hostname, tmp_dir, lock)\n+\n+        self.schema: dict[str, str] = {}\n+        self.columns_per_table: dict[str, list[str]] = {}\n+        self.required_structure = {\n+            \"moz_places\": frozenset([\"url\", \"title\", \"visit_count\", \"rev_host\", \"hidden\", \"typed\", \"id\"]),\n+            \"moz_historyvisits\": frozenset([\"id\", \"visit_date\", \"from_visit\", \"visit_type\", \"place_id\"]),\n+            \"moz_bookmarks\": frozenset([\"type\", \"title\", \"dateAdded\", \"lastModified\", \"id\", \"fk\"]),\n+            \"moz_items_annos\": frozenset([\"content\", \"dateAdded\", \"lastModified\", \"id\", \"item_id\"]),\n+        }\n+\n+    def _get_schema(self, curr: sqlite3.Cursor) -> None:\n+        sql_req = (\n+            'SELECT tbl_name, sql FROM sqlite_master WHERE type = \"table\" AND '\n+            'tbl_name != \"xp_proc\" AND tbl_name != \"sqlite_sequence\"'\n+        )\n+        sql_results = curr.execute(sql_req)\n+        self.schema = {table_name: \" \".join(query.split()) for table_name, query in sql_results}\n+\n+    def _validate_db(self) -> bool:\n+        if not self.required_structure:\n+            return True\n+\n+        for required_table, required_columns in self.required_structure.items():\n+            if required_table not in self.schema:\n+                return False\n+\n+            ze_table = self.columns_per_table.get(required_table)\n+            if ze_table is not None and (not frozenset(required_columns).issubset(ze_table)):\n+                return False\n+\n+        return True\n+\n+    def _parse_visited_row(self, curr: sqlite3.Cursor, sqlite_path: Path) -> None:\n+\n+        query = (\n+            \"SELECT moz_historyvisits.id, moz_places.url, moz_places.title, \"\n+            \"moz_places.visit_count, moz_historyvisits.visit_date, \"\n+            \"(select moz_places.url from moz_places where moz_historyvisits.from_visit=moz_places.id) as referer_name,\"\n+            \"moz_historyvisits.from_visit, moz_places.rev_host, \"\n+            \"moz_places.hidden, moz_places.typed, moz_historyvisits.visit_type \"\n+            \"FROM moz_places, moz_historyvisits \"\n+            \"WHERE moz_places.id = moz_historyvisits.place_id\"\n+        )\n+\n+        curr.execute(query)\n+        sql_result = curr.fetchall()\n+        col_names = [desc[0] for desc in curr.description]\n+\n+        for row in sql_result:\n+            row_dict = {col_names[i]: row[i] for i in range(len(col_names))}  # trick to create a dict from sql result\n+            description = (\n+                f\"Url: {row_dict['url']} - \"\n+                f\"Title: {row_dict['title']} - \"\n+                f\"Count: {row_dict['visit_count']} - \"\n+                f\"Typed: {row_dict['typed']} - \"\n+                f\"Referer: {row_dict['referer_name']}\"\n+            )\n+            # typed = tapé manuellement ?\n+            # hidden\n+            # referer => résoudre\n+            epoch = datetime(1970, 1, 1, tzinfo=pytz.utc)\n+            timestamp = epoch + timedelta(microseconds=int(row_dict[\"visit_date\"]))\n+            event = Event(timestamp=timestamp, source=self._get_original_path(sqlite_path), description=description)\n+            self._add_event(event)\n+\n+    def _parse_artefact(self, artefact: Path) -> None:\n+        if \"places.sqlite-wal\" in artefact.stem or \"places.sqlite-shm\" in artefact.stem:\n+            return\n+\n+        try:\n+            conn = sqlite3.connect(artefact)\n+            curr = conn.cursor()\n+\n+            self._get_schema(curr)\n+            for table_name in self.schema:\n+                self.columns_per_table.setdefault(table_name, [])\n+                pragma_results = curr.execute(f'PRAGMA table_info(\"{table_name:s}\")')\n+                for pragma_result in pragma_results:\n+                    self.columns_per_table[table_name].append(pragma_result[1])\n+\n+            if self._validate_db():\n+                self._parse_visited_row(curr, artefact)\n+\n+        except sqlite3.DatabaseError:\n+            self.logger.exception(\"%s is not a valid databse\", artefact)\n+        except Exception:\n+            raise\ndiff --git a/src/orc2timeline/plugins/GenericToTimeline.py b/src/orc2timeline/plugins/GenericToTimeline.py\nindex 5db1c91..41d532e 100644\n--- a/src/orc2timeline/plugins/GenericToTimeline.py\n+++ b/src/orc2timeline/plugins/GenericToTimeline.py\n@@ -246,7 +246,7 @@ class GenericToTimeline:\n         self.logger.critical(\"Reinitialization of chunks\")\n \n         self.current_chunk = SortedChunk(10000)\n-        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"')\n+        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"', escapechar=\"`\")\n         self.output_files_list = []\n         self._setup_next_output_file()\n \n@@ -375,7 +375,10 @@ class GenericToTimeline:\n         file.\n         \"\"\"\n         # sanitize output\n-        rows_to_write = [row.replace(\"\\n\", \"\\\\n\") for row in (date, self.hostname, sourcetype, description, sourcefile)]\n+        rows_to_write = [\n+            row.replace(\"\\n\", \"\\\\n\").replace(\"\\r\", \"\\\\r\")\n+            for row in (date, self.hostname, sourcetype, description, sourcefile)\n+        ]\n         self.csvWriter.writerow(rows_to_write)\n         if self.current_chunk.is_full():\n             self._flush_chunk_and_new_chunk()\n@@ -414,7 +417,7 @@ class GenericToTimeline:\n     def add_to_timeline(self) -> int:\n         \"\"\"Create the result file with the result of argument parsing.\"\"\"\n         self.logger.debug(\"%s started\", self.__class__.__name__)\n-        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"')\n+        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"', escapechar=\"`\")\n         self._setup_next_output_file()\n         self._deflate_archives()\n         self._filter_files_based_on_first_bytes()\ndiff --git a/src/orc2timeline/plugins/RecycleBinToTimeline.py b/src/orc2timeline/plugins/RecycleBinToTimeline.py\nnew file mode 100644\nindex 0000000..9dccc34\n--- /dev/null\n+++ b/src/orc2timeline/plugins/RecycleBinToTimeline.py\n@@ -0,0 +1,63 @@\n+\"\"\"Plugin to parse RecycleBin files.\"\"\"\n+\n+from __future__ import annotations\n+\n+import struct\n+from datetime import datetime, timezone\n+from typing import TYPE_CHECKING\n+\n+if TYPE_CHECKING:\n+    from pathlib import Path\n+    from threading import Lock\n+\n+    from orc2timeline.config import PluginConfig\n+\n+from orc2timeline.plugins.GenericToTimeline import Event, GenericToTimeline\n+\n+EPOCH_AS_FILETIME = 116444736000000000\n+HUNDREDS_OF_NANOSECONDS = 10000000\n+\n+\n+class RecycleBinToTimeline(GenericToTimeline):\n+    def __init__(\n+        self,\n+        config: PluginConfig,\n+        orclist: list[str],\n+        output_file_path: str,\n+        hostname: str,\n+        tmp_dir: str,\n+        lock: Lock,\n+    ) -> None:\n+        \"\"\"Construct.\"\"\"\n+        super().__init__(config, orclist, output_file_path, hostname, tmp_dir, lock)\n+\n+    def _parse_artefact(self, artefact: Path) -> None:\n+        with artefact.open(\"rb\") as f:\n+            try:\n+                raw = f.read()\n+                (header, file_size, deletion_ts) = struct.unpack_from(\"<3q\", raw)\n+                epoch_timestamp = (deletion_ts - EPOCH_AS_FILETIME) / HUNDREDS_OF_NANOSECONDS\n+                event = Event(\n+                    timestamp=datetime.fromtimestamp(epoch_timestamp, tz=timezone.utc),\n+                    source=self.originalPath[artefact.name],\n+                )\n+                index = struct.calcsize(\"<3q\")\n+                if header == 1:\n+                    # INFO2 file version 1: Windows Vista or Windows 7\n+                    filepath = raw[index : index + 250].decode(\"utf_16_le\").rstrip(\"\\x00\")\n+\n+                elif header == 2:  # noqa: PLR2004\n+                    # INFO2 file version 2: Windows 10+\n+                    (fp_length,) = struct.unpack_from(\"<i\", raw[index:])\n+                    index += struct.calcsize(\"<i\")\n+                    filepath = raw[index : index + fp_length * 2].decode(\"utf_16_le\").rstrip(\"\\x00\")\n+\n+                else:\n+                    msg = f\"[RecycleBinToTimeline] [{self.hostname}] Unexpected header value : {header}\"\n+                    self.logger.warning(msg)\n+                    return\n+\n+                event.description = f\"Deletion of file {filepath} - Filesize : {file_size}\"\n+                self._add_event(event)\n+            except Exception as e:  # noqa: BLE001\n+                self.logger.warning(\"Error while parsing %s : %s\", artefact.name, e)\ndiff --git a/src/orc2timeline/plugins/RegistryToTimeline.py b/src/orc2timeline/plugins/RegistryToTimeline.py\nindex 01de696..4c55287 100644\n--- a/src/orc2timeline/plugins/RegistryToTimeline.py\n+++ b/src/orc2timeline/plugins/RegistryToTimeline.py\n@@ -107,7 +107,7 @@ class RegistryToTimeline(GenericToTimeline):\n             event = Event(\n                 timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n                 source=self._get_original_path(artefact),\n-                description=key.path,\n+                description=str(key.path),\n             )\n             self._add_event(event)\n         except Exception as e:  # noqa: BLE001\ndiff --git a/src/orc2timeline/plugins/UserAssistToTimeline.py b/src/orc2timeline/plugins/UserAssistToTimeline.py\nnew file mode 100644\nindex 0000000..4256cb4\n--- /dev/null\n+++ b/src/orc2timeline/plugins/UserAssistToTimeline.py\n@@ -0,0 +1,114 @@\n+\"\"\"Plugin to decode unformation from UserAssist registry keys.\"\"\"\n+\n+from __future__ import annotations\n+\n+import codecs\n+import struct\n+from datetime import datetime, timedelta\n+from typing import TYPE_CHECKING, Any\n+\n+import pytz\n+from dfwinreg import regf as dfwinreg_regf\n+from dfwinreg import registry as dfwinreg_registry\n+\n+if TYPE_CHECKING:\n+    from pathlib import Path\n+    from threading import Lock\n+\n+    from orc2timeline.config import PluginConfig\n+\n+from orc2timeline.plugins.GenericToTimeline import Event\n+from orc2timeline.plugins.RegistryToTimeline import RegistryToTimeline\n+\n+GUID_to_path = {}\n+GUID_to_path[\"{1AC14E77-02E7-4E5D-B744-2EB1AE5198B7}\"] = r\"C:\\Windows\\System32\"\n+GUID_to_path[\"{6D809377-6AF0-444B-8957-A3773F02200E}\"] = r\"C:\\Program Files\"\n+GUID_to_path[\"{7C5A40EF-A0FB-4BFC-874A-C0F2E0B9FA8E}\"] = r\"C:\\Program Files (x86)\"\n+GUID_to_path[\"{F38BF404-1D43-42F2-9305-67DE0B28FC23}\"] = r\"C:\\Windows\"\n+GUID_to_path[\"{0139D44E-6AFE-49F2-8690-3DAFCAE6FFB8}\"] = r\"C:\\ProgramData\\Microsoft\\Windows \\Start Menu\\Programs\"\n+GUID_to_path[\"{9E3995AB-1F9C-4F13-B827-48B24B6C7174}\"] = (\n+    r\"%AppDat\n\n… truncated at 24,000 bytes of 28,212", "bytes": 28212, "truncated": true}, {"label": "Bug patch", "file": "environment/bug.patch", "text": "diff --git a/src/orc2timeline/conf/Orc2Timeline.yaml b/src/orc2timeline/conf/Orc2Timeline.yaml\nindex 6a201c7..789cf3c 100644\n--- a/src/orc2timeline/conf/Orc2Timeline.yaml\n+++ b/src/orc2timeline/conf/Orc2Timeline.yaml\n@@ -28,27 +28,3 @@ Plugins:\n       sub_archives: [\"NTFSInfo_i30Info.7z\"]\n       match_pattern: \"^I30Info.*\\\\.csv$\"\n       sourcetype: \"I30\"\n-\n-  - FirefoxHistoryToTimeline:\n-      archives: [\"Browsers\", \"General\", \"Offline\"]\n-      sub_archives: [\"Browsers_history.7z\", \"GetBrowsers_History.7z\", \"Browsers_complet.7z\"]\n-      match_pattern: \".*places\\\\.sqlite.*data$\"\n-      sourcetype: \"FirefoxHistory\"\n-\n-  - RecycleBinToTimeline:\n-      archives: [\"General\"]\n-      sub_archives: [\"Residents.7z\"]\n-      match_pattern: \"(.*fichiers_residents/.*_\\\\$I.*data$)\"\n-      sourcetype: \"RecycleBin\"\n-\n-  - UserAssistToTimeline:\n-      archives: [\"Detail\", \"Offline\"]\n-      sub_archives: [\"UserHives.7z\"]\n-      match_pattern: \".*NTUSER\\\\.DAT.*\"\n-      sourcetype: \"UserAssist\"\n-\n-  - AmCacheToTimeline:\n-      archives: [\"Little\", \"Detail\", \"Offline\"]\n-      sub_archives: [\"SystemHives_little.7z\", \"SystemHives.7z\"]\n-      match_pattern: \".*AmCache.hve.*data$\"\n-      sourcetype: \"AmCache\"\ndiff --git a/src/orc2timeline/core.py b/src/orc2timeline/core.py\nindex e37539d..7ea40aa 100644\n--- a/src/orc2timeline/core.py\n+++ b/src/orc2timeline/core.py\n@@ -31,7 +31,7 @@ def _add_header_to_csv_file(output_path: str) -> None:\n     \"\"\"Add header at the beginning of csv file.\"\"\"\n     header = [\"Timestamp\", \"Hostname\", \"SourceType\", \"Description\", \"SourceFile\"]\n     with gzip.open(output_path, \"wt\", newline=\"\") as f:\n-        csv_dict_writer = csv.DictWriter(f, delimiter=\",\", quotechar='\"', fieldnames=header, escapechar=\"`\")\n+        csv_dict_writer = csv.DictWriter(f, delimiter=\",\", quotechar='\"', fieldnames=header)\n         csv_dict_writer.writeheader()\n \n \ndiff --git a/src/orc2timeline/plugins/AmCacheToTimeline.py b/src/orc2timeline/plugins/AmCacheToTimeline.py\ndeleted file mode 100644\nindex 4e22674..0000000\n--- a/src/orc2timeline/plugins/AmCacheToTimeline.py\n+++ /dev/null\n@@ -1,221 +0,0 @@\n-\"\"\"Plugin to parse AmCache Registry Hives.\"\"\"\n-\n-from __future__ import annotations\n-\n-from datetime import datetime, timezone\n-from typing import TYPE_CHECKING, Any\n-\n-from dfwinreg import regf as dfwinreg_regf\n-from dfwinreg import registry as dfwinreg_registry\n-\n-if TYPE_CHECKING:\n-    from pathlib import Path\n-    from threading import Lock\n-\n-    from orc2timeline.config import PluginConfig\n-\n-from orc2timeline.plugins.GenericToTimeline import Event\n-from orc2timeline.plugins.RegistryToTimeline import RegistryToTimeline\n-\n-EPOCH_AS_FILETIME = 116444736000000000\n-HUNDREDS_OF_NANOSECONDS = 10000000\n-\n-\n-class AmCacheToTimeline(RegistryToTimeline):\n-    def __init__(\n-        self,\n-        config: PluginConfig,\n-        orclist: list[str],\n-        output_file_path: str,\n-        hostname: str,\n-        tmp_dir: str,\n-        lock: Lock,\n-    ) -> None:\n-        \"\"\"Construct.\"\"\"\n-        super().__init__(config, orclist, output_file_path, hostname, tmp_dir, lock)\n-\n-    def _parse_inventory_application_file(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n-        event = Event(\n-            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n-            source=self.originalPath[artefact.name],\n-        )\n-        desc = [f\"KeyPath: {key.path}\"]\n-        filename = key.GetValueByName(\"Name\")\n-        if filename:\n-            desc.append(f\"Name: {filename.GetDataAsObject()}\")\n-        path = key.GetValueByName(\"LowerCaseLongPath\")\n-        if path:\n-            desc.append(f\"ExecPath: {path.GetDataAsObject()}\")\n-        file_id = key.GetValueByName(\"FileId\")\n-        if file_id:\n-            desc.append(f\"SHA1: {file_id.GetDataAsObject()[4:]}\")\n-        file_size = key.GetValueByName(\"Size\")\n-        if file_size:\n-            desc.append(f\"FileSize: {file_size.GetDataAsObject()}\")\n-        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n-        self._add_event(event)\n-        # check if there is a compilation date to add another line\n-        lnkdate = key.GetValueByName(\"LinkDate\")\n-        if lnkdate:\n-            epoch_timestamp = datetime.strptime(\n-                lnkdate.GetDataAsObject(),\n-                \"%m/%d/%Y %H:%M:%S\",\n-            ).replace(tzinfo=timezone.utc)\n-            eventlnk = Event(\n-                timestamp=epoch_timestamp,\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventlnk.description = \"Compilation timestamp - \" + \" - \".join(desc)\n-            self._add_event(eventlnk)\n-\n-    def _parse_inventory_driver_binary(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n-        event = Event(\n-            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n-            source=self.originalPath[artefact.name],\n-        )\n-        desc = [f\"KeyPath: {key.path}\"]\n-        filename = key.GetValueByName(\"DriverName\")\n-        if filename:\n-            desc.append(f\"Name: {filename.GetDataAsObject()}\")\n-        path = key.GetValueByName(\"LowerCaseLongPath\")\n-        if path:\n-            desc.append(f\"DriverPath: {path.GetDataAsObject()}\")\n-        file_id = key.GetValueByName(\"DriverId\")\n-        # sha1 can be either in one of the value or the name of the key\n-        if file_id:\n-            desc.append(f\"SHA1: {file_id.GetDataAsObject()[4:]}\")\n-        elif key.name.startswith(\"0000\"):\n-            desc.append(f\"SHA1: {key.name[4:]}\")\n-        file_size = key.GetValueByName(\"ImageSize\")\n-        if file_size:\n-            desc.append(f\"FileSize: {file_size.GetDataAsObject()}\")\n-        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n-        self._add_event(event)\n-        # check if there is a compilation date to add another line\n-        dlwt = key.GetValueByName(\"DriverLastWriteTime\")\n-        if dlwt:\n-            eventlnk = Event(\n-                timestamp=datetime.strptime(dlwt.GetDataAsObject(), \"%m/%d/%Y %H:%M:%S\").replace(tzinfo=timezone.utc),\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventlnk.description = \"Driver Last Write time - \" + \" - \".join(desc)\n-            self._add_event(eventlnk)\n-\n-    def _parse_file_key(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n-        event = Event(\n-            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n-            source=self.originalPath[artefact.name],\n-        )\n-        desc = [f\"KeyPath: {key.path}\"]\n-        path = key.GetValueByName(\"15\")\n-        if path:\n-            desc.append(f\"ExecPath: {path.GetDataAsObject()}\")\n-        file_id = key.GetValueByName(\"101\")\n-        if file_id:\n-            desc.append(f\"SHA1: {file_id.GetDataAsObject()[4:]}\")\n-        file_size = key.GetValueByName(\"6\")\n-        if file_size:\n-            desc.append(f\"FileSize: {file_size.GetDataAsObject()}\")\n-        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n-        self._add_event(event)\n-        # check if there is a modification date to add another line\n-        moddate = key.GetValueByName(\"17\")\n-        if moddate:\n-            epoch_timestamp = (int(moddate.GetDataAsObject()) - EPOCH_AS_FILETIME) / HUNDREDS_OF_NANOSECONDS\n-            eventmod = Event(\n-                timestamp=datetime.fromtimestamp(epoch_timestamp, tz=timezone.utc),\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventmod.description = \"Modification time - \" + \" - \".join(desc)\n-            self._add_event(eventmod)\n-        # check if there is a creation date to add another line\n-        moddate = key.GetValueByName(\"12\")\n-        if moddate:\n-            epoch_timestamp = (int(moddate.GetDataAsObject()) - EPOCH_AS_FILETIME) / HUNDREDS_OF_NANOSECONDS\n-            eventbirth = Event(\n-                timestamp=datetime.fromtimestamp(epoch_timestamp, tz=timezone.utc),\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventbirth.description = \"Creation time - \" + \" - \".join(desc)\n-            self._add_event(eventbirth)\n-        # check if there is a compilation date to add another line\n-        lnkdate = key.GetValueByName(\"f\")\n-        if lnkdate:\n-            eventlnk = Event(\n-                timestamp=datetime.fromtimestamp(int(lnkdate.GetDataAsObject()), timezone.utc),\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventlnk.description = \"Compilation time - \" + \" - \".join(desc)\n-            self._add_event(eventlnk)\n-\n-    def _parse_programs_key(self, key: Any, artefact: Path) -> None:  # noqa: ANN401\n-        event = Event(\n-            timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n-            source=self.originalPath[artefact.name],\n-        )\n-        desc = [f\"KeyPath: {key.path}\"]\n-        name = key.GetValueByName(\"0\")\n-        if name:\n-            desc.append(f\"Name: {name.GetDataAsObject()}\")\n-        version = key.GetValueByName(\"1\")\n-        if version:\n-            desc.append(f\"Version: {version.GetDataAsObject()}\")\n-        publisher = key.GetValueByName(\"2\")\n-        if publisher:\n-            desc.append(f\"Publisher: {publisher.GetDataAsObject()}\")\n-        event.description = \"Key last modified timestamp - \" + \" - \".join(desc)\n-        self._add_event(event)\n-        # check if there is an installation date to add another line\n-        installdate = key.GetValueByName(\"a\")\n-        if installdate:\n-            eventinst = Event(\n-                timestamp=datetime.fromtimestamp(int(installdate.GetDataAsObject()), timezone.utc),\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventinst.description = \"Installation time - \" + \" - \".join(desc)\n-            self._add_event(eventinst)\n-        # check if there is a creation date to add another line\n-        uninstalldate = key.GetValueByName(\"b\")\n-        if uninstalldate and int(uninstalldate.GetDataAsObject()) != 0:\n-            eventuninst = Event(\n-                timestamp=datetime.fromtimestamp(int(uninstalldate.GetDataAsObject()), timezone.utc),\n-                source=self.originalPath[artefact.name],\n-            )\n-            eventuninst.description = \"Uninstallation time - \" + \" - \".join(desc)\n-            self._add_event(eventuninst)\n-\n-    def _parse_artefact(self, artefact: Path) -> None:\n-        with artefact.open(\"rb\") as f:\n-            try:\n-                reg_file = dfwinreg_regf.REGFWinRegistryFile(emulate_virtual_keys=False)\n-                reg_file.Open(f)\n-                win_registry = dfwinreg_registry.WinRegistry()\n-                key_path_prefix = win_registry.GetRegistryFileMapping(reg_file)\n-                reg_file.SetKeyPathPrefix(key_path_prefix)\n-                inventory_application_file = reg_file.GetKeyByPath(\n-                    \"\\\\Root\\\\InventoryApplicationFile\",\n-                )\n-                if inventory_application_file:\n-                    for iaf in inventory_application_file.RecurseKeys():\n-                        self._parse_inventory_application_file(iaf, artefact)\n-                inventory_driver_binary = reg_file.GetKeyByPath(\n-                    \"\\\\Root\\\\InventoryDriverBinary\",\n-                )\n-                if inventory_driver_binary:\n-                    for idb in inventory_driver_binary.RecurseKeys():\n-                        self._parse_inventory_driver_binary(idb, artefact)\n-                file_key = reg_file.GetKeyByPath(\n-                    \"\\\\Root\\\\File\",\n-                )\n-                if file_key:\n-                    for disk_guid in file_key.RecurseKeys():\n-                        for entry in disk_guid.RecurseKeys():\n-                            self._parse_file_key(entry, artefact)\n-                programs_key = reg_file.GetKeyByPath(\n-                    \"\\\\Root\\\\Programs\",\n-                )\n-                if programs_key:\n-                    for entry in programs_key.RecurseKeys():\n-                        self._parse_programs_key(entry, artefact)\n-            except Exception as e:  # noqa: BLE001\n-                self.logger.warning(\"Error while parsing %s : %s\", artefact.name, e)\ndiff --git a/src/orc2timeline/plugins/FirefoxHistoryToTimeline.py b/src/orc2timeline/plugins/FirefoxHistoryToTimeline.py\ndeleted file mode 100644\nindex 291ff35..0000000\n--- a/src/orc2timeline/plugins/FirefoxHistoryToTimeline.py\n+++ /dev/null\n@@ -1,132 +0,0 @@\n-\"\"\"Generic plugin to parse Firefox histories (places.sqlite).\"\"\"\n-\n-from __future__ import annotations\n-\n-import sqlite3\n-from datetime import datetime, timedelta\n-from typing import TYPE_CHECKING\n-\n-import pytz\n-\n-if TYPE_CHECKING:\n-    from pathlib import Path\n-    from threading import Lock\n-\n-    from orc2timeline.config import PluginConfig\n-\n-from orc2timeline.plugins.GenericToTimeline import Event, GenericToTimeline\n-\n-\n-def _reverse_hostname(hostname: str) -> str:\n-\n-    if not hostname:\n-        return \"\"\n-\n-    if len(hostname) <= 1:\n-        return hostname\n-\n-    if hostname[-1] == \".\":\n-        return hostname[::-1][1:]\n-\n-    return hostname[::-1][0:]\n-\n-\n-class FirefoxHistoryToTimeline(GenericToTimeline):\n-    def __init__(\n-        self,\n-        config: PluginConfig,\n-        orclist: list[str],\n-        output_file_path: str,\n-        hostname: str,\n-        tmp_dir: str,\n-        lock: Lock,\n-    ) -> None:\n-        \"\"\"Construct.\"\"\"\n-        super().__init__(config, orclist, output_file_path, hostname, tmp_dir, lock)\n-\n-        self.schema: dict[str, str] = {}\n-        self.columns_per_table: dict[str, list[str]] = {}\n-        self.required_structure = {\n-            \"moz_places\": frozenset([\"url\", \"title\", \"visit_count\", \"rev_host\", \"hidden\", \"typed\", \"id\"]),\n-            \"moz_historyvisits\": frozenset([\"id\", \"visit_date\", \"from_visit\", \"visit_type\", \"place_id\"]),\n-            \"moz_bookmarks\": frozenset([\"type\", \"title\", \"dateAdded\", \"lastModified\", \"id\", \"fk\"]),\n-            \"moz_items_annos\": frozenset([\"content\", \"dateAdded\", \"lastModified\", \"id\", \"item_id\"]),\n-        }\n-\n-    def _get_schema(self, curr: sqlite3.Cursor) -> None:\n-        sql_req = (\n-            'SELECT tbl_name, sql FROM sqlite_master WHERE type = \"table\" AND '\n-            'tbl_name != \"xp_proc\" AND tbl_name != \"sqlite_sequence\"'\n-        )\n-        sql_results = curr.execute(sql_req)\n-        self.schema = {table_name: \" \".join(query.split()) for table_name, query in sql_results}\n-\n-    def _validate_db(self) -> bool:\n-        if not self.required_structure:\n-            return True\n-\n-        for required_table, required_columns in self.required_structure.items():\n-            if required_table not in self.schema:\n-                return False\n-\n-            ze_table = self.columns_per_table.get(required_table)\n-            if ze_table is not None and (not frozenset(required_columns).issubset(ze_table)):\n-                return False\n-\n-        return True\n-\n-    def _parse_visited_row(self, curr: sqlite3.Cursor, sqlite_path: Path) -> None:\n-\n-        query = (\n-            \"SELECT moz_historyvisits.id, moz_places.url, moz_places.title, \"\n-            \"moz_places.visit_count, moz_historyvisits.visit_date, \"\n-            \"(select moz_places.url from moz_places where moz_historyvisits.from_visit=moz_places.id) as referer_name,\"\n-            \"moz_historyvisits.from_visit, moz_places.rev_host, \"\n-            \"moz_places.hidden, moz_places.typed, moz_historyvisits.visit_type \"\n-            \"FROM moz_places, moz_historyvisits \"\n-            \"WHERE moz_places.id = moz_historyvisits.place_id\"\n-        )\n-\n-        curr.execute(query)\n-        sql_result = curr.fetchall()\n-        col_names = [desc[0] for desc in curr.description]\n-\n-        for row in sql_result:\n-            row_dict = {col_names[i]: row[i] for i in range(len(col_names))}  # trick to create a dict from sql result\n-            description = (\n-                f\"Url: {row_dict['url']} - \"\n-                f\"Title: {row_dict['title']} - \"\n-                f\"Count: {row_dict['visit_count']} - \"\n-                f\"Typed: {row_dict['typed']} - \"\n-                f\"Referer: {row_dict['referer_name']}\"\n-            )\n-            # typed = tapé manuellement ?\n-            # hidden\n-            # referer => résoudre\n-            epoch = datetime(1970, 1, 1, tzinfo=pytz.utc)\n-            timestamp = epoch + timedelta(microseconds=int(row_dict[\"visit_date\"]))\n-            event = Event(timestamp=timestamp, source=self._get_original_path(sqlite_path), description=description)\n-            self._add_event(event)\n-\n-    def _parse_artefact(self, artefact: Path) -> None:\n-        if \"places.sqlite-wal\" in artefact.stem or \"places.sqlite-shm\" in artefact.stem:\n-            return\n-\n-        try:\n-            conn = sqlite3.connect(artefact)\n-            curr = conn.cursor()\n-\n-            self._get_schema(curr)\n-            for table_name in self.schema:\n-                self.columns_per_table.setdefault(table_name, [])\n-                pragma_results = curr.execute(f'PRAGMA table_info(\"{table_name:s}\")')\n-                for pragma_result in pragma_results:\n-                    self.columns_per_table[table_name].append(pragma_result[1])\n-\n-            if self._validate_db():\n-                self._parse_visited_row(curr, artefact)\n-\n-        except sqlite3.DatabaseError:\n-            self.logger.exception(\"%s is not a valid databse\", artefact)\n-        except Exception:\n-            raise\ndiff --git a/src/orc2timeline/plugins/GenericToTimeline.py b/src/orc2timeline/plugins/GenericToTimeline.py\nindex 41d532e..5db1c91 100644\n--- a/src/orc2timeline/plugins/GenericToTimeline.py\n+++ b/src/orc2timeline/plugins/GenericToTimeline.py\n@@ -246,7 +246,7 @@ class GenericToTimeline:\n         self.logger.critical(\"Reinitialization of chunks\")\n \n         self.current_chunk = SortedChunk(10000)\n-        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"', escapechar=\"`\")\n+        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"')\n         self.output_files_list = []\n         self._setup_next_output_file()\n \n@@ -375,10 +375,7 @@ class GenericToTimeline:\n         file.\n         \"\"\"\n         # sanitize output\n-        rows_to_write = [\n-            row.replace(\"\\n\", \"\\\\n\").replace(\"\\r\", \"\\\\r\")\n-            for row in (date, self.hostname, sourcetype, description, sourcefile)\n-        ]\n+        rows_to_write = [row.replace(\"\\n\", \"\\\\n\") for row in (date, self.hostname, sourcetype, description, sourcefile)]\n         self.csvWriter.writerow(rows_to_write)\n         if self.current_chunk.is_full():\n             self._flush_chunk_and_new_chunk()\n@@ -417,7 +414,7 @@ class GenericToTimeline:\n     def add_to_timeline(self) -> int:\n         \"\"\"Create the result file with the result of argument parsing.\"\"\"\n         self.logger.debug(\"%s started\", self.__class__.__name__)\n-        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"', escapechar=\"`\")\n+        self.csvWriter = csv.writer(self.current_chunk, delimiter=\",\", quotechar='\"')\n         self._setup_next_output_file()\n         self._deflate_archives()\n         self._filter_files_based_on_first_bytes()\ndiff --git a/src/orc2timeline/plugins/RecycleBinToTimeline.py b/src/orc2timeline/plugins/RecycleBinToTimeline.py\ndeleted file mode 100644\nindex 9dccc34..0000000\n--- a/src/orc2timeline/plugins/RecycleBinToTimeline.py\n+++ /dev/null\n@@ -1,63 +0,0 @@\n-\"\"\"Plugin to parse RecycleBin files.\"\"\"\n-\n-from __future__ import annotations\n-\n-import struct\n-from datetime import datetime, timezone\n-from typing import TYPE_CHECKING\n-\n-if TYPE_CHECKING:\n-    from pathlib import Path\n-    from threading import Lock\n-\n-    from orc2timeline.config import PluginConfig\n-\n-from orc2timeline.plugins.GenericToTimeline import Event, GenericToTimeline\n-\n-EPOCH_AS_FILETIME = 116444736000000000\n-HUNDREDS_OF_NANOSECONDS = 10000000\n-\n-\n-class RecycleBinToTimeline(GenericToTimeline):\n-    def __init__(\n-        self,\n-        config: PluginConfig,\n-        orclist: list[str],\n-        output_file_path: str,\n-        hostname: str,\n-        tmp_dir: str,\n-        lock: Lock,\n-    ) -> None:\n-        \"\"\"Construct.\"\"\"\n-        super().__init__(config, orclist, output_file_path, hostname, tmp_dir, lock)\n-\n-    def _parse_artefact(self, artefact: Path) -> None:\n-        with artefact.open(\"rb\") as f:\n-            try:\n-                raw = f.read()\n-                (header, file_size, deletion_ts) = struct.unpack_from(\"<3q\", raw)\n-                epoch_timestamp = (deletion_ts - EPOCH_AS_FILETIME) / HUNDREDS_OF_NANOSECONDS\n-                event = Event(\n-                    timestamp=datetime.fromtimestamp(epoch_timestamp, tz=timezone.utc),\n-                    source=self.originalPath[artefact.name],\n-                )\n-                index = struct.calcsize(\"<3q\")\n-                if header == 1:\n-                    # INFO2 file version 1: Windows Vista or Windows 7\n-                    filepath = raw[index : index + 250].decode(\"utf_16_le\").rstrip(\"\\x00\")\n-\n-                elif header == 2:  # noqa: PLR2004\n-                    # INFO2 file version 2: Windows 10+\n-                    (fp_length,) = struct.unpack_from(\"<i\", raw[index:])\n-                    index += struct.calcsize(\"<i\")\n-                    filepath = raw[index : index + fp_length * 2].decode(\"utf_16_le\").rstrip(\"\\x00\")\n-\n-                else:\n-                    msg = f\"[RecycleBinToTimeline] [{self.hostname}] Unexpected header value : {header}\"\n-                    self.logger.warning(msg)\n-                    return\n-\n-                event.description = f\"Deletion of file {filepath} - Filesize : {file_size}\"\n-                self._add_event(event)\n-            except Exception as e:  # noqa: BLE001\n-                self.logger.warning(\"Error while parsing %s : %s\", artefact.name, e)\ndiff --git a/src/orc2timeline/plugins/RegistryToTimeline.py b/src/orc2timeline/plugins/RegistryToTimeline.py\nindex 4c55287..01de696 100644\n--- a/src/orc2timeline/plugins/RegistryToTimeline.py\n+++ b/src/orc2timeline/plugins/RegistryToTimeline.py\n@@ -107,7 +107,7 @@ class RegistryToTimeline(GenericToTimeline):\n             event = Event(\n                 timestamp_str=key.last_written_time.CopyToDateTimeString()[:-4],\n                 source=self._get_original_path(artefact),\n-                description=str(key.path),\n+                description=key.path,\n             )\n             self._add_event(event)\n         except Exception as e:  # noqa: BLE001\ndiff --git a/src/orc2timeline/plugins/UserAssistToTimeline.py b/src/orc2timeline/plugins/UserAssistToTimeline.py\ndeleted file mode 100644\nindex 4256cb4..0000000\n--- a/src/orc2timeline/plugins/UserAssistToTimeline.py\n+++ /dev/null\n@@ -1,114 +0,0 @@\n-\"\"\"Plugin to decode unformation from UserAssist registry keys.\"\"\"\n-\n-from __future__ import annotations\n-\n-import codecs\n-import struct\n-from datetime import datetime, timedelta\n-from typing import TYPE_CHECKING, Any\n-\n-import pytz\n-from dfwinreg import regf as dfwinreg_regf\n-from dfwinreg import registry as dfwinreg_registry\n-\n-if TYPE_CHECKING:\n-    from pathlib import Path\n-    from threading import Lock\n-\n-    from orc2timeline.config import PluginConfig\n-\n-from orc2timeline.plugins.GenericToTimeline import Event\n-from orc2timeline.plugins.RegistryToTimeline import RegistryToTimeline\n-\n-GUID_to_path = {}\n-GUID_to_path[\"{1AC14E77-02E7-4E5D-B744-2EB1AE5198B7}\"] = r\"C:\\Windows\\System32\"\n-GUID_to_path[\"{6D809377-6AF0-444B-8957-A3773F02200E}\"] = r\"C:\\Program Files\"\n-GUID_to_path[\"{7C5A40EF-A0FB-4BFC-874A-C0F2E0B9FA8E}\"] = r\"C:\\Program Files (x86)\"\n-GUID_to_path[\"{F38BF404-1D43-42F2-9305-67DE0B28FC23}\"] = r\"C:\\Windows\"\n-GUID_to_path[\"{0139D44E-6AFE-49F2-8690-3DAFCAE6FFB8}\"] = r\"C:\\ProgramData\\Microsoft\\Windows \\Start Menu\\Programs\"\n-GUID_to_path[\"{9E3995AB-1F9C-4F13-B827-48B24B6C7174}\"] = \n\n… truncated at 24,000 bytes of 46,175", "bytes": 46175, "truncated": true}, {"label": "Dockerfile", "file": "environment/Dockerfile", "text": "FROM ubuntu:24.04\n\nRUN apt-get update && apt-get install -y \\\n    git \\\n    curl \\\n    ca-certificates \\\n    patch \\\n    python3 \\\n    python3-pip \\\n    python3-venv \\\n    && rm -rf /var/lib/apt/lists/*\n\nRUN curl -LsSf https://astral.sh/uv/install.sh | sh && \\\n    mv /root/.local/bin/uv /usr/local/bin/uv\n\nWORKDIR /app\n\nRUN git clone https://github.com/ANSSI-FR/orc2timeline.git src && \\\n    cd src && \\\n    (git fetch --depth 1 origin 0bc00766e40b2fc98887dcedf4b423702f1784da || git fetch --depth 1 origin \"+refs/pull/5/head:refs/remotes/origin/pr/5\") && \\\n    git checkout --detach FETCH_HEAD && \\\n    git submodule update --init --recursive\n\nWORKDIR /app/src\n\nRUN uv venv /opt/venv && \\\n    uv pip install --python /opt/venv/bin/python -e \".[tests]\" && \\\n    uv pip install --python /opt/venv/bin/python pytest\n\nENV CI=true\n\nCOPY bug.patch /tmp/bug.patch\nRUN (patch -p1 < /tmp/bug.patch || true) && rm /tmp/bug.patch\n\nRUN rm -rf /app/src/.git\n\nENV PATH=\"/opt/venv/bin:${PATH}\"\nWORKDIR /app/src\n", "bytes": 998, "truncated": false}]}]