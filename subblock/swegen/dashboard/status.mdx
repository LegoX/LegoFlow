# SWE-gen — Status

See `status.yaml` for live phase, current job, next steps, and history.

## Per-Language Status

See `inputs.yaml` under `languages.{lang}.status` for per-language metrics:
- `total_success` / `total_failed` / `total_filtered`
- `success_rate`
- `pr_pool_remaining`
- `last_checked`

## Adaptive Tuning Log

See `artifacts/logs/adaptive_decisions.jsonl` for a full record of parameter adjustments.
