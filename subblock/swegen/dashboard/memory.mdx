# SWE-gen — Memory

Long-form context, decisions, and experiment records live in `memory/`.

## Files

- `memory/notes.md` — architecture overview, adaptive tuning rules, experiment log, known failure modes
- `memory/experiment-log.md` — end-to-end pipeline validation log (2026-04-20)
- `memory/adaptive-tuning-validation.md` — adaptive parameter tuning full-cycle validation (2026-04-22)

For per-run archives see `artifacts/index.yaml` and `artifacts/files/`.
