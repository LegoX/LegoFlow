{
  "name": "evaluator",
  "description": "evaluator block skills — Harbor + LiteLLM env bootstrap, preflight (uv env + LiteLLM venv + LLM endpoint + harbor registry lookup), dashboard (per-job task counts + accuracy), and run (LiteLLM proxy + harbor benchmark execution). Provides /evaluator:setup, /evaluator:check, /evaluator:dashboard, /evaluator:run.",
  "version": "0.1.0",
  "author": { "name": "SWE-Lego" }
}
