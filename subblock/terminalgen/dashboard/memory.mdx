# Terminal-gen — Memory

Long-form context, decisions, and experiment records live in `memory/`.

## Files

- `memory/notes.md` — architecture overview, adaptive tuning rules, domain bucketing rationale, known failure modes

For per-run archives see `artifacts/index.yaml` and `artifacts/archives/`.
