# Terminal-gen — Status

See `config.yaml` under `status` for live phase, progress, next steps, and blockers.

## Per-Domain Status

See `config.yaml` under `runtime_info.input.domains.{domain}.status` for
per-domain metrics:
- `total_success` / `total_failed` / `total_filtered`
- `success_rate`
- `question_pool_remaining`
- `last_checked`

## Adaptive Tuning Log

See `artifacts/logs/adaptive_decisions.jsonl` for a full record of parameter adjustments.
