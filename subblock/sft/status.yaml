# status.yaml — growing history of this block's operational state
# Agent appends to `history` after each meaningful change.
# Do not truncate this file; it is the permanent record.

block: sft_training

# ─── Current State ───────────────────────────────────────────────────────────

updated_at: "2026-04-24T16:16:45Z"

# idle | running | paused | completed | failed | blocked
phase: idle

# Free-form label for the current high-level stage.
stage: completed

current_job:
  name: qwen3_8b_jierun_glm5_swerebench_oraclesolved_oh_sdk_test_gbs64pbs1acc8_lr1e-4_epo4_think
  started_at: "2026-04-24T15:53:28Z"
  progress: 1.0
  detail: "4/4 steps, loss 0.6194, 20m56s"

next_steps:
  - order: 1
    label: "Start next training run"
    detail: "Update inputs.yaml with new trajectory source and run bash scripts/start.sh"
    blocked_by: null

blockers: []

metrics:
  final_loss: 0.6194279789924622
  total_steps: 4
  train_runtime_seconds: 1256.0117
  epochs: 4.0

# ─── History ─────────────────────────────────────────────────────────────────

# Append-only. Agent adds an entry each time phase/stage changes or a milestone is reached.
history:
  - timestamp: "2026-04-24T16:16:45Z"
    phase: idle
    stage: completed
    label: "Training run completed"
    detail: "Qwen3-8B SFT on jierun_glm5_swerebench_oraclesolved_oh_sdk_test. 4 steps, loss 0.6194, 20m56s."
    artifact: "artifacts/model/qwen3_8b_jierun_glm5_swerebench_oraclesolved_oh_sdk_test_gbs64pbs1acc8_lr1e-4_epo4_think"
