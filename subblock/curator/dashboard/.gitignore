# Wrangler local deploy cache (Cloudflare Pages). Machine-local, never commit.
.wrangler/
site/.wrangler/

# Large dataset task/tag payloads — regenerated by the tagging pipeline.
datasets/*/tasks.jsonl
datasets/*/tags.jsonl

# Python bytecode cache
__pycache__/

# Pipeline run log (machine-local)
pipeline.log
