{
  "name": "curator",
  "description": "curator block skills — env/venv bootstrap, preflight (GitHub tokens + LLM endpoint + docker), separate PR collection, task generation + verification, and dashboard. Provides /curator:setup, /curator:check, /curator:collect-prs, /curator:dashboard, /curator:run.",
  "version": "0.1.0",
  "author": { "name": "haoli" }
}
