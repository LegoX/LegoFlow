{
  "name": "curator",
  "description": "curator block skills — env/venv bootstrap, preflight (GitHub tokens + LLM endpoint + docker), dashboard, and pipeline launch (PR collection → task generation → verification). Provides /curator:setup, /curator:check, /curator:dashboard, /curator:run.",
  "version": "0.1.0",
  "author": { "name": "haoli" }
}
