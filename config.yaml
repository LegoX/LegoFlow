meta_info:
  name: swe_lego_live
  label: "SWE Lego Live"
  description: Root orchestration block for the self-evolving LLM pipeline (task curation -> trajectories -> SFT; evaluation standalone).
  parent: null
  subblocks:
    curator:
      role: "Convert GitHub PRs into verified SWE tasks (task source)"
    tracer:
      role: "Generate agent trajectories from verified tasks via Harbor"
    trainer:
      role: "SFT on trajectories via LLaMA-Factory + DeepSpeed"
    evaluator:
      role: "Run registered benchmarks via Harbor (standalone; no upstream dep)"
  dependencies: {}
  resources:
    ip: local
    directory: null
    description: Orchestrator — runs wherever the tree is checked out.
    public_storage: null

runtime_info:
  input: {}    # all external inputs are per-block; root owns none
  output: {}   # pipeline outputs are read from leaf configs (e.g. trainer.output.checkpoint_path)
