meta_info:
  name: swe_lego_live
  label: "SWE Lego Live"
  description: Root orchestration block for the SWE-Lego-Live data pipeline.
  parent: null
  subblocks:
    curator:
      role: "Collect GitHub PRs, and turn them into verified SWE tasks"
    tracer:
      role: "Generate LLM trajectories from verified tasks via Harbor"
    trainer:
      role: "SFT on trajectories via LLaMA-Factory + DeepSpeed"
    evaluator:
      role: "Run registered benchmarks via Harbor"
  dependencies:
    from: {}
    to: {}
  resources:
    ip: local
    directory: null
    description: Orchestrator — runs wherever the tree is checked out.
    public_storage: null

runtime_info:
  input: {}    # all external inputs are per-block; root owns none
  output: {}   # pipeline outputs are read from leaf configs (e.g. trainer.output.checkpoint_path)
