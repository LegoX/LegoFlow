---
title: tracer
description: Roll a coding agent out across verified tasks
---

`tracer` is the trajectory-generation stage. It runs a coding agent (Claude Code, OpenCode, OpenHands SDK, or Terminus-2) over the tasks `curator` verified, captures every rollout via a per-job LiteLLM proxy, and optionally converts the good ones into LLaMA-Factory SFT data.

Full docs: **[swe-tracer-docs.pages.dev/docs](https://swe-tracer-docs.pages.dev/docs)**

<Callout title="Long-form docs live there" type="info">
The tracer block ships its own complete documentation site — getting started, run-jobs walkthroughs, SFT-data conversion, dashboard, and the full reference. This page is just an orientation; follow the link above for everything else.
</Callout>

## At a glance

- **Inputs**: `llm_api` (api_key, api_base_url, model). Task source comes from the curator dependency.
- **Outputs**: `raw_trajectories_dir` and `sft_data_dir` (LLaMA-Factory LF format; consumed by `trainer`).
- **Runs**: Local (CPU + Docker). Long-running.

## How to run

```text
/tracer:setup     # clone harbor + swe_data_process, build uv envs
/tracer:check     # preflight: envs, LLM endpoint, Harbor image, ledger
/tracer:run       # prepare_tasks → proxy → Harbor → optional convert
/tracer:dashboard # local HTML board or Cloudflare sync
```

## Reference

- Block contract: `subblock/tracer/CLAUDE.md`
- Full docs site: [swe-tracer-docs.pages.dev/docs](https://swe-tracer-docs.pages.dev/docs)
