---
name: legoflow-curator-create-tasks
description: Convert curated pull requests into verified SWE tasks after approval.
---

# Create Verified Tasks

Read the curator task-generation contract and existing artifacts first. After explicit confirmation, invoke the existing language-specific creation scripts, verify NOP/Oracle results, and report the manifest and rejected-task counts. Preserve prior task pools.
