---
name: legoflow-evaluator-check
description: Validate benchmark registry, model endpoint, Harbor environment, and evaluation resources without running tasks.
---

# Check Evaluator

Read `blocks/evaluator/AGENTS.md`, `CLAUDE.md`, and `config.yaml`. Run `bash blocks/evaluator/scripts/dryrun.sh`; report benchmark, version, task count, agent scaffold, endpoint, credentials, Docker, and resource requirements. Do not launch Harbor.
