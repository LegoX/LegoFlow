---
title: Memory
block: swe_lego_live
source: memory/notes.md
---

# Memory

Long-form context maintained by the agent. Last updated: 2026-05-11.

---

## Active Run: run_001

**Goal**: Generate 40 new Python SWE tasks (swegen) and produce trajectories for them (trajgen).

**The 3 pre-existing tasks must be skipped** — they are NOT counted toward the 40-task target:
- `agentscope-ai__agentscope-885`
- `agentscope-ai__agentscope-1342`
- `aider-ai__aider-4146`

---

## Current Pipeline State (as of 2026-05-11 ~19:10 UTC)

### swegen (producer) — remote node 192.168.35.240

- **verifiable_tasks.txt** has 9 entries: 3 pre-existing + **6 new tasks**
- New tasks so far: `agentscope-ai__agentscope-964`, `django-guardian__django-guardian-677`, `elastic__elasticsearch-py-3329`, `elastic__elasticsearch-py-3332`, `cartography-cncf__cartography-1628`, `cartography-cncf__cartography-1673`
- **Success rate is critically low: ~0.025%** (7/26,099 for swegen-py, 2/8,729 for swegen-retry)
- At this rate, reaching 40 new tasks will take ~47 days
- Main failure modes: NOP/Oracle validation failures (168 cases), CC timeouts (82 cases)
- `run-llama/llama_index` is a large monorepo that's particularly hard — consider skipping it
- `swegen-fresh` tmux session is stuck in "All tokens exhausted. Waiting..." loop since May 10 12:25 — but the GitHub token (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) actually has 4,654 remaining requests. The script is in a bad state. Not urgent — python_pr_ids.txt has 67,517 PRs and python_infra_retry.txt has 62,885 PRs.
- **Proposed throughput fixes** (not yet implemented): use repos with simpler/more isolated tests, increase cc_timeout, collect PRs from repos with known high success rates, lower min-source-files from 3 to 2

### trajgen (consumer) — runs locally

Ledger state (`subblock/trajgen/artifacts/consumption_ledger.yaml`):

| Task | Status | Reward | Notes |
|------|--------|--------|-------|
| agentscope-ai__agentscope-885 | skipped | — | Pre-existing |
| agentscope-ai__agentscope-1342 | skipped | — | Pre-existing |
| aider-ai__aider-4146 | skipped | — | Pre-existing |
| agentscope-ai__agentscope-964 | **failed** | null | RuntimeError in job 20260511002722 |
| django-guardian__django-guardian-677 | done | 0.0 | Trajectory generated, tests failed |
| elastic__elasticsearch-py-3329 | running | — | Job 20260511181453 |
| elastic__elasticsearch-py-3332 | done | 0.0 | Trajectory generated, tests failed; env start timed out on first attempt |
| cartography-cncf__cartography-1628 | running | — | Job 20260511181453 |
| cartography-cncf__cartography-1673 | running | — | Job 20260511181453 |

**New tasks with trajectory: 2/6** (both reward=0.0 — agent ran but didn't pass tests)
**New tasks pending/running: 3/6**
**New tasks failed: 1/6**

---

## Infrastructure

### Remote node
- IP: `192.168.35.240`
- SSH password: `k9#mP2$vL7@nQ4!xZ8`
- Shared filesystem: `/mnt/public/storage/haoli/code/` (local) = `/gpufs/haoli/code/` (remote)
- swegen runs in tmux sessions: `swegen-py`, `swegen-retry`, `swegen-fresh` on remote node

### API
- LiteLLM proxy: port 4001, model `openai/deepseek-v4-flash` via `az.gptplus5.com`
- API key: `sk-Rz6InDxYzznd3uCXk3ectMXoLlzjkItSTLl3L7jdELFuHKrY`
- **API quota has been exhausted multiple times** — check balance before spawning agents
- Harbor agent: `custom-claude-code 2.1.118`, image `docker.io/jierun/c-cc-2.1.118:v0.1`
- `CLAUDE_CODE_ATTRIBUTION_HEADER=0` must be set (configured in `.claude/settings.json`)

### trajgen tmux
- Session `trajgen-run001` on remote node — may need to be restarted
- Run: `cd /gpufs/haoli/code/SWE-Lego-Live/subblock/trajgen && bash scripts/start.sh`

---

## Immediate Next Steps

1. **Check if trajgen Harbor job is still running** on remote node (job `20260511181453`). The 3 "running" tasks in the ledger may have completed or failed.
   ```bash
   sshpass -e ssh root@192.168.35.240 "tmux capture-pane -t trajgen-run001 -p"
   ```

2. **Update consumption_ledger.yaml** with actual results once Harbor job completes.

3. **Address throughput problem**: With only 6 new tasks after days of running, the 40-task target needs a strategy change. Options:
   - Switch to repos with simpler test suites (avoid large monorepos like llama_index)
   - Lower `min-source-files` from 3 to 2 in swegen config
   - Increase `cc_timeout` beyond 2400s
   - Collect PRs from repos with known high success rates

4. **Restart swegen-fresh** if more PR diversity is needed (the stuck "All tokens exhausted" loop can be killed and restarted).

---

## Key File Paths

| File | Purpose |
|------|---------|
| `subblock/swegen/artifacts/swe_tasks/py-cc/verifiable_tasks.txt` | Manifest of verified tasks (producer output) |
| `subblock/trajgen/artifacts/consumption_ledger.yaml` | Trajgen processing state (consumer ledger) |
| `subblock/trajgen/artifacts/jobs/` | Harbor job output directories |
| `subblock/trajgen/scripts/start.sh` | Start trajgen (LiteLLM + Harbor) |
| `subblock/swegen/config.yaml` | swegen config (remote node, API, params) |
| `subblock/trajgen/config.yaml` | trajgen config (Harbor, agent, task source) |
